# 2026-09-23

## Pushed 11 commits; measured the per-app budgets gap and the thinking-blocks item

**Pushed.** `6327ecc..7dcbf0f` → `origin/main`. The 11 commits from 2026-09-22 were local-only. `gh` is not
installed on this machine — the Actions API is readable unauthenticated at
`api.github.com/repos/<owner>/<repo>/actions/runs`, which is how the run status was checked.

### Per-app budgets: the documented gap understates the work

`09-status.md` said "Per-app keys exist; per-app limits do not". Measured against the live DB, that is only
half the story — **there is no per-app attribution either**, so a budget would have nothing to sum.

The trap is a name collision. Two different things are called `key_id`:

| | What it is | Join |
|---|---|---|
| `ledger.key_id` | the **provider credential** (`api_keys.id`), e.g. `2f4b37eb-…` | **713** of 792 gateway rows |
| `gateway_keys.id` | the **gateway app key** the client presented, e.g. `ak-fc85350a2fb1dc67` | **0** rows |

The ledger's 16 columns contain no app-key column at all. The first join I tried — `ledger.key_id IN (SELECT id
FROM gateway_keys)` — returned **0**, which reads as "no attribution" but was really "wrong table". The second
join against `api_keys` returned 713/792 and settled it.

So the work is three parts, and the first is a prerequisite nobody documented: (1) migration 0016 adds
`app_key_id` to the ledger **and the TS router must start passing it** — a column alone proves nothing, which
is this project's own rule; (2) a cap column on `gateway_keys`; (3) enforcement, which changes
`SpendProvider`'s signature — it is `Arc<dyn Fn() -> (i64, i64)>`, **zero arguments**, so the gate cannot tell
callers apart today — touching every construction site and test, then `SpendStatus`, the UI and the shim.

### `thinking` blocks: not "quality-only, last" — unmeasurable, like `cache_control`

The plan files it as backlog item 5, "quality-only, last", with no spec. Measured:

- **Nothing exists.** Zero handling anywhere in the codebase.
- **The drop is deliberate and documented** — `gateway_anthropic.rs:204-206`: *"Anything else (thinking,
  images) has no OpenAI equivalent here and is dropped rather than guessed at."* A turn of only-unrecognised
  blocks still emits an empty message, so the assistant turn it answers is not left dangling.
- **Four things are dropped, not one.** The `thinking: {type, budget_tokens}` **request parameter** is
  discarded by `to_chat_body`, which builds a fixed field set (`model`, `messages`, `stream`, `max_tokens`,
  plus `tools`/`tool_choice`/`response_format`) with no passthrough. `thinking` blocks in history are dropped.
  Non-stream responses never emit one. And the stream **does** multiplex blocks — text at index 0, `tool_use`
  at `1 + idx` (`gateway_anthropic.rs:446`) — but only those two kinds, and the ordering constraint is already
  load-bearing: index 0 is closed *before* index 1 opens, because Anthropic orders blocks strictly and an open
  block makes the client's parser wait for a stop that never comes (`:429-432`). A `thinking` block must
  *precede* text, so it would take index 0 and shift every tool index.
  *(Corrected 2026-09-23: this note first claimed "the stream hardcodes a single `content_block_start` … there
  is no block multiplexing". The code contradicts that — the index scheme is right there at `:446`.)*
- **The bridge protocol cannot express it.** `BridgeMsg` is `Delta(String)` · `ToolCalls` · `Result` ·
  `Usage{prompt_tokens, completion_tokens}` · `Done` · `Error`. No reasoning variant. So this is a bridge
  protocol change across Rust *and* TS, not an adapter-local edit.
- **There is nothing to test against.** `SELECT COUNT(*) FROM models_cache WHERE raw_json LIKE '%reasoning%'`
  → **0**, across all 455 cached models. No provider in this install advertises reasoning support.

Same shape as `cache_control`, which the plan parked as "BLOCKED on measurement": **measure → decide →
rework**, and the measurement is not available. Doing it blind means building a protocol change with no
provider that can exercise it.

## CI went red on the pushed commit: toolchain drift, not the code

`7dcbf0f` pushed, and the clippy step failed. **Not formatting** — the new `cargo fmt --check` gate was fine.

Root cause: `dtolnay/rust-toolchain@stable` on the runner is **1.98.1**; this machine was on **1.88.0**. Ten
minor releases, four new lints, no code change in between. Reproduced with `rustup update stable`.

Fixed all four:

- `model_context.rs` — `ModelContextRow` was dead. No command, no TS bridge, no test constructs it; the UI
  shows a *count* (`Memory.tsx:224` → `router_model_context_count`), never rows. Its own doc comment claimed a
  purpose it never had. Removed, plus the `Serialize` import it was the only user of.
- `crash_report.rs` — `is_leap` hand-rolled `% n == 0` → `is_multiple_of`.
- `context_scope.rs` — `sort_by` on one bool → `sort_by_key` + `Reverse`. Both are stable sorts, so the
  documented "everything else keeps `recall_scoped`'s order" property is unchanged.
- `gateway.rs` — `try_slot` returns `Result<Slot, Response>`, over the 128-byte threshold. **Kept it, behind a
  local `#[allow]` with the reason written down.** Boxing would have been the wrong fix: the value is built
  once and consumed once, and the five call sites are **not uniform** — three return it to hyper, two
  (`gateway_gemini.rs:209`, `gateway_anthropic.rs:351`) read only `status()` and rebuild the envelope, one
  maps it. No caller uses `?`. Boxing would add a heap allocation to every refusal, including the at-capacity
  429, which is the one refusal that is hot *by construction*.

Verified: the same clippy command reported 6 warnings before and none after; `cargo fmt --check` exits 0 —
checked **without a pipe**, since `${PIPESTATUS[0]}` is empty in zsh and a piped exit code is `head`'s, which
proves nothing; gate ALL GREEN 14/14.

Also corrected a stale claim in `ci.yml`: it named "the two remaining judgement calls … (a test helper with 8
positional args, three counts from three statements)". The `type_complexity` allow no longer exists — both
sites now return a named type and cite the lint in a comment (`store.rs:323`, `gateway_tests.rs:10`).

**Open decision:** the clippy step runs against whatever `stable` is, so a Rust release can turn CI red with no
code change. Pinning (`rust-toolchain.toml` + the action's `toolchain:` input) would stop that, at the cost of
not adopting new lints automatically. The repo pins nothing today — no `engines`, no `.nvmrc`, no
`rust-version`.

## The open decision resolved *against* pinning, for a reason I had not measured

**The drift paid for itself.** The four lints 1.98 found included genuinely dead code (`ModelContextRow` — no
command, no bridge, no test, and a doc comment claiming a UI that shows a count instead of rows). A pinned
toolchain would never have surfaced it. So the toolchain keeps floating and the fix is legibility, not freezing:
`ci.yml` gains a `Rust toolchain` step and `scripts/ci-local.sh` prints `rustc --version` in its Rust block.
`ci-local.sh` already printed `node -v` for exactly this reason — this is the Rust half of an existing
convention, not a new one. Verified by running the gate: `rustc 1.98.1 (48a229cea 2026-09-01)` at line 219,
15/15 steps PASS.

**CI logs are not readable without auth.** `api.github.com/.../actions/jobs/<id>/logs` returns **403**
unauthenticated and `gh` is not installed, so the step *list* is readable (that is how the failing step was
identified) but no log content is. The "CI is on 1.98.1" attribution was therefore an inference from four
reproduced lints, never a measurement — which is precisely what made the version worth stating in the output.

**And polling the API burns the budget.** Unauthenticated core access is **60 requests/hour**, and a 20-second
poll loop exhausts it in minutes — after which calls fail as `KeyError: 'workflow_runs'` rather than a clear
403, which reads like a parse bug. Two sources still work and do **not** count against the limit: the workflow
badge (`…/actions/workflows/ci.yml/badge.svg?branch=main` → `<title>CI - passing</title>`) and the Actions HTML
page. The HTML page is the better of the two — it carries run number, SHA and status per row, so it can be read
per-commit. **Falsify the method before trusting it**: the same extraction reported `#76/#75/#74/#73` as
`success` and `#72` (`7dcbf0f`, the run known to have failed) as *not* success. Without that control, a matcher
that always answers "success" would have looked identical.

So CI is green on `326f41c` and `63c15ce`, confirmed by two independent sources rather than assumed.

## Two status rows understated their item

Corrected in the docs, because the docs are where the claim lives:

- **`thinking` blocks** was labelled "quality-only, last". That says low *priority*; the measurement says not yet
  *measurable* — a different thing, with a different next action. It is now a "BLOCKED on measurement" row plus
  a paragraph, mirroring how `cache_control` is already treated.
- **Per-app budgets** said per-app *limits* do not exist. True, and one step short: there is no per-app
  *attribution* either, so a budget would have nothing to sum.

**The label was in two places** — `09-status.md` and `PRODUCT_COMPLETION_PLAN.md` §6 — and fixing one without
the other would have been fresh drift. A Grep for the *claim's words* found the copies; a search by filename
would not have.

## ESLint measured — and one of its numbers was an artefact

ESLint is the last gate the plan lists as absent (§4.1), so I measured it rather than arguing about it.
`pnpm install` is destructive in this sandbox, so the packages went into an isolated location; the install
walked up to the nearest `package.json` and landed in `$HOME` instead (see the cleanup note below). The
measurement itself ran fine — config in `$HOME`, project passed by absolute path:

    files linted        : 136
    files with problems : 7
    total messages      : 14   (12 errors, 2 warnings)

    6  @typescript-eslint/no-unused-vars
    3  react-hooks/exhaustive-deps
    2  None            (parse failures)
    2  @typescript-eslint/no-this-alias
    1  @typescript-eslint/no-explicit-any

**Three of those fourteen are not findings about the code.** `react-hooks/exhaustive-deps` cannot come from
`tseslint.configs.recommended` — I checked the installed package and it contains no reference to
`react-hooks` at all. What it is: the repo carries three `eslint-disable` comments naming that rule
(`Assistant.tsx:381`, `:389`, `Settings.tsx:57`), and with the plugin absent ESLint reports each as
*"Definition for rule 'react-hooks/exhaustive-deps' was not found"*, attributed to the rule's own id.

So the real churn is **11 messages**, and the plan's claim that adoption has "nothing to reconcile" is wrong:
adoption needs a **third** dependency (`eslint-plugin-react-hooks`) or the removal of three comments. A linter
that is silent about a rule the code already disables for has a baseline that is wrong on arrival.

The two parse failures are still unnamed. Identifying them means reinstalling the packages, and I had already
begun reverting — that re-run belongs to the adoption work, not to the measurement. Recorded as open rather
than guessed.

Corrected in `PRODUCT_COMPLETION_PLAN.md` §4.1 (two places — the closing note, and the pointer from the
resolved-in-part blockquote) and in `SECURITY_AUDIT_2026-09-20.md` L1's recommended fix, which named two
packages where three are needed.

## `$HOME` contamination from the ESLint install — reverted in part

`npm install` run in a directory with no `package.json` walks *up* to the nearest one. The managed node
workspace has none, so it installed into `~/node_modules` and wrote to `~/package.json` and
`~/package-lock.json`. `~/node_modules` pre-existed with 27 entries (an axios/ws/`@openzeppelin` closure); the
install added ~69 and put two lines into `~/package.json`.

Reverted with npm's own bookkeeping: `npm uninstall eslint typescript-eslint` restored `~/package.json` to its
three original dependencies and shrank `.package-lock.json` from 120 entries to **25** — exactly the predicted
closure, which is what makes the revert verifiable rather than hopeful.

**What npm did not clean up, and why it matters.** The tree still holds ~67 full package directories renamed
to `.name-hash` staging names, plus 8 populated scoped dirs and 6 `.bin` staging links. npm renames victims
*before* deleting them, so the bulk-delete guard refusing the delete leaves the packages on disk under the
staging names while `npm ls` reports success and `npm uninstall` says "up to date". That is a new instance of
the guard trap already in `MEMORY.md`, now recorded there. **Cleaned up on approval, 2026-09-23.** Approved as "move them to Trash". 78 items moved via `/usr/bin/trash`
in batches of 10 with a stop-on-first-failure loop — 63 staging dirs, 8 scoped dirs, 6 `.bin` links, and the
`$HOME` scratch config. The target list was built and **inspected before anything moved**: a grep confirmed no
`@openzeppelin`/`axios`/`ws`/`.vite`/`.package-lock.json` entry was in it, and that exactly one path reached
outside `~/node_modules`.

End state verified: `~/node_modules` back to **29 entries** (4 dotfiles + the 25-package axios/ws closure),
`.bin` empty, `~/package.json` at its three original dependencies, `.package-lock.json` at 25 entries, and the
survivors intact (`@openzeppelin/contracts` 377 files, `axios` 86, `ws` 19).

**A trap worth remembering: `-e` follows symlinks, so it reports npm's `.bin` staging links as absent.** The
first inventory said "0 `.bin` links" when there were 6 — they are *broken* symlinks by construction, so the
test has to be `-L`. The count went from 72 to the correct 78 only after that fix.

**The Trash could not be listed to take a receipt** — `~/.Trash` is TCC-protected (`Operation not permitted`)
and Finder automation is blocked too (`privilege violation, -10004`). So the evidence for "moved, not deleted"
comes from the binary rather than from the Trash: `/usr/bin/trash` is `com.apple.trash` and references
`trashItemAtURL:resultingItemURL:error:` — the `NSFileManager` call that *moves* to Trash. A deleting tool
would reference `removeItemAtURL:error:` instead. Recorded because "gone from where it was" is not the same
evidence as "recoverable", and only the second one was promised.

Also removed: the project-root scratch config `eslint.measure.cjs`.

## MEMORY.md consolidated

Was 7,914/8,000 — 86 bytes of headroom, which is zero in practice. Now **7,805** (195 bytes), and it gained
the `.name-hash` rule. Trims were prose only; every rule survived. The `cargo fmt` churn numbers (638/42% vs
354/30%) left the index — they are recorded in `09-status.md:31` and the plan, so the index no longer
duplicates them. The file is at its practical ceiling: the next rule needs something displaced into
`REFERENCE.md` rather than another round of word-trimming.

## Landed

Committed as `62fcb4d` — 4 files, +117/−11: the two doc corrections, `MEMORY.md`, and this log. Working tree
clean, **not pushed**. `check-doc-links` was verified *before* committing (47 files, 105 links, all resolve),
and it is the only gate step that reads docs, so a docs-only change cannot move the other 14. The full
`pnpm ci:local` was then run anyway rather than argued about — the project's bar is the gate, not a plausible
argument about the gate.

**Per-app budgets reconnoitred, not started.** Two facts that make the work concrete: `SpendProvider` is
`Arc<dyn Fn() -> (i64, i64)>` (`gateway.rs:285`) — **zero arguments**, so the gate physically cannot tell
callers apart today — and `month_spend_micros` (`persist.rs:943`) is deliberately *source*-agnostic by design
("the user sets a budget on what they pay, not on one client"), so per-*app* is a different axis rather than a
narrowing of that one. The three parts the status doc names therefore stand: ledger column + TS router
passing it, then a cap column on `gateway_keys`, then the signature change.

## Per-app attribution — part 1 of 3, landed

Migration 0016, the writer, the TypeScript producer chain, and the bridge. 14 files, +509/−80.

**The bridge was the piece that made "wired" a claim rather than a hope.** `BridgeRequest` (`gateway.rs:302`)
carried `request_id`, `kind`, `body`, `headers` — and no app key. So the column, the `LedgerEntry` field and the
`store.ts` mapping were all correct and the value still could not physically arrive: the ledger is written on the
*webview* side, and the identity was only known on the Rust side. Adding a field to a struct on one side of a
process boundary is not wiring.

**`check_gateway_key` now returns the identity it was already computing** — `Result<Option<String>, GateRefusal>`
where `Ok(Some(id))` is the per-app key, `Ok(None)` is the master key and `Err` denies. The alternative was
calling the existing `app_key_for` at each dispatch site, which would have been six edits instead of nine but
would have made a *second* authority for "who is this" — and the project's own rule is that an identity needs
one authority, not two that must be kept in step. All nine call sites converted; the compiler found every one,
and found the one dispatch site I had missed (`models_h`).

**Four tests, each falsified before being trusted.** The valuable one asserts the key at the *bridge*, not on the
function's return value, because the return value is only half the wiring. Falsified twice: forcing
`app_key_id: None` at the dispatch made it fail `Some(None)` vs `Some(Some("ak-1"))`, and making the lookup
report the first active key instead of the matched one failed `ak-1` vs `ak-2`. The second probe is what proves
the test checks *which* key rather than merely *that* some key arrived.

**The gate failed on `cargo fmt`, and the failure was not from this session.** Both hunks were `query_map`
chains in `persist.rs` and `store.rs` — test code written in the previous session and never formatted. Worth
noting because it is the third time the lesson has landed: uncommitted work is not verified work, and only
running the gate says otherwise. `cargo fmt` then touched exactly those two files.

## The live database is one migration behind, on purpose

Re-measured read-only: `schema_version` **15**, `ledger` has **no `app_key_id` column**, 1530 rows, 1297 of them
`source='gateway'`, and `gateway_keys` holds exactly **one** row (`ak-fc85…`, "Work buddy") that nothing
attributes to. So 0016 is defined and tested and will record nothing until the app is rebuilt and relaunched —
the same shape as D8/0015, which is why `04-data-model.md` now carries the comparison explicitly.

**A stale figure corrected while passing.** The status doc said `ledger.key_id` joins **713 of 792** gateway
rows. Re-measured, the numerator was right and the denominator was not: 713 of **1297**. 792 is the count of
gateway rows with a *non-null* `key_id`, not the row count — two different numbers that a previous reading had
collapsed. Also corrected in the same pass: `store.rs:943-946` → `977-980` (the migration-count assertions moved
when 0016 landed), and the Rust test count 473 → 481.

## Auto context compression — Tier 1 landed

Nothing trimmed a conversation, so a long session failed at the provider with a context-length error. Tier 1
(hard truncation) fixes that: `compressMessages` drops the oldest **complete turns** until the prompt fits a
budget sized from the resolved model's window.

**The finding that shaped it: the two paths converge.** Gateway and assistant look like separate features and
would invite two implementations — but the gateway reaches `router.generateText` through `gateway-bridge.ts`
and the assistant through `runAgentLoop`, so the trim lives there once. This is the "a switch in two places
drifts" rule applied *before* writing anything, and it is why the work is one module plus one call site rather
than two of each.

**Three properties, all pinned and all falsified** (flipped the code, watched the tests fail, reverted):
system survives; the newest turn survives even alone over budget; a turn boundary only falls on a `user`
message, so `tool_calls` and their `tool` results always drop together. Breaking turn grouping failed 1 test;
loosening the loop bound to allow dropping *all* turns failed 3 — including "handles a single message", which
is the one I would not have predicted.

Budget uses the **narrowest known** window across the failover plan, not the first candidate's, because
failover may serve from any of them. Unknown windows fall back to `DEFAULT_CONTEXT_WINDOW` (under-sends).

router-core 249 → **265** tests. Gate green. Committed as `a0e566a` + `3b59150`.

**Tier 2 (summarise instead of drop) is deliberately not done** — it costs an extra model call on the request
path. Recorded as a gap in `09-status.md`, narrowed from "no auto context compression".

## Tier 2 — summarization — also landed

compressWithSummary replaces dropped turns with a compact summary. Key design decisions:

- **One implementation, not two.** Both gateway and assistant converge on `router.generateText`, so the trim
  lives there once. The assistant gets Tier 2 via `opts.summarize`; the gateway stays on Tier 1 (stateless,
  latency-sensitive).
- **`skipCompression: true` on the inner call.** Without it, compressing would trigger a summary, which would
  compress — an unbounded chain. This is the only thing breaking it.
- **The summary lands in the system prefix,** so it survives any later trim. Tested directly.
- **Failure degrades.** A throwing or empty summarizer falls back to Tier 1 truncation. Tested by flipping
  the code: without the catch block, 1 test fails; with it, all pass.

The assistant wires `createSummarizer` into every agent turn via `generate: (req, opts) =>
router.generateText(req, { ...opts, summarize: createSummarizer(chosen) })`. The summarizer calls the same
model with a dedicated prompt, capped at 300 tokens out and 12K chars in.

router-core 265 → **273** tests. Gate green. Committed `2f8e0dd` + `934a6bb` + `8795cd5`.

## Per-app budgets — parts 2 and 3, landed

Migration **0017**, the enforcement, the UI, and six browser specs. 15 files, +~700.

**The design decision worth recording: the two caps are independent, not narrowed.** The obvious
implementation is to compute `min(global_remaining, app_remaining)` and enforce one number. That is wrong, and
wrong in a way that only shows at scale: with a **$10 global** cap and a **$100 per-app** cap, an app that
spends $50 has breached the global limit while sitting at half of its own. A single narrowed number cannot
express "this app is fine, the install is not". So `SpendLimits` carries **two pairs** — `(total_micros,
total_cap_micros)` and `(app_micros, app_cap_micros)` — and `spend_gate` checks them **sequentially**, global
first.

**And the two refusals stay distinguishable** (`spend_cap_exceeded` vs `app_budget_exceeded`) even though both
are `402 insufficient_quota`, because the remedies are opposite: one says "raise the global cap", the other says
"raise *this app's* budget". A caller that cannot tell them apart cannot act.

**`NULL` is the only representation of "no cap".** `gateway_key_cap_set` normalizes `<= 0` to `NULL` rather
than storing a zero. The alternative — `NOT NULL DEFAULT 0` — collapses "not reported" and "explicitly zero"
into one value, and two spellings of one state is the defect the project already recorded for 0015. A test
(`clearing_a_per_app_cap_stores_null_not_zero`) exists for exactly this, and it is the *only* test that can see
it: the behavioural test passes either way.

**The index ships in 0017, not 0016, and 0016 says why.** 0016 declined to ship a column with an index for a
query nobody had written. 0017 is that query. `idx_ledger_app_key_ts` on `ledger(app_key_id, ts)`.

### Two real bugs the new work exposed, both pre-existing in kind

1. **`shim.ts:toRustArgs` renames top-level keys to snake_case *before* `dispatch`.** My new case read
   `args.capMicros`, so `Number(undefined) || 0` was always `0` and setting a budget silently cleared it.
   Confirmed the convention against working cases (`args.value_json`, `args.run_id`), fixed to
   `args.cap_micros` — and then found **the identical defect in the pre-existing `gateway_spend_cap_set` case**,
   which had been reading `args.capMicros` since it was written. Nothing could catch it: the only spec
   exercising the global cap seeds it through `__webTest.spendStatus` and never calls the command. Two latent
   bugs, one file, same cause.
2. **A refresh that re-seeds a draft field wipes what the user typed.** The mount-time `gateway_app_keys`
   response resolved *after* the user began typing and re-seeded `capDrafts`, so the save sent `0`. Fixed by
   seeding only keys not already present (`prev[k.id] ?? …`) plus an optimistic local write. **This was a real
   product bug that the new browser spec caught on its first run** — the UI looked correct and the write went
   nowhere.

### Seven falsification probes, one at a time

Each disabled exactly one mechanism; each failed exactly the naming tests and left the others passing.

| # | Mechanism disabled | Tests that failed |
|---|---|---|
| A | per-app check | `a_per_app_cap_refuses_only_the_app_that_reached_it` |
| B | missing cap defaulted to `0` — **did not fail** | none |
| B′ | B **plus** the `cap > 0` guard | both per-app tests |
| C | identity hardcoded to `ak-1` | the two identity-dependent tests |
| D | global check | `r4_spend_cap_blocks_at_threshold`, `the_global_cap_refuses_an_app_that_is_under_its_own_cap` |
| E | index renamed | the index assertion only |
| F | clear stores `0` | `clearing_a_per_app_cap_stores_null_not_zero` |
| G | app predicate dropped from the `SUM` | the scoping test |

**Probe B is the one that paid.** I had written a doc comment claiming a defaulted `0` "would refuse every
request from an app that has no cap at all". B removed the `Option` and **all 113 tests still passed** — the
claim was false, because the `cap > 0` guard short-circuits first. Rather than record a mechanism that does not
exist, the probe was refined to B′ (remove *both* defences), which failed both tests. The doc comment and the
test docstring were then rewritten to describe the **observable property** and to state that two independent
defences guard the uncapped case. This is the "a reason is a claim too" rule catching its own author.

### Measured end state

- Rust lib **481 → 489**; browser **98 → 104**
- `pnpm ci:local` — **ALL GREEN**; `clippy --all-targets -- -D warnings` clean; `cargo fmt --check` clean (0 bytes with `-- --color=never`)
- `pnpm typecheck` and `web-test:types` both exit 0; `docs:book` regenerated (10 chapters, 121 ids); `check-doc-links` 47 files / 105 links resolve
- Docs updated: `CHANGELOG.md`, `09-status.md` (gap row removed), `07-drift-register.md` (D4 closed), `ARCHITECTURE.md:843`

### The Playwright timeout was the proxy again, not the guard

Both `webServer` timeouts this session came from **all six proxy vars** pointing at `http://127.0.0.1:63517`.
`DEBUG=pw:webserver` showed vite up in 835 ms while the readiness probe got 404; a manual curl gave 200 once and
502 the next, which proves the proxy. **A proxy `502` satisfies a "status != 000" readiness loop**, so the loop
exits instantly and every probe after it measures the proxy — the rule is already in `MEMORY.md` and it still
cost time, because the *symptom* (`webServer` timeout) is the same one the bulk-delete guard produces. Fixed by
unsetting all six on every Playwright invocation.

### Correction to the status doc, found while verifying rather than trusting

`09-status.md` contradicted itself: line 26 said 0016 was "defined but not yet applied", line 42 said
attribution "landed 2026-09-23". Measured read-only against the live DB: `schema_version` **15**,
`ledger.app_key_id` columns **0**. Line 26 was right; line 42 was misleading and is now corrected. The installed
app is still one migration behind by design — it records nothing until rebuild and relaunch, the same shape as
D8/0015.

**Not closed, and not closable by code:** the notarized release. It needs Apple Developer ID credentials
(`APPLE_CERTIFICATE`, `APPLE_CERTIFICATE_PASSWORD`, `APPLE_ID`, `APPLE_PASSWORD`, `APPLE_TEAM_ID`) provisioned
as repository secrets. Nothing in this session's work touches it, and no further code change can.
*(Corrected later the same day: "no further code change can" was wrong, and the section below says what it was
wrong about. The code half of this gap was closable and is now closed; only the credential provisioning
remains, and that one genuinely is an account action.)*

## The release gap — the code half closed

The gap row said "**No notarized release**". That named the **symptom** and implied the pipeline was missing. It
was not missing; it was **unfalsifiable**, and that was the real defect: `tauri build` succeeds with **no**
Apple secrets at all and emits an **ad-hoc signed** app. A tag push therefore produced a green job and a draft
Release containing something macOS refuses to launch, and the failure surfaced on a user's machine rather than
in CI. `05-workflow.md` warned about it in prose; nothing enforced it. So the row is now "**Release not
provisioned**", which names what is actually left.

### The finding that shaped everything: `codesign --verify` proves nothing

Measured against an ad-hoc bundle:

    codesign --verify --deep --strict --verbose=2 /tmp/adhoc-test.app
      /tmp/adhoc-test.app: valid on disk
      /tmp/adhoc-test.app: satisfies its Designated Requirement
      exit 0

An ad-hoc signature **is** a valid signature. A verifier that stops there is decoration. What actually
separates the two states: `spctl` (exit 3 vs 0 — `rejected` vs `accepted` + `source=Notarized Developer ID`),
`stapler validate` (exit 65 vs 0), the `CodeDirectory` flags word (`0x2(adhoc)` vs `0x12a00(…,runtime)`), the
`Authority=Developer ID Application:` chain, and `TeamIdentifier` (`not set` vs a real team).

Two measurement errors corrected along the way, both of which would have produced a wrong rule:
- The flags word sits **on the `CodeDirectory` line**, not on a `flags=` line of its own — a grep for `^flags`
  matched nothing, which I nearly read as "Chrome has no hardened runtime".
- **`$?` after a pipeline is the last command's status, and `PIPESTATUS` is empty in zsh.** Both printed
  `exit=0` for a command that had in fact exited 3 and 65. Re-measured with `out=$(cmd 2>&1) || rc=$?`.

### Two scripts, each falsified in both directions

`scripts/release-preflight.sh` runs **first**, before the Rust toolchain download — it costs about a second and
catches the one mistake that otherwise survives a 20-minute universal build. Fails on a missing or empty secret,
when the `.p12` will not open with the given password (a truncated paste or a mismatched password, caught before
the build rather than at the signing step), and when `bundle.macOS.signingIdentity` is pinned in
`tauri.conf.json` — which is the rule no other job can enforce, because nothing outside `release.yml` runs a
full `tauri build`. Never prints a secret; only names and shapes.

`scripts/verify-release-signature.sh` runs **after** the build and reads the artefacts back. On failure the job
goes red **and** the draft release is deleted, so a bad artefact cannot be published by someone who only sees
that a release exists.

Probes, each run deliberately: ad-hoc bundle → **6 failures**; Chrome (real Developer ID + notarized) → **all
pass**; wrong expected team → **exactly 1** failure; garbage base64 → **1** failure; a pinned identity in a
throwaway root → **exactly 1**, with the same config unpinned as the control. The Chrome control is the one
that matters — without a *passing* example, a script that fails on everything looks identical to a correct one.

### Two bugs found by running the scripts, not by reading them

- **`mapfile` does not exist in bash 3.2.** macOS ships bash 3.2, so `#!/usr/bin/env bash` gives you 3.2 here
  and on a `macos-14` runner. The discovery branch died with `mapfile: command not found` — and only on the
  **no-argument path**, which every earlier probe had skipped by passing a path. Replaced with a
  `while IFS= read -r` loop over process substitution.
- **`hdiutil` leaves scratch images.** A failed DMG build deposits `rw.<pid>.<Product>_<v>_<arch>.dmg` files in
  `bundle/macos/`. The first discovery run found **13**, every one unsigned, and reported **35** bogus failures
  that buried the single real finding. The glob now excludes `! -name 'rw.*'`.

### Config: measured, not guessed

`bundle.macOS` field names came from the schema itself (`https://schema.tauri.app/config/2.11.6`) — the docs
pages truncate the `MacConfig` definition, so guessing would have been wrong. `signingIdentity` is deliberately
absent and now mechanically enforced as absent; `hardenedRuntime` is explicit.

**`minimumSystemVersion` is "11.0" because the binary says so.** `VersionMin=720896` decodes to
`major<<16 | minor<<8 | patch` = **11.0.0**. Declaring the schema default (10.13) would have been a lie —
offering the app to systems the binary cannot run on.

### Docs

- **`CONTRIBUTING.md`** gained a "Releasing" section with the full one-time provisioning. This fixed a
  **dangling reference**: `release.yml` had said "Required secrets (see CONTRIBUTING.md)" and `CONTRIBUTING.md`
  documented no `APPLE_*` secrets at all — a maintainer following the workflow's own pointer found nothing.
- **`07-drift-register.md`** — logged as **D11**.
- `09-status.md` (gap row + Governance row), `05-workflow.md`, `CHANGELOG.md`; `docs/dev-book/book.html`
  regenerated (10 chapters, 122 ids).

### Measured end state

`pnpm ci:local` — **ALL GREEN**; 104 browser tests; `check-doc-links` 47 files / 105 links resolve.

**What remains, and it is genuinely not code:** creating the Developer ID certificate, exporting and base64-ing
the `.p12`, creating an app-specific password, and setting six repository secrets. That is an Apple Developer
account action. The pipeline will refuse to produce a release until it is done, which is the difference from
before — previously it produced one nobody could open.

## Rebuilt, reinstalled, relaunched — the installed app is no longer behind

Committed as three separate commits rather than one, because they are three different kinds of change:

- `44508a5` `feat(gateway): per-app monthly budgets (parts 2 and 3)` — 11 files, +953/−42
- `b44bfc7` `ci(release): refuse to publish an artefact that is not notarized` — 5 files, +427/−3
- `c6b673b` `docs: record per-app budgets and the release guards` — 9 files, +1408/−909

Then `pnpm tauri build` (4m17s), installed to `/Applications`, relaunched. **The old app was moved to `/tmp`
rather than deleted** — the same convention `build:clean` and `web-test:clean` use, and the reason is the
sandbox bulk-delete guard, not tidiness.

**The DMG step failed exactly as `REFERENCE.md` records it** (`bundle_dmg.sh`), and the `.app` was already
complete. Took the `.app`, as the note says to.

Measured after relaunch, read-only:

| | before | after |
|---|---|---|
| `schema_version` | 15 | **17** |
| `ledger` columns | 17 | **18** (`app_key_id` present) |
| `gateway_keys` columns | 5 | **6** (`cap_micros` present) |
| `idx_ledger_app_key_ts` | absent | **present** |
| rows with `app_key_id` | — | **0**, which is correct: attribution begins on the first request after the rebuild |

The single existing key (`ak-fc85350a2fb1dc67`, "Work buddy") reads `cap_micros` **NULL** — uncapped, which is
the state the nullable design was written to produce. Two spellings of "no budget" would have shown up here as
a `0`.

**The gateway is live, and proving it took reading the body.** My first probe of `http://127.0.0.1:8800` gave a
confusing mix: `/v1/models` → `000`, `/health` → `429`. Both were explained by the response bodies rather than
by the status codes — `/health` is **401 `invalid_api_key`**, which this project's contract says is the *healthy*
signal, and the 429 was `too many failed auth attempts — backing off` with `retry-after: 30`, triggered by my own
unauthenticated probes. This is the "three different 429s — read the body" rule, and also a reminder that an
unauthenticated probe loop is itself an input to the system it is measuring.

The gateway log confirms a clean startup: `setup complete`, `auto-restore: restored on port 8800`, `enabled on
port 8800`, `startup: key refs probed`.

## Pushed — and the badge lied until the commit was checked

`8795cd5..5757c52` → `origin/main`. CI run **#81** for `5757c52` finished `status=completed, conclusion=success`.

**The badge was not evidence about this push.** `.../ci.yml/badge.svg?branch=main` said `<title>CI - passing</title>`
while run #81 was still `in_progress`, because the badge reports the latest **completed** run — #80, on the
*previous* commit. A green badge immediately after a push is therefore consistent with your run still running
and with your run being red. It only becomes evidence once the run for **your** head SHA is completed.

So the check is keyed on the SHA, not on the branch status. One unauthenticated call:

    api.github.com/repos/<owner>/<repo>/actions/runs?per_page=6
    -> #81 5757c52 in_progress null      <- ours, still running
       #80 8795cd5 completed  success    <- what the badge was reporting

**And the method was falsified before being trusted.** A matcher that always answers "success" looks identical
to a correct one, so the same query was run with `?status=failure`, and it returned three real runs — **#72
(`7dcbf0f`), #55, #50** — each `conclusion=failure`. #72 is the run this project already knew had failed, which
is what makes it a working control rather than a plausible-looking one. Then the run was polled by id
(`.../actions/runs/35835859285`) until it completed.

Three API calls total, against a 60/hour unauthenticated budget — cheap because the page was not polled in a
loop. The badge and the Actions HTML page remain usable and do not count against that budget, but the HTML page
turned out to be hard to key reliably: the run number and the status token are far apart in the row, so "the
nearest status token" associated a token from a different run. The API is the source that answers per-SHA.

## Headless service mode plan — chapter 10 added to dev book

**User asked for the plan first, before implementation.** Created `docs/dev-book/10-headless-service.md`
(694 lines) and added it as chapter 10. Pushed as `7ac97e3`.

**The plan is comprehensive and evidence-based.** It measures the current architecture (5,880 lines
of router-core TypeScript, 25,612 lines of Rust), maps every component, estimates effort (3-4 weeks),
and records 4 decisions.

## Cross-platform tech choice — chapter 11 added, FreeLLMAPI analysed

**User wants cross-platform from the start, not macOS-only.** They referenced
`tashfeenahmed/freellmapi` as an example. Analysed the repo: FreeLLMAPI is TypeScript end-to-end
(Node.js + Express server, React + Vite dashboard, Electron-style tray app). The server is a
standalone Node.js process; the tray app merely manages it.

**Key finding: we cannot copy FreeLLMAPI's architecture without abandoning the key-blind security
model.** FreeLLMAPI handles credentials in Node.js. Our project handles them in Rust with OS
keychain integration. Moving the gateway to Node.js would break the invariant that the TypeScript
layer never sees a raw secret.

**Three paths evaluated in `docs/dev-book/11-cross-platform-tech-choice.md`:**

| Path | Description | Verdict |
|---|---|---|
| A | Port router core to Rust | **Recommended** — preserves security model, single binary, cross-platform by default |
| B | Embed JS runtime (deno_core / rquickjs) | Rejected — complex, unstable, two GCs in one process |
| C | Node.js subprocess | Rejected — +40MB bundle, two HTTP hops, splits security boundary |

**Comparison matrix covers 10 criteria:** time to implement, router core changes, binary size,
memory, cross-platform ease, security boundary, performance, debuggability, reliability, long-term
maintainability. Path A wins on 8 of 10.

**Cross-platform process management (revised from macOS-only):**
- macOS: launchd LaunchAgent (low effort)
- Windows: Background process (v1), Windows Service optional later (medium effort)
- Linux: systemd user service (low effort)

**Four decisions made:**
1. Commit to Path A (port to Rust) — yes
2. Drop Tier-2 adapters temporarily if `rquickjs` fails — yes, behind feature flag
3. Ship headless as default — no, opt-in for one release
4. Windows Service vs background process — background process for v1

**Phased approach revised: 11-17 weeks total.** Phase 1 (extract core lib) includes a CI gate that
builds the binary on `macos-latest`, `windows-latest`, and `ubuntu-latest`.

Pushed as `f7fdf2c`. Book: 249.9 KB, 12 chapters, 187 ids, 109 links.
of router-core TypeScript, 25,612 lines of Rust), maps every component (what stays, what moves,
what is deleted), estimates the effort (3-4 weeks, 6 phases), and records 4 decisions that must be
answered before implementation starts.

**Key finding from writing the plan:** the gateway is already a Rust HTTP server. What makes it
dependent on the UI is the **router core** (execution engine, route planner, model router, context
compression, adapter runtime) — all in TypeScript, all running in a hidden webview. Moving these
to Rust is the work. Everything else is wiring.

**The biggest open question:** whether `rquickjs` or `boa` can run the existing Tier-2 adapter
sandbox. The contract suite is the test. Until that spike is done, the plan treats it as a
decision point with a subprocess fallback.

**The phased approach is designed for safety:** Phase 1 extracts the core library (no behaviour
change), Phase 2-4 port the engine (tests first), Phase 5 deletes the bridge, Phase 6 adds process
management. A feature flag (`webview-gateway`) keeps both code paths for one release, making
rollback a one-line change.

## Release gap reframed: blocked on a purchase, not pending work

**Measured 2026-09-23.** `security find-identity -v -p codesigning` shows exactly **one** identity:
`AI-Provider IDE Dev Signing`, and it is **self-signed** (subject == issuer) — a local dev certificate
that cannot notarize. `gh` and `brew` are both **MISSING** from this machine.

The user has **no paid Apple Developer membership** ($99/year, confirmed via web search 2026-09-23).
Without it, no Developer ID Application certificate can exist, and the preflight script correctly refuses
the build. The code half is complete: `scripts/release-preflight.sh`, `scripts/verify-release-signature.sh`,
and the `release.yml` workflow are all pushed and CI-green (#82). The gap is **not provisionable yet**,
not "not yet implemented."

**What was done:**
- `CONTRIBUTING.md` → new `## Releasing` section with one-time provisioning steps, the $99/year cost,
  and the explicit statement that without the membership none of the steps can start.
- `docs/dev-book/09-status.md` → gap row changed from "Release not notarized" to
  "**Release not provisionable yet** | **Blocked, not pending.**" plus a "What 'blocked' means"
  paragraph covering local builds, the preflight refusal, and the purchase-not-task framing.
- `docs/dev-book/07-drift-register.md` → new **D11** recording the dangling `CONTRIBUTING.md` reference
  (the workflow pointed at CONTRIBUTING.md for secrets before the file documented them).
- `docs/ARCHITECTURE.md` → signing/notarization risk downgraded from **major** to **minor**
  (the pipeline ships; what is missing is the certificate, not the code).
- `CHANGELOG.md` → added the self-verifying release pipeline entry.
- Regenerated `book.html` (194.8 KB), `check-doc-links` passes (105 links).
- Pushed as `fb38d1e`.

**Scripts falsified in both directions:**
- Ad-hoc bundle → 6 failures; Chrome (real notarized) → all pass.
- Wrong team → exactly 1 failure; garbage base64 → 1 failure.
- Pinned identity in `tauri.conf.json` → preflight catches it (unpinned control passes).

## Phase 1 pre-flight — the prompt's module layout contradicts the code in 4 places

Read-only reconnaissance against `fb38d1e`-descendant tree (`main` == `origin/main`, one untracked file:
`AGENT_PROMPT_HEADLESS_SERVICE.md`). Baseline **green: `cargo test` 489 passed, 0 failed**, rustc 1.98.1.

Rule: **Step 1 of the prompt is "read, do not write"** — so nothing was moved. What follows is the measured
dependency map the prompt asks for in its Step 2.

### The tauri:: map (Grep tool counts, not bash grep — the shim reported zero files again)

| File | `tauri::` refs | Prompt says | Verdict |
|---|---|---|---|
| `commands.rs` | 61 | tauri/ | agrees |
| `gateway_cmds.rs` | 37 | tauri/ | agrees |
| `lib.rs` | 16 | tauri/ | agrees (`generate_context!`, tray, `RunEvent`) |
| **`persist.rs`** | **32** | **core/** | **conflict** — 32 `#[tauri::command]` fns, `use tauri::State`, `use crate::commands::CommandError` |
| `workbuddy.rs` | 6 | *unlisted* | tauri/ |
| `tools.rs` | 4 | *unlisted* | tauri/ |
| **`egress.rs`** | **1** | **core/** | **conflict** — `stream()` takes `tauri::ipc::Channel<StreamEvent>` |

Tauri-free: `gateway.rs` + its 9 `#[path]` submodules, `store.rs`, `vault.rs`, `injection_log.rs`,
`capture.rs`, `context.rs`, `crash_report.rs`, `memory.rs`, `orchestrator.rs`, `skills.rs`.

**`gateway.rs` already has the seam the split needs.** `pub trait Bridge { dispatch, cancel }`
(`gateway.rs:568`); the only production impl is `EventBridge` (`gateway_cmds.rs:82`). So the HTTP core is
already Tauri-independent — the split is mostly file moves.

### Four blockers that need a decision before any file moves

1. **`/health` does not exist.** The prompt's §8.3 proof-of-work is `curl :8800/health` → 200
   `{"status":"ok"}`. `spawn()` registers exactly 7 routes (`gateway.rs:1904-1911`), none of them `/health`;
   the fallback is `unknown_route` (404, after auth). Adding it violates the prompt's own §8.2 ("the HTTP API
   surface — 7 routes, same responses — must NOT change") and constraint #1 (no behaviour change). The prompt
   contradicts itself. *(Note: `/health` unauthenticated returns 401 `invalid_api_key`, which this project
   already records as the healthy signal — so a naive probe misreads it.)*
2. **No bridge exists for headless mode.** `GatewayCore::new(bridge, key_provider)` requires
   `Arc<dyn Bridge>`; `is_available() = is_running() && beat_is_fresh()`. With no webview there is no
   heartbeat, so every chat route returns **503**. `aiproviderd` can bind and serve, but cannot answer a
   completion until Phase 2 ports the router core. The binary sketch has no bridge decision in it.
3. **`DEFAULT_PORT` is 8787, not 8800** (`gateway.rs:36`), used at `gateway_cmds.rs:274`. 8787 is AI Hub v2's
   port. A binary that hardcodes it binds the wrong port. It must read the persisted setting like
   `persisted_gateway_port` does.
4. **CI has no matrix.** `ci.yml` is one job, `checks`, on `macos-14` only. The prompt says "add to the
   existing matrix or create a new job" — there is no matrix, so this is a **new job** with a 3-OS matrix.

### Secondary facts worth carrying into the work

- The prompt's file list accounts for **15 of 29** `.rs` files. 14 are unlisted (~14k lines); 9 of those are
  `#[path]` submodules of `gateway.rs` and move with it automatically.
- `build.rs` is `tauri_build::build()` unconditionally, and `tauri` is an unconditional dependency — so
  `cargo build --bin aiproviderd` still *compiles* Tauri. The binary is Tauri-free in **source**, not in
  **dependency**. Real decoupling needs a feature flag or a separate crate; the prompt forbids new deps and
  never mentions features.
- `gateway_tests.rs:1947` uses `crate::gateway_cmds::GatewayState` — a **core → tauri** edge in test config
  only. Does not block `--bin aiproviderd`, but "core must not import tauri/" is then false under `cargo test`.
- Prompt says "104 Rust tests"; measured **489**. Also stale: `README`/docs figures.
- `aiproviderd --version` (§9.2) — the binary sketch has no argument parsing.
- A new binary is a new code signature → **a fresh keychain ACL prompt** for the master key on first run.

### Decisions taken (all three the recommended option)

| Blocker | Decision |
|---|---|
| `/health` | **Add it, tightly scoped** — one new route returning `{"status":"ok"}`. Accepted as the single documented behaviour change: §8.2 amended, drift entry logged. The plan (§4.2) needs it for service discovery anyway. |
| `persist.rs` + `egress.rs` | **Both stay in `tauri/`.** Zero logic edits. `gateway.rs` imports neither, so the service loses nothing. Splitting is deferred to Phase 2. — **FALSIFIED, see below.** |
| `DEFAULT_PORT` 8787 ≠ 8800 | **Binary-local default.** `aiproviderd` reads the persisted gateway setting first and falls back to **8800**. `DEFAULT_PORT` stays 8787 — the Tauri app is untouched. |

Implied and not separately asked, because the prompt already answers it: the binary gets a **no-op bridge**,
so it starts, binds and serves while `/v1/chat/completions` returns 503 — which is exactly what the prompt's
Phase 1 claims to prove ("the HTTP server can start without Tauri"), and is honest about the router core not
being ported yet. CI gets a **new job**, not a matrix edit.

### Correction: `gateway.rs` DOES import `persist` — my own premise was wrong

I told the user "gateway.rs imports neither, so the service loses nothing". **That is false, and it was
verifiable.** My first grep was `^\s*use\s+crate::`, which only matches *top-of-file imports*; every one of
these is an **inline path** inside a function body, so the grep could not see any of them:

    gateway.rs:217   crate::persist::active_gateway_key_ids(&store)   // vault_app_key_provider
    gateway.rs:316   crate::persist::app_month_spend_micros(&store,id) // vault_spend_provider
                     crate::persist::gateway_key_cap(&store, id)
                     crate::persist::month_spend_micros(&store)
                     crate::persist::spend_cap_micros(&store)
    gateway.rs:907   crate::persist::active_gateway_key_ids(s).ok()

Rule restated (and it is the same shape as the `DEFAULT_PORT` trap): **a `use` statement is one way to name a
dependency, not the only way.** Mapping a module graph needs `crate::\w+` over the whole file, not `use crate::`.

**The consequence is a cycle, not a tie.** `persist` cannot be placed on either side:

- persist → `tauri/` ⇒ `gateway.rs` (core) → persist (tauri) ✗
- persist → `core/` ⇒ persist imports `crate::egress::EgressState` (3 command signatures + `recompute_allow`)
  and `crate::commands::CommandError`, so core → tauri anyway ✗

So {`persist`, `egress`, `CommandError`} is one cluster that must sit with `gateway.rs`, or be split.
`CommandError` is `pub struct CommandError(pub String)` (`commands.rs:25`) and its `From<egress::EgressError>`
impl is the only edge that genuinely needs `egress` — and a trait impl may live in **any** module of the
defining crate, so that impl can stay in `tauri/commands.rs` while the struct moves to core.

The five functions `gateway.rs` needs are plain `pub fn`s over `&Store`, not commands:
`active_gateway_key_ids` (`:904`), `gateway_key_cap` (`:965`), `month_spend_micros` (`:1015`),
`app_month_spend_micros` (`:1037`), `spend_cap_micros` (`:1082`). Test-only extras: `gateway_key_insert`,
`gateway_key_revoke` (used by `context_scope.rs` and `gateway_tests.rs` under `cfg(test)`).

## Phase 1 landed — core/tauri split + aiproviderd

**`cargo test` 489 → 489, clippy `--all-targets -D warnings` clean, `cargo fmt --check` clean,
`cargo build --bin aiproviderd` exit 0.** The test count is byte-identical before and after, which is the
whole point of a structural-only phase.

### Two bugs that only a real `tauri build` finds

1. **`[[bin]]` broke the bundle.** `cargo check`, `cargo test`, clippy, fmt and `cargo build --bin
   aiproviderd` were **all green** while `tauri build` failed: *"failed to find main binary, make sure you
   have a `package > default-run` in the Cargo.toml file"*. Two binaries in one package and no `default-run`
   leaves Tauri unable to name the app binary. Fixed with `default-run = "ai-provider-router"`. **A green
   `cargo` gate is not evidence that the app bundles.**
2. **`pnpm build` is not `tauri build`.** The prompt says "`pnpm build` (which runs `tauri build`)". It does
   not: the desktop script is `build:clean && tsc && vite build` — frontend only, 17s. The bundle is
   `pnpm --filter ai-provider-router-desktop tauri build` (~4 min). Another stale claim in the prompt.

### The `/health` proof, run rather than asserted

    AIP_DATA_DIR=/tmp/aipd-test ./target/debug/aiproviderd
    curl http://127.0.0.1:8800/health          -> 200 {"status":"ok"}
    POST /v1/chat/completions                  -> 503

The 503's **body** said `master key unavailable — the OS keychain did not respond; approve the keychain
prompt for this app` — the new-binary code-signature ACL prompt already in `MEMORY.md`, which precedes auth.
It is 503 either way, but the reason matters: it was the keychain, not the missing bridge.

### Small things worth keeping

- **`pub mod` exposed a latent lint.** `InjectionLog::len` is `#[cfg(test)]`, but making `core/` public made
  the type public, so `clippy::len_without_is_empty` started firing. Fixed by adding the matching
  `#[cfg(test)] is_empty` rather than an `#[allow]` — the lint was right, just late.
- **`persisted_gateway_port` moved to `core/gateway.rs`.** Two processes now read it; two copies would drift.
  The app calls `gateway::persisted_gateway_port`, the binary falls back to **8800** (not `DEFAULT_PORT`
  = 8787).
- **`CommandError` extracted to `core/error.rs`.** Its `From<egress::EgressError>` impl stayed in
  `tauri/commands.rs` — a trait impl is visible crate-wide regardless of which module it is written in, so
  the type can be core while its glue-side conversion is not.
- **340 `crate::` paths rewritten by script**, 22 files, no unmapped module. One manual correction: the
  script sent `persist.rs` to `crate::tauri::commands::CommandError` (a private import) instead of
  `crate::core::error::CommandError` — E0603.

### Docs

`10-headless-service.md` §2.1.1 (the actual layout and the three ways it deviates from the prompt),
`09-status.md` (new row + 4 moved paths + the "Needs improvement" paragraph now says *why* it stays),
`03-contracts.md` (7 → 8 routes), `02-architecture.md`, `04-data-model.md`, `CHANGELOG.md`, and drift
entries **D12** (the prompt's module layout and "104 tests" are false) and **D13** (~230 stale `file:line`
citations across 23 files — live dev-book chapters updated, dated audit/plan snapshots left as history
because rewriting them would misrepresent what was measured and when).

`check-doc-links` 50 files / 112 links resolve; `docs:book` rebuilt — 12 chapters, 188 ids.
