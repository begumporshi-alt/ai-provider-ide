# 2026-09-19

## Closed: the zero-cost bug (it was three defects, not one)

Yesterday's note ended with tokens still recording 0 "STILL UNEXPLAINED". Resolved. Three
independent defects sat on the same path, which is why each fix looked like it hadn't worked:

1. Nothing asked the provider for usage on a stream (`stream_options` never sent). Fixed in
   `29ba465` via `stream.requestUsage` + a dialect default for manifests stored before the flag.
2. `execution-engine` swallowed the caller's `onUsage`, so the gateway bridge never learned the
   counts even when the engine had them.
3. **The real cause:** the interpreter `return`ed as soon as it saw `finish_reason`, but
   OpenRouter delivers usage on a *later* chunk. Measured order:
   `content -> finish_reason=stop, usage=null -> usage={...}, choices=[] -> [DONE]`.
   Fixed by `continue`-ing past finish_reason when usage was requested.

`1c149e0` covers 2 and 3. Runtime proof: `tokens_in 311 / tokens_out 3` where every row had
been 0/0.

## Closed: cost was still 0 even with tokens — pricing never survived a restart

Second, independent cause. `pricing_json` was in the schema and `ModelCatalog` computed prices
correctly, but the cache round-trip dropped them:

- `persist::ModelRow` had no pricing field; `models_cache_replace` (INSERT + ON CONFLICT) and
  `models_cache_list` (SELECT) both omitted the column.
- **Why the gateway specifically:** `gateway-worker.ts` calls `bootstrap()` and never calls
  `refreshCatalog`, so it priced every request purely from that persisted column — always
  unknown, always 0. The UI at least prices correctly for the rest of a session in which a
  refresh happened.

`a2e3d87` persists `pricing_json` and parses it back on hydrate (NULL stays NULL — unknown is
not free). Also extracted `replace_models`/`list_models` taking `&Connection` so they can be
tested without a Tauri `State`.

Verified: 440/457 OpenRouter models priced (17 publish none); a gateway request logged
`316 in / 2 out / 49 µUSD`. Check: `316*150_000 + 2*600_000` /1e6 = 48.6 -> 49. The R4 monthly
spend cap can now actually fire.

## Bare `openai/gpt-4o-mini` did not route — real bug, now fixed

Tushu added a WorkBuddy custom model with `{"id":"openai/gpt-4o-mini"}`. It 404'd:
`no route for model "openai/gpt-4o-mini" (no enabled provider carries it)` — even though the
model WAS in the cached catalog. `openrouter/openai/gpt-4o-mini` worked fine.

Cause: `resolveWanted` read everything before the first `/` as a provider slug. No provider named
`openai`, so it pushed no candidate — and the bare-native-id branch was gated on
`!model.includes("/")`, so it never ran either. OpenRouter's native ids contain a slash, which
makes a bare native id indistinguishable from a qualified one on the wire.

Fix (`ef98827`): track whether the id actually qualified. Read the slash as a qualifier only when
it names a real provider; otherwise fall through to the bare-id lookup. A genuinely qualified id
still short-circuits, so nothing can be silently rerouted to another provider. 3 tests, incl. a
guard that a qualified id is not rerouted even when a model literally named
`openrouter/openai/gpt-4o-mini` exists in the catalog.

Verified on the rebuilt app: `openai/gpt-4o-mini`, `custom-local:openai/gpt-4o-mini` and
`openrouter/openai/gpt-4o-mini` all return 200 with usage.

## Two models.json files, and the skill had it wrong

- `~/.workbuddy-ai/models.json` — top-level **list**; this is where Tushu's UI-added entry landed
  (mtime 01:21) and what the UI text claims. Treated as the live one.
- `~/.codebuddy/models.json` — `{"models": [...]}` shape; referenced 9x in app.asar (legacy).
  This is where I added entries earlier, so those may never have been visible.
- They are separate real directories, **not** symlinks (`os.path.islink` = False for both).
- Bundle scan for exact path strings: 7x `~/.codebuddy/models.json`, 2x `/.codebuddy/models.json`,
  1x `~/.workbuddy/models.json`, zero `.workbuddy-ai` — so the bundle alone does not settle it;
  the UI's own text plus observed write behaviour is the better evidence.
- Skill `workbuddy-custom-provider` said `~/.codebuddy/models.json` flatly and called
  `~/.workbuddy/models.json` wrong. Corrected to document both, plus the slash-id pitfall and
  the `supportsReasoning` caveat.

## The gateway now maintains its own client entry (no more hand-editing)

Tushu asked for something dynamic instead of hand-setting `supportsReasoning` and the token caps.
`9b9c296` + `bfa1e9f`:

- **New module `src-tauri/src/workbuddy.rs`.** On gateway enable it rewrites our entries in
  `~/.workbuddy-ai/models.json` from the catalog + keychain: url/port, apiKey, maxInputTokens,
  maxOutputTokens, capability flags. Runs every launch for free since the gateway auto-restores.
- **Only ours is touched.** Ours = by id, or by pointing at our endpoint (that is what retires a
  stale entry). Another local proxy on another port is left alone. Unparseable file, or neither a
  list nor `{"models":[...]}`, is an error — never clobbered. Atomic rename. User-chosen
  name/vendor preserved.
- **Published ids** live in `settings.workbuddy`, seeded on first run from whatever already points
  at us — so adopting an existing hand-written entry needs zero configuration.
- **Prerequisite:** the facts had to exist first. `models_cache.context_window` and
  `capabilities_json` were 0/457 populated — never captured, same latent bug class as pricing.
  `model-meta.ts` now parses them (OpenRouter `context_length`, `supported_parameters`).
- Verified: `maxInputTokens 128000` and `supportsReasoning false` both read from the catalog (not
  the fallback), user's name/vendor survived, other 6 entries untouched, chat still 200.
- Log line to look for: `workbuddy sync: N published, M stale removed`.

Also removed the two redundant entries from the legacy `~/.codebuddy/models.json` (backed up to
/tmp), restoring it to Tushu's original 4.

**Natural follow-up, not done:** publishing additional models still needs a list in settings — a
per-model "expose to clients" toggle on the Models screen would be the real UI for that.

## That follow-up is now built (`5934511`)

Model Browser rows carry Expose/Unexpose, backed by `workbuddy_status` / `workbuddy_set_models`,
which persist and rewrite the client file immediately instead of waiting for the next launch.

**The subtle bug I had to design around:** "never configured" (`None`) and "explicitly empty"
(`Some(vec![])`) must be different. Without that distinction, unpublishing everything gets
re-seeded from the client file on the next launch and the entry silently comes back. Related: sync
had an early return on an empty list, which meant unpublish would *skip* and leave our entries in
the client's picker. Empty now runs the merge so it removes.

Verified across three real restarts: 2 published (deepseek got `maxInputTokens 163840` — its real
context window, not the 128000 fallback, which proves the values are catalog-derived) -> 0
published with the other 6 entries untouched -> 1 published; then 4/4 chat 200.

**One transient to be aware of:** a 401 appeared on the first request after a rapid
pkill/relaunch cycle. Re-reading the key gave 200, and 4/4 passed after. Don't read a single 401
right after a restart as a key problem — retry before investigating.

## CONFIRMED WORKING: chatting through the router from WorkBuddy

Tushu: *"ai-provider router appears, and i can chat with workbuddy now."* That closes the original
request ("add our ai-provider router as custom router in workbuddy so that we can test it") and
settles the two-file question: **`~/.workbuddy-ai/models.json` is the live path.** Entries written
to `~/.codebuddy/models.json` never appeared in the picker, so that file is legacy despite being
referenced in `app.asar`. Skill corrected from "verify, there are two" to a definite answer.

Follow-ups offered, not done: remove the two now-redundant entries from `~/.codebuddy/models.json`;
set `supportsReasoning:false` and add `maxInputTokens/maxOutputTokens` to the working entry.

## WorkBuddy-shaped traffic: verified, plus two observations

Simulated how WorkBuddy actually calls the gateway (streaming, tool definitions supplied, client
User-Agent) rather than the bare non-stream probes used earlier:

- `/v1/models`: 457 ids, `object: "list"`, **all provider-qualified** (`openrouter/openai/gpt-4o-mini`);
  zero bare ids. So a bare id now *routes* but is not *advertised*. Left as-is: a client that
  ignores the catalog is unaffected, and adding bare ids would roughly double the list and create
  duplicates. Revisit only if a client is observed validating the configured id against the list.
- Streaming: first frame carries `role`, last line is `data: [DONE]`, text reassembles cleanly,
  and **no tool_calls injected** — a client that declares its own tools gets pass-through, which is
  the correct behaviour (the sandbox tool set must only engage when the client declares none).
- Non-stream with tools: 200, usage 51/2.

**Reliability: 10/10 at a realistic 3s cadence** — all HTTP 200, every ledger row with
`tokens_in=316, cost=49`, zero errors. This closes the earlier intermittent-failure question:
those were my own rapid-fire requests tripping OpenRouter's rate limit into key cooldown, not a
defect. Don't judge reliability from burst traffic.

## Spend cap (R4) now actually enforceable — verified

With cost finally non-zero, the cap works end to end. Set `settings.spend = {"capMicrosPerMonth":10}`
while month-to-date was 49 µUSD, and the next gateway request returned:

    402 {"error":{"code":"spend_cap_exceeded",
         "message":"monthly spend cap reached — 49/10 micro-USD this month",
         "type":"insufficient_quota"}}

Setting the cap back to 0 released it immediately (the gate reads on every request, no caching).
Cap row deleted afterwards to restore the original state — `spend_cap_micros` treats `cap <= 0`
as disabled, so 0 and absent are equivalent.

Cost math verified against the persisted prices, both exact:
- `openai/gpt-4o-mini` 150000/600000 per 1M -> 316 in / 2 out = 49 µUSD
- `deepseek/deepseek-chat` 320000/890000 per 1M -> 892 in / 2 out = 287 µUSD

## A null-usage scare that was NOT a usage bug

deepseek came back with `usage: null` and 0 tokens on one request. Looked like the same class of
bug again. It was not:

- Direct upstream check (non-stream AND stream): deepseek reports usage exactly like gpt-4o-mini
  does — finish_reason on chunk N, usage on chunk N+1. Identical shape.
- The failing ledger row was `status=error, error_class=NO_ROUTE` — **no request was ever sent**,
  so of course there was no usage. Rapid-fire requests had tripped OpenRouter's rate limit, the
  key went to cooldown, and the planner had no candidate.
- Re-ran: deepseek 892/2 and gpt-4o-mini 316/2, both with usage, both with correct cost.

Lesson: `error_class` in the ledger is what distinguishes "usage was dropped" from "nothing ran".
Check it before assuming the capture path broke again. Also: `usage: null` on a *successful*
response and `usage: null` on an error response look identical from curl — read the ledger row.

## Verification techniques that paid off (and one that cost time)

- **Decisive technique: query the live feed, then replay it in a unit test.** Fetching
  OpenRouter's real `/models` and running `selectAll` + `parsePricing` over it proved the
  capture path was sound in one shot, which narrowed the search to persistence immediately.
- **A schema column that appears only in the schema and in tests is a smell.** Grepping for
  `pricing_json` and finding no INSERT/SELECT was the whole answer.
- **Cost me time:** the sandbox `grep` shim silently returns nothing for alternation (`a|b`).
  It looks like "no matches" rather than "unsupported". Use the Grep tool for anything with `|`.
- **Making a cache row stale is the way to exercise the boot self-heal.** `refreshStaleCatalogs`
  only fires on rows that are missing or past the 24h TTL, so a cache written by a pre-fix build
  stays priceless until the TTL expires. Setting `fetched_at = 0` triggers a legitimate re-list
  through the product's own path — no speculative code needed. (DB backed up first.)

## Runaway tool-call XML in WorkBuddy chat — root cause: manifest provers drop `tools`

Tushu pasted a WorkBuddy chat where the assistant emitted a recursive, unterminated
`<invoke name="Bash"><parameter name="command">…` blob and had to be cancelled at 44s.

Ledger first (`ledger` table, `datetime(ts/1000,'unixepoch','+6 hours')`) — this is the single
most useful debugging move, it names the model that actually served:

| local | model | latency | tok in/out |
|---|---|---|---|
| 02:34:10 | agnes-3.0-flash | 1782 | 0/0 |
| 02:35:18 | agnes-2.5-flash | 2950 | 0/0 |
| 02:37:44 | agnes-2.5-flash | **40359** | 0/0 |

So it was Agnes, not gpt-4o-mini. Chain:

1. **The active Agnes manifest drops tools.** `requestTemplate` is only
   `{model, messages, stream, max_tokens?, temperature?}` — no `tools`, no `tool_choice`, no
   `response_format`. Verified by reading the row:
   `SELECT body_json FROM manifests WHERE provider_id='8234f8af-…' AND is_active=1`.
2. **The engine could carry them.** `manifest-interpreter.ts` already passes
   `tools/toolChoice/responseFormat` into `renderTemplate`, and honours `responseMap.toolCalls`,
   `stream.chunkMap.toolCalls`, `stream.toolCallStream`, `responseMap.usage`. The gap is only that
   `adapter-generator.ts:81` tells the generator the placeholder set is limited to five names, so
   no AI-generated manifest ever declares them.
3. **`workbuddy.rs:281` hardcodes `"supportsToolCall": true`** for every published model, so
   WorkBuddy is told Agnes can call tools. (The reasoning flag is deliberately `unwrap_or(false)`;
   tool call is not.)
4. Result: model sees WorkBuddy's tool-heavy system prompt with zero tool schemas, role-plays tool
   calls as inline XML, never terminates, degenerates. `parseAssistantStream` only knows
   `<|tool_call_start|>`-family markers, so `<invoke>` XML passes through unfiltered.
5. **Agnes upstream DOES support tools.** Direct call to
   `https://apihub.agnes-ai.com/v1/chat/completions` with `tool_choice:{"function":"Bash"}`
   returned a well-formed `tool_calls` (with `type:"function"`, `index`, `usage`). Key from
   keychain: `security find-generic-password -s ai-provider-router -a key:<keyId> -w`. So the
   manifest is the ONLY thing blocking it.

Two further defects found while reproducing:

- **Pass-through tool_calls are not OpenAI-shaped.** Live capture from the gateway:
  `{"delta":{"tool_calls":[{"arguments":"{…}","id":"call_…","name":"Bash"}]}}` — missing per-call
  `index`, missing `type:"function"`, missing the `function:{name,arguments}` wrapper. Emitted in
  both the SSE branch and the non-stream branch of `gateway_handlers.rs`.
- **Agnes ledger rows are 0/0 forever** because the manifest has no `responseMap.usage` — spend
  accounting and the R4 cap are blind for this provider.
- `~/.workbuddy-ai/models.json` has 5 entries all named "ai-provider router"
  (openai/gpt-4o-mini + 4 agnes variants), so the picker cannot distinguish them.

Fixes, in value order: (1) add tools/tool_choice + toolCalls + usage to the Agnes manifest;
(2) allow them in the generator prompt; (3) derive `supportsToolCall` from real capability data;
(4) emit OpenAI-shaped tool_calls; (5) unique names per published model.

## All five fixes applied — plus two more the fix exposed

Rebuilt (build 03:11) and installed to `/Applications/AI-Provider Router.app`. Tests: 111 Rust
(up from 88), 210 router-core, 43 desktop; `tsc` clean on both packages.

1. **Agnes manifest v2 staged + activated** through the app's own two-step flow
   (`manifest_stage` then `manifest_activate`, replicated in one SQL transaction so the provider
   is never left with zero active manifests). Now carries `tools`/`tool_choice`/`response_format`
   in `requestTemplate`, `usage` + `toolCalls` in `responseMap`, `chunkMap.toolCalls` and
   `requestUsage: true`. DB backed up to `/tmp/ai-provider-router.db.bak-20260919-025454`.
   Confirmed upstream first: Agnes returns real `tool_calls` AND a usage block when
   `stream_options.include_usage` is set — the manifest was the only thing in the way.
2. **`adapter-generator.ts` prompt** no longer forbids tool placeholders. It was self-
   contradictory: the "complete example" it shows the generator IS
   `BUILTIN_TEMPLATES["openai-compat"]`, which has `tools`. Watch out: `SCHEMA_SUMMARY` is a JS
   template literal — backticks in the added text break the build (hit this once).
3. **`workbuddy.rs`**: `supportsToolCall` now comes from `tool_support()`, which joins
   `models_cache -> manifests` and checks whether the ACTIVE manifest puts `tools` in
   `requestTemplate`. Not from the catalog: a provider's catalog rarely publishes tool support,
   and what actually decides the outcome is whether our adapter forwards `tools`. Unknown = false
   (same rule as `supportsReasoning`). Display names are now unique — a preserved name is honoured
   only while it stays unique, otherwise `Router: <id>`.
4. **`normalize_tool_calls()` added to `gateway.rs`**, applied in `gateway_tool_calls` (the single
   point where tool calls enter). This turned out to be bigger than a cosmetics fix: the bridge
   emitted `{id, name, arguments}` flat, but **every** dialect reader — OpenAI, Anthropic,
   Responses, Gemini — reads `function.name` / `function.arguments`. So the Anthropic, Gemini and
   Responses paths were silently emitting tool calls with an empty name and null arguments.
5. **Two more defects surfaced once tools actually flowed.** The Anthropic and Gemini ingress
   modules forwarded their own native tool shapes verbatim into the OpenAI-shaped bridge request
   — `{name, description, input_schema}` and `tools:[{functionDeclarations:[...]}]`. Agnes
   rejected both with 502 SERVER_ERROR. Added `tools_to_openai` / `tool_choice_to_openai` to
   each. Invisible before, because no manifest forwarded tools at all.

Live verification, all four dialects, `agnes-2.5-flash`:
- OpenAI stream: `{"function":{"arguments":"{\"command\": \"ls -la /tmp\"}","name":"Bash"},"id":..,"index":0,"type":"function"}` with `finish_reason: tool_calls`.
- OpenAI non-stream: same shape on `message.tool_calls`, plus `usage`.
- Anthropic `/v1/messages`: `stop_reason: tool_use`, `tool_use: Bash {"command": "ls /tmp"}`.
- Gemini `generateContent`: `functionCall: Bash {"command": "ls -la /tmp"}`.
- **The original failing turn** ("skill of reasoning", tools, auto): 0 inline `<invoke>` markers
  and one real structured tool call. It was 40,359 ms of recursive XML before.
- Ledger: agnes rows now log real tokens (550 in / 46 out) instead of 0/0.

Cost is still 0 µUSD for agnes — `pricing_json` is NULL because the provider publishes no
pricing. Checked its `/models` directly: entries carry only `id`, `object`, `created`,
`owned_by` and `supported_endpoint_types` — no pricing, no context length, no capability list.
So NULL is correct (unknown is not free) and this is not a bug to fix. It does mean the
monthly spend cap cannot see agnes spend at all.

**Known leftover:** one WorkBuddy entry is still named "ai-provider router" (openai/gpt-4o-mini).
It is the user's own choice and is now unique, so the sync preserves it. Ask before renaming.

## Display name settled: `ai-provider router · <id>` (both facts in one string)

Tushu wanted the provenance marker and the model id in the same name, so the picker row says
which gateway serves it *and* which model it is. `display_name()` in `workbuddy.rs` now builds
`ai-provider router · <id>` and is applied unconditionally — `name` is generated, not preserved.

That reverses part of the earlier uniqueness rule. Preserving a user-chosen name could not work
here: his names were "ai-provider router" (no id) and "agnes-3.0-flash" (no marker), so neither
carried both facts. `vendor` is still preserved. Ids are de-duplicated upstream, so names are
unique by construction — the collision bookkeeping came out. 112 Rust tests.

Live after rebuild: all 9 published entries read `ai-provider router · <id>`.

**Incidental finding — the gateway 503s while the worker window is cold.** Smoke-testing after
the rebuild, the first two requests returned 503 `AI-Provider Router core unavailable` and the
third succeeded. The log shows the watchdog firing all day: `no heartbeat for ~30s — re-warming
worker window`. That is the R1 hidden-window behaviour and is NOT a broken install — retry. Worth
knowing when a chat through WorkBuddy fails once and then works.

Also noted: Tushu has since exposed 9 models (added `openrouter/auto-beta`, `openrouter/free`,
`z-ai/glm-5.2:free`), so the published list is no longer the 6 I diagnosed against.

## State

- Installed at `/Applications/AI-Provider Router.app` (build 00:37, all fixes in).
- Tests: 88 Rust, 196 router-core, 43 desktop; `tsc` clean on both packages.
- `~/.codebuddy/models.json` has the two AI-Provider Router entries; WorkBuddy hot-reloads in
  ~1s. The end-to-end test of picking the model *from inside WorkBuddy* is still unrun — that
  needs the user.
- DMG bundling still fails in this sandbox (`osascript` blocked, grep shim); the `.app` builds
  fine and that is what gets installed.
