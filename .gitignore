# Node / pnpm
node_modules/
dist/
*.tsbuildinfo
coverage/

# Rust / Tauri
target/
apps/desktop/src-tauri/gen/

# Local dev database (contains provider refs + ledger history — never commit)
apps/desktop/src-tauri/*.db
apps/desktop/src-tauri/*.db-shm
apps/desktop/src-tauri/*.db-wal
*.bak

# App data / local
.playwright-mcp/
.video_agent/
.DS_Store
*.local

# Never commit anything that could carry secrets
.env
.env.*
*.pem
*.key
