[package]
name = "jsengine-spike"
version = "0.0.0"
edition = "2021"

[dependencies]
rquickjs = { version = "0.9", features = ["bindgen", "futures", "loader", "macro", "parallel"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "sync"] }
serde_json = "1"
