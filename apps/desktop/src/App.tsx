/**
 * AI-Provider IDE — desktop shell (Phase 0 placeholder).
 * Screens (Providers / Models / Playground / Usage / Router Settings / Gateway /
 * Onboarding) land in Phase 2a per ARCHITECTURE.md §9.
 */
function App() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-2 bg-zinc-950 text-zinc-100">
      <h1 className="text-2xl font-semibold">AI-Provider IDE</h1>
      <p className="text-sm text-zinc-400">Scaffold — Phase 0</p>
    </main>
  );
}

export default App;
