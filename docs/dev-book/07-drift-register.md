# 07 — Drift register

## Why this file exists

Documentation drifts behind code, always. The damage is not the drift itself — it is a stale claim that **looks
authoritative**, because a new contributor who finds one false statement stops trusting all of them.

So the rule is not "never drift". The rule is:

> **If you change a fact, update its owner — and if you cannot, log it here.**

A claim that is known to be stale, and written down as stale, costs nothing. The same claim left unmarked is
what makes every other page suspect.

## The change checklist

The practical half of this chapter. Find your change, update every row.

| If you change | Also update |
|---|---|
| A `#[tauri::command]` | `apps/desktop/web-test/shim.ts` **the same day**, and the command count in [03](03-contracts.md) |
| A migration or a table | version + count assertions in `store.rs`, the table list in [04](04-data-model.md) |
| The product version | `pnpm check-version-sync` covers six manifests; add a `CHANGELOG.md` entry |
| The gate (`ci.yml`) | `scripts/ci-local.sh` (the mirror) and the step table in [05](05-workflow.md) |
| An HTTP route | the surface table in [03](03-contracts.md), plus a gateway test |
| An invariant | [03](03-contracts.md) **and** `ARCHITECTURE.md` §5 |
| A screen | the `NAV` constant in `components/Shell.tsx` **and** the screen map in [01](01-orientation.md) |
| A dependency's major version | the toolchain table in [01](01-orientation.md) |
| A design decision | a dated entry in `DECISIONS.md` |
| A chapter, or any link inside one | regenerate the HTML — `pnpm docs:book` fails on a broken reference |
| Any fact stated in two places | delete one of them, or add a row below |

## The register

| ID | Claim | Where | Evidence | Verdict | Status |
|---|---|---|---|---|---|
| **D1** | "Status: greenfield, pre-scaffold" | `ARCHITECTURE.md:8` | The app ships v1.0.0 with 15 migrations, a release workflow and a licence | **False** | **Fixed** |
| **D2** | OS keychain CRUD via `keyring` **v3** | `ARCHITECTURE.md:125`, `:702`, `:871` | `Cargo.toml:33` is `keyring = "2"`; `DECISIONS.md` 2026-09-16 records the v3→v2 downgrade because v3's macOS data-protection keychain breaks unsigned dev builds. Line `:782` already said v2 correctly, so the file contradicted itself | **False** | **Fixed** |
| **D3** | "Per-app gateway keys … are a stated v1 limitation — one master key for now"; "a single master key (no per-app keys) is a stated v1 limitation" | `ARCHITECTURE.md:516-518`, `:812-813` | Per-app keys **ship**: `gateway_app_key_create`, `gateway_app_keys`, `gateway_app_key_revoke`, `gateway_app_key_delete` (`commands.rs:713-716`), the `gateway_keys` table, and `CHANGELOG.md` 1.0.0 | **False** | **Fixed** |
| **D4** | "per-app keys + budgets are the follow-up" | `ARCHITECTURE.md:848-850` | Half true when written. Per-app keys shipped, but the spend cap was **global and monthly** — `gateway_spend_status` compared one `month_micros` against one `cap_micros`, with no per-key dimension | **Half** | **Fixed** — the remaining half closed 2026-09-23 (0017: `gateway_keys.cap_micros` + per-app enforcement). The `ARCHITECTURE.md` line now states both ship |
| **D5** | The local mirror "runs the same steps in the same order as `ci.yml`" | `ci-local.sh:2`, `:10`; `CONTRIBUTING.md:21` | The step **sets** were identical; the **order** differed in one place. Measured by extracting both: `dependency audit` was step 2 in `ci.yml` and step 7 in `ci-local.sh`. Every other shared step was in the same relative order | **False** (ordering only) | **Fixed** — the claim was corrected first, then the script was aligned on 2026-09-22. Both mirrors are now step-for-step identical |
| **D6** | Module map lists 7 screens from the spec era | `ARCHITECTURE.md` §1.2 | The app ships **13** screens (`ScreenId` in `ui-state.ts`; 13 files in `screens/`). Seven post-date the map — and one of those is a **rename**, not an addition: `screen-usage` no longer exists, and `Activity.tsx` is headed "the request ledger first". So the map is missing 6 genuinely new screens and stale on 1. Their subsystems are designed in `GATEWAY_MEMORY_LAYER.md` and `CONTROL_SCREEN_BUILD.md` | **Stale** | **Fixed** — a shipped-screens table was *added* below the spec map rather than replacing it, so both the plan and the difference survive |
| **D7** | "The gateway listens on port 8800" vs "a fresh install starts on 8787" | `README.md:121-122` | Both are correct and describe different things: **8787** is `DEFAULT_PORT` (`gateway.rs:36`), **8800** is this machine's persisted setting because AI Hub v2 also claims 8787. Not drift | **Correct** | **Verified** — recorded so the next person does not re-check it |
| **D8** | "The column exists; the data does not yet" (`cached_tokens`) | `PRODUCT_COMPLETION_PLAN.md:449-454` | The live database **was** at `schema_version = 14` and `ledger.cached_tokens` **was** absent (`pragma_table_info`), because the installed bundle had not been rebuilt since migration 0015 landed | **Inaccurate, now true** | **Fixed** — rebuilt, reinstalled and relaunched 2026-09-22. Measured afterwards: `schema_version = 15`, `ledger` has 17 columns, all **1530** pre-existing rows carry `cached_tokens IS NULL`, and 0 rows report a value |
| **D9** | Six relative references to the diagram assets — `diagrams/architecture.html`, `diagrams/self-construction.html`, `diagrams/gateway.html`, `diagrams/memory-context-gateway-read-path.svg`, `diagrams/memory-context-gateway-write-path.svg`, `diagrams/` | `ARCHITECTURE.md:16-18`, `AUDIT_REPORT.md:4`, `MEMORY_CONTEXT_GATEWAY_INTEGRATION.md:16,17,34,205` | The diagram assets live at the **repository root** in `diagrams/`, not under `docs/`. The 2026-09-22 reorganisation moved 19 docs into `docs/` and verified "17 markdown links across 36 files, 0 broken" — but that check validated `.md` targets only, so **every `.html`, `.svg` and directory link broke silently**. Two of the four in `MEMORY_CONTEXT_GATEWAY_INTEGRATION.md` are image embeds, so they render as broken images | **False** | **Fixed** — all six now use `../diagrams/` |
| **D10** | "closing the window stops the gateway, and the Gateway settings screen says so plainly"; "a reloading, crashed or **closed** window means `503` for every client" | `ARCHITECTURE.md:520-521`, `dev-book/09-status.md:58` | The opposite ships, and has since R1. `RunEvent::WindowEvent` (`lib.rs:262-275`) calls `prevent_close()` then `hide()` whenever `hide_on_close(app)` is true, and `hide_on_close` (`lib.rs:131-148`) reads `settings.background.hideOnClose`, **defaulting to true** — including when the store state is absent. The line it logs is "window hidden — gateway still serving in background". The UI agrees with the code (`screens/Gateway.tsx:193-199` — "closing the window hides the app and the gateway keeps serving"; `:312` — "including with the window closed, once background mode is on"), and `DECISIONS.md:541-556` records the R1 decision that fixed precisely this: "This change fixes the first; the other two need a real headless core (v2)". Both docs therefore describe pre-R1 behaviour, and `ARCHITECTURE.md` is wrong twice over — about the behaviour *and* about what the screen says. **Nothing pinned it:** `hide_on_close` appeared in exactly two places — its definition and its call site — with no test on the default. Closed by extraction: `hide_on_close_from` (`lib.rs`) now holds the decision, and `hide_on_close_defaults_on_and_only_an_explicit_false_turns_it_off` pins all 12 inputs, both arms falsified | **False** | **Fixed** |

| **D11** | "Required secrets (see CONTRIBUTING.md)" | `.github/workflows/release.yml:52` (as written 2026-09-22) | `CONTRIBUTING.md` documented **no** `APPLE_*` secrets at all. A Grep for `APPLE_\|secret\|release\|sign` returned two unrelated hits — `:13` ("Xcode command line tools for `codesign`") and `:68` (a clippy note). The six secrets were documented only in `docs/dev-book/05-workflow.md:166-167`, so a maintainer following the workflow's own pointer found nothing and had no way to provision a release. The gap was compounded by `09-status.md`'s "**No notarized release**" row, which named the *symptom* and implied the pipeline was missing — when the real defect was that it was **unfalsifiable**: `tauri build` succeeds with no secrets and emits an ad-hoc signed app, so the job went green and a draft appeared | **False** | **Fixed** — a "Releasing" section was added to `CONTRIBUTING.md` on 2026-09-23 with the one-time provisioning; `release-preflight.sh` and `verify-release-signature.sh` make the pipeline fail loudly instead of silently emitting an unusable artefact; the gap row was reworded to "Release not provisioned" |

| **D12** | The Phase 1 task prompt's module layout: "`persist.rs` … MOVED" to `core/` and "`egress.rs` … MOVED" to `core/` — as files that can move **unchanged**; and "104 Rust tests + 460 TS unit tests + 27 e2e tests" | `AGENT_PROMPT_HEADLESS_SERVICE.md:99,103,55` | Both placements are impossible as stated. `gateway.rs` calls `crate::persist::{active_gateway_key_ids, gateway_key_cap, month_spend_micros, app_month_spend_micros, spend_cap_micros}` in **non-test** code (`gateway.rs:217,316,907`), so `persist` in `tauri/` makes `core/` depend on the glue; and `persist` in `core/` fails from the other side because `persist.rs` imports `crate::egress::EgressState` and `crate::commands::CommandError`. `{persist, egress, CommandError}` is one cluster — all three moved and `CommandError` was extracted to `core/error.rs`. The test count is **489**, not 104 (measured `cargo test`, 2026-09-23). The prompt's `104` appears to be a count of `gateway_tests.rs` alone — and that file holds **113**, so the figure is wrong under either reading: too low for the file, and a fraction of the crate. The prompt's file list covers **15 of 29** `.rs` files | **False** | **Fixed** — see [10](10-headless-service.md) §2.1.1 |
| **D13** | Every `file:line` citation in the docs that names a module moved by the Phase 1 split — `gateway.rs`, `store.rs`, `persist.rs`, `commands.rs`, `gateway_cmds.rs`, `egress.rs`, `vault.rs`, `tools.rs`, `memory.rs`, `crash_report.rs`, `context_scope.rs`, `lib.rs` | ~230 matches across 23 markdown files (Grep, 2026-09-23) | The split moved 27 of 29 modules into `src/core/` or `src/tauri/`, so a reader following "`gateway.rs:1755`" lands on nothing. **Scoped deliberately rather than swept:** the *live* docs were updated — `09-status.md` (5 references plus a new Phase 1 row), `03-contracts.md`, `02-architecture.md`, `04-data-model.md`, `10-headless-service.md`. The **dated** audit and plan files (`SECURITY_AUDIT_2026-09-20.md`, `ARCHITECTURE_AUDIT.md`, `AUDIT_TRAIL_READER_2026-09-21.md`, the `*_PLAN.md` files and similar) are point-in-time records with the date in the filename, and rewriting their citations would misrepresent what was measured and when. Earlier entries in this register are in that second class: their `lib.rs` and `gateway.rs` citations describe the tree as it was | **Stale** | **Partly fixed** — live docs current, dated snapshots left as history |
| **D14** | "Tauri build \| `pnpm build` \| Yes" — `pnpm build` is presented as the gate that proves the Tauri app still builds; §10 restates it as "`pnpm build` produces a working Tauri app" | `AGENT_PROMPT_HEADLESS_SERVICE.md:232`, `:302` | `pnpm build` never invokes Tauri. Root `build` is `pnpm -r build`; the desktop `build` is `pnpm build:clean && tsc && vite build` — TypeScript and Vite only, ~17 s, no `cargo` and no bundler. The bundler runs only under the `tauri` script: `pnpm --filter ai-provider-router-desktop tauri build` (~2m42s). **This is a gate that cannot fail on the error it exists to catch**, and Phase 1 proved it: adding the second `[[bin]]` broke `tauri build` with `failed to find main binary` — its own suggested remedy, `default-run`, being the actual fix — while `pnpm build`, `cargo check`, `cargo test`, `cargo clippy` and `cargo fmt --check` were **all green**. Only a real `tauri build` found it | **False** | **Met by running the real command** — the criterion was satisfied with `tauri build` and the fix (`default-run` in `Cargo.toml`) is in place. Note the `checks` CI job still runs only `pnpm build`, so the same blind spot persists in CI |

| **D15** | The `headless-service` CI job — "a module split that accidentally reached back into Tauri glue would surface here, not in `checks`" — offered as evidence that the service is Tauri-independent | `.github/workflows/ci.yml:118-119`, `:143` (as written 2026-09-23) | The job ran `cargo build --bin aiproviderd --release` with **default features**. When written that was not a wrong flag but a wrong *claim*: `tauri` was an unconditional dependency, so no flag existed that could have excluded it — the job built what every other job built, and the WebKitGTK install beside it was honest evidence of the coupling. It became a live blind spot the moment the `app` feature landed, because `default = ["app"]` means the flagless command still compiles Tauri — the job would have stayed green while proving nothing about the split it exists to test. Fixed by building with `--no-default-features` and dropping the GTK/WebKit packages, now unreachable: `cargo tree --no-default-features --edges all \| grep -ci 'webkit\|wry\|gtk'` → **0** | **False** (after the split) | **Fixed** 2026-09-23 |

| **D16** | `AGENT_PROMPT_HEADLESS_SERVICE.md` §8.1 lists the dev book as a gate step — "Dev book, `pnpm docs:book`, Yes" | `AGENT_PROMPT_HEADLESS_SERVICE.md:235` | `pnpm docs:book` runs in **neither** mirror: not among `ci.yml`'s 17 `checks` steps, not among `ci-local.sh`'s 18. The prompt has claimed it since Phase 1, so the gap is old; it became visible on 2026-09-23 when a table row in [10](10-headless-service.md) carried **two adjacent escaped pipes inside one code span**. The generator rejected it — "table row has 4 cells but the header has 2" — while the entire local gate, `Doc links resolve` included, reported **ALL GREEN**. A book that will not build could therefore be committed, and the failure is quiet by construction: the offending row reaches a reader as a mangled table rather than as an error. The two checks are not substitutes — `check-doc-links` resolves links, `docs:book` parses structure — and the generator does tolerate a *single* escaped pipe inside a code span (D15's row relies on exactly that), but not two adjacent ones | **False** | **Fixed** 2026-09-23 — `Dev book builds` added to both mirrors at position 11, and the parity table re-measured to 18/19 |

| **D17** | The Phase 1 acceptance for the Tauri-free split — `core/` "must not name `crate::tauri::*`" — carried by building the service with `--no-default-features` | `.github/workflows/ci.yml` headless job step 2; `scripts/ci-local.sh:136`; `AGENT_PROMPT_HEADLESS_SERVICE.md` §8.1 | The check is real but **narrower than the claim it is offered for**. Both mirrors build `--no-default-features` *without* `--all-targets`, so the lib's `cfg(test)` code is never compiled in that configuration — and it does not compile: `cargo check --no-default-features --all-targets` fails with **33 errors**, the first being `persist.rs:2349` calling `list_drift_events`, which is defined behind `#[cfg(feature = "app")]` at `persist.rs:825`. So `core/`'s *shipping* code is Tauri-free — which is what the claim was about, and it holds — while `core/`'s *tests* are not. Measured 2026-09-23 while landing increment 3 of Phase 2. `persist.rs` is untouched by that work (`git diff --name-only` lists nine files, none of them `persist.rs`) and `cargo check --no-default-features` without `--all-targets` passes, so this is pre-existing and invisible rather than newly introduced. **Closed 2026-09-23:** `persist.rs`'s three test modules now carry the `app` gate — the four individual tests in `persist_tests` that call app-gated readers, and the two trail modules wholesale, because every test in them does — and `gateway_tests.rs`'s one helper that went dead once they were gated carries it too. Both mirrors gained a `Headless targets check` step (`cargo check --no-default-features --all-targets`), which is the flag whose absence this entry is about. Measured after: that command compiles with **0 errors and 0 warnings**, and `cargo test` is still **541 / 0**, so gating lost no test — the ten tests in `persist_tests` that never needed the feature still compile without it, which is the proof the step exists to make. The flag is shown to be the whole of the difference: a `cfg(test)` probe naming `tauri::AppHandle` fails the check with an `E0433` (cannot find module or crate) **with** `--all-targets` and passes **without** it | **Half** | **Fixed** |

| **D18** | "release is idempotent — a double release cannot leak capacity" — the test's own name, offered as the guard on `ProviderLimiter.acquire`'s `released` flag | `packages/router-core/test/concurrency.test.ts:134-142`; the flag at `concurrency.ts:77-80` | The test does not test idempotence. Measured 2026-09-23: deleting the `released` flag leaves the test **passing** — `vitest run -t "release is idempotent"` reports 1 passed, 272 skipped. With `maxPerProvider = 1` the count is 0 and the entry already deleted after the first release, so the second and third calls compute `0 - 1 = -1`, take the `next <= 0` branch and delete again: a no-op either way, flag or no flag. The property only bites with **two** permits held, where releasing one twice drops the count from 2 to 0 while the other is still in flight, so the limiter then admits two more and three attempts run concurrently against a cap of two. The code is correct — only the test's claim is broader than its check. The Rust port (increment 5) keeps the test for fidelity, marks it as weak in place, and carries the property in `an_explicit_release_followed_by_drop_counts_once` (cap 2), whose falsification fires | **False** | **Open** |

| **D19** | The classification of a caught attempt error — spelled twice in `executeText` as though it were one rule | `execution-engine.ts:113` (already-emitted path) and `:118` (not-yet-emitted path) | The two spellings are **not equivalent**, and they agree on every input reachable today only because of a constant at the throw site. `:113` tests only `classify(status) === "OK"`; `:118` additionally forces `PARSE_ERROR` whenever the failure kind is `mid-stream`. Measured 2026-09-23 by executing the real `classify` against both expressions: a mid-stream `429` is `RATE_LIMITED` under `:113` and `PARSE_ERROR` under `:118`, and a mid-stream `503` is `SERVER_ERROR` under `:113` and `PARSE_ERROR` under `:118` — two divergences — while the input a producer actually emits (mid-stream, status `200`) agrees. That agreement is not the two rules meeting; it is one constant at the throw site. `manifest-interpreter.ts:360` is `new ManifestHttpError(200, JSON.stringify(err), "mid-stream")`, and it is the only one of the three `ManifestHttpError` construction sites that is not `kind: "response"` (`:246` and `:304` both pass a real status). So the divergence is **latent rather than live** — no reachable input separates the branches. It is not cosmetic either: `:113` is the branch that skips `recordResult`, so under that spelling a mid-stream `429` would classify as `RATE_LIMITED`, the one class that cools a key, and then never cool it. The Rust port (increment 6 of Phase 2) states the rule once, takes the `:118` spelling because it does not depend on a constant chosen at a throw site, and pins the choice with `the_two_spellings_agree_only_because_the_midstream_producer_reports_two_hundred` | **False (unreachable today)** | **Recorded** — the TypeScript is left exactly as written, so the reference the port is measured against does not move under it |

| **D20** | The Phase 4 note on recursive compression — "The `skipCompression` flag (`gateway-bridge.ts:102`) prevents infinite recursion — the same flag exists in the Rust port" | `docs/dev-book/10-headless-service.md:866-867` | Both halves are false, measured 2026-09-23. **The citation names the wrong file:** `gateway-bridge.ts` contains no `skipCompression` at all — a Grep for `skipCompression`, `compress` and `summar` over that file matches nothing, and the line the citation points at is inside a doc comment about `GATEWAY_WINDOW`. The flag's real homes are `model-router.ts:108` (the option on the request), `:134` (the branch that skips the compressor) and `Assistant.tsx:100` (the caller that sets it). **And the flag does not exist in the Rust port:** `src-tauri` has no path that compresses the *client's* `messages` — the gateway forwards them verbatim. **The evidence here was too strong, corrected 2026-09-23:** the tree is not without a compression path, it is without *this* one. `context_scope.rs:884-1020` estimates tokens (`estimate_tokens`, `estimate_prompt_tokens`), sizes a budget (`plan_budget_for`, against `DEFAULT_WINDOW_TOKENS` and `MEMORY_CHARS_PER_TOKEN`) and drops what will not fit (`rank`, `trim`, `compose_block`), and `inject_context` runs it on the gateway's read path. That path compresses *recalled memory* into a system message; it never touches the conversation, and `skipCompression`'s recursion guard has no counterpart at all. The distinction is load-bearing for Phase 4: a porter who read the original sentence would look for a missing module, when what is actually missing is a second input to a path that already exists. Phase 4 had not started when this was measured, so the sentence stated as present fact something that could not yet be true; Phase 4 has since begun (increment 14a, 2026-09-24), and the sentence has been rewritten to describe the port rather than the absence. The second half is the more dangerous of the two: a reader planning Phase 4 would conclude the recursion guard is already in place and omit it, and the omission is invisible until the first summary recurses. Fixed by correcting the citation and marking the flag as a Phase 4 deliverable rather than an existing one | **False** | **Fixed** 2026-09-23; re-read 2026-09-24 as Phase 4 opened |

| **D21** | Three names the pending Phase 3 ports must take from the TypeScript unchanged | `context_scope.rs:929` (`Candidate`), `:871` (`CHARS_PER_TOKEN`), `:878` (`RESERVE_FRACTION`) against `route-planner.ts:13-17`, `context-compress.ts:42`, `:45` | Measured 2026-09-23 while landing increment 7. Each name already means something else in this crate. `context_scope::Candidate` was `{id, layer, text, pinned}` — *a recalled memory headed for the prompt* — while `route-planner.ts`'s `Candidate` is `{provider, key, model}`, a planned attempt. `context_scope::CHARS_PER_TOKEN` is **3.5**; `context-compress.ts:42` is **4**. `context_scope::RESERVE_FRACTION` is **0.20**; `context-compress.ts:45` is **0.25**. The shared window *was* considered and does agree (`DEFAULT_WINDOW_TOKENS` is 8192 on both sides, and `context-compress.ts:32-34` says so deliberately — "Matches the host's `DEFAULT_WINDOW_TOKENS` so both sides of the bridge plan against the same conservative number") — so one of the three was checked and the other two were not. None of it fails today, because the two sides are in different languages. All of it fails the moment the ports land in the same crate, and `Candidate` failed *immediately*: increment 7's loop needs the routing meaning. The rename has to happen on the Rust side, because the TypeScript is this port's reference and cannot move | **False** (a name that will mean two things) | **Fixed** 2026-09-23 — `Candidate` → `MemoryItem`, `CHARS_PER_TOKEN` → `MEMORY_CHARS_PER_TOKEN`, `RESERVE_FRACTION` → `MEMORY_RESERVE_FRACTION`, freeing all three of the TypeScript's names. `SkipReason::NoCandidates` was **left alone on purpose**: `as_str()` renders `"no_candidates"` into the `aip-memory` response header (`apply_memory_headers:447`), so the variant's string is on the wire and renaming the variant would have meant either a wire change or a name contradicting its own value |

| **D22** | The image path treats a provider failure the way the text path does | `execution-engine.ts:183-188` against `:113-130`; `manifest-interpreter.ts:461` against `:246`/`:304`; the type at `:57-60` | Measured 2026-09-23. **Two differences, one of them live.** *(1) Reachable.* `generateImage` returns `{ok: false, status, errorBody}` for `>= 400` (`manifest-interpreter.ts:461`) and never calls `retryAfterFrom`, although `res.headers` is in scope on that very line — while `listModels` (`:246`) and `generateText` (`:304`) both pass it. `ImageAttemptResult` (`:57-60`) has **no field** for it, so the wait cannot travel even in principle. `executeImage` therefore records `{cls, status}` with no `retryAfterMs` (`:184`) and calls `recordResult(c.key, cls)` with none (`:185`), so `health-tracker.ts:64`'s `Math.max(retryAfterMs ?? 0, COOLDOWN_FLOOR_MS)` lands on the **1000 ms floor**. A `429 Retry-After: 30` on the image path retries after one second. The text path fixed exactly this and says so at `:126-128` — "a key that asked for a minute is retried a second later — straight back into the window it was told to wait out" — one path away. It reaches the client too: no image attempt can name a wait, so `AllAttemptsFailedError.minRetryAfterMs()` is **0** for an image-only chain and the client is told nothing. *(2) Latent.* `catch {}` (`:186`) names `NETWORK`/`0` and keeps nothing the error carried, where `:117-121` classifies a thrown `ManifestHttpError` by its status. The two agree only because a status-bearing refusal is **returned** rather than thrown, so that arm is never reached with a status — the same "agrees only because of a producer's contract" shape as D19 | **False** | **Recorded** — the port keeps the image path's behaviour, so Rust and TypeScript stay comparable. `a_status_bearing_adapter_error_still_records_network_on_the_image_path` pins the latent half, `an_image_refusal_cools_its_key_by_the_floor_not_the_named_wait` pins the live one, and `a_whole_chain_of_failures_reports_every_attempt_in_order` pins the reporting half. The fix belongs in `manifest-interpreter.ts:461` plus the `ImageAttemptResult` shape, which would correct both implementations at once; fixing it in Rust alone would create a silent behavioural difference between the two, which is the defect class this register exists to catch |

| **D23** | `ports.ts` shapes — "already realised: `UsageTokens.cached_tokens` is `LedgerRow.cached_tokens` plus `BridgeMsg::Usage`" | `docs/dev-book/10-headless-service.md:784` against `ports.ts:106-117`, `persist.rs:491`, `gateway.rs:365-368` | **Half, and the half that is missing is the third field.** The sentence groups two types as though they jointly realise the shape; only one of them can carry `cached_tokens`. `UsageTokens` has **three** fields; `LedgerRow.cached_tokens` is one of them (`persist.rs:491`, `Option<i64>`, nullable with no default — `store.rs:1083-1102`); `BridgeMsg::Usage` (`gateway.rs:365-368`) is `{prompt_tokens: u64, completion_tokens: u64}` and carries the other two, not all three — as do the four dialects that destructure it (`gateway_handlers.rs:184`,`:221`; `gateway_responses.rs:341`,`:429`; `gateway_anthropic.rs:479`,`:564`; `gateway_gemini.rs:295`,`:351`) and the client-facing bodies they build (`gateway_handlers.rs:260`,`:267`). A Grep for `UsageTokens`, `struct Usage` and `TokenUsage` over `src-tauri/src` matches **no file**: the bridge variant is the crate's only usage type. **This is not a second finding of the client-facing gap** — `09-status.md:179` already states that in full, with the same citations and with §8.2 as the reason its fix is deferred. What was unrecorded is the port-planning consequence, and the two sides cost different things: a client loses a field it cannot derive, while a **porter** loses the measurement. Today's routing is TypeScript, so the ledger row is built where the full three-field shape is in scope (`model-router.ts:435`, `:519`, in process, never through the bridge); route in Rust and no webview holds the third value, making "reuse the crate's only usage type" the obvious move — the exact silent drop `ports.ts:101-104` describes that field suffering the first time. Closed for the port by `core::usage::UsageTokens` (increment 8), which is the shape `on_usage` now hands over; `BridgeMsg::Usage` is deliberately left as written, because widening it changes a response body §8.2 protects | **Half** | **Fixed** (the plan's sentence and the port's shape; the client-facing half stays deferred under §8.2) |

| **D24** | The prescription for Tier 2's recursion guard — "Phase 4 must introduce the compression and the guard together, and the guard is the part that is easy to leave out because its absence is invisible until the first summary recurses", and §12's open question "whether the summarization call … works correctly when the engine calls itself recursively. The `skipCompression` flag is designed for this" | `docs/dev-book/10-headless-service.md` §7 (Phase 4) and §12, both as written 2026-09-23 | **The guard is not needed, because the recursion it guards against is not expressible in Rust.** The TypeScript's summarizer is `createSummarizer` (`Assistant.tsx:55-106`), which closes over the `router` singleton and calls `router.generateText` from inside `router.generateText`; the flag is what stops that becoming unbounded. Measured 2026-09-24 rather than reasoned about: a minimal reproduction — an inherent method `async fn generate_text(&mut self, …)` taking a callback that calls the same method on the same value — is rejected with **`E0501`**, "cannot borrow `*self` as mutable more than once at a time". The first guess was `E0499` and the compiler disagreed, which is the reason the measurement exists. The sequenced counterpart (summarize, *then* route) compiles and runs, so the constraint is the re-entrancy and nothing else. **What the wiring actually needs is a seam, not a guard**: the summarizer handed to the router as a `&dyn Fn`-style boundary the router calls and does not own, which is this crate's existing shape for `AdapterFactory` and the usage callbacks. The claim was not merely unnecessary — it was **unfalsifiable as written**. Nothing in the Rust tree could have contradicted it, because the code it warns about cannot be written; a reader following it would have gone looking for a guard to add, and the honest answer is that the design removes the need for one. Same shape as D10's "cannot be tested": a *prescription* is a claim too, and this one had no evidence behind it until increment 14a tried to write the code | **False** (a prescription with no referent) | **Fixed** 2026-09-24 — increment 14a's section records the measurement, §7 and §12 were rewritten, and the wiring increment is scoped as "add a seam" rather than "add a guard" |

| **D25** | The "what must move" table's third column, reading "Must be ported to Rust" — for the execution engine, the route planner, the model router, the health tracker, the concurrency limiter and the usage ledger | `docs/dev-book/10-headless-service.md` §2.1, the six rows between "Model adapters" and "Gateway bridge" | **Six of the eight rows were false, measured 2026-09-24.** Every one of those modules has had a Rust counterpart for several increments: `core/engine.rs` (increments 1–10, and `HealthTracker` lives there too), `core/planner.rs` (11b), `core/router.rs` (13), `core/limiter.rs` (5), `core/ledger.rs` (12). The table was accurate when written — it was a *plan* — and it is the one place in the chapter that lists every module at once, so it was the one place no increment had a reason to revisit. Each increment updated the prose section it added and the parked row in [09](09-status.md); this table stayed at its pre-port state for the whole of Phase 2 and Phase 3. **Nothing could have caught it mechanically:** `build-dev-book` validates structure and links, and no test asserts that a doc's prose matches the tree, so a claim can be arbitrarily stale and every gate stays green. **What it cost is misdirection of the worst kind** — a reader planning the remaining work would have budgeted six modules that are already done, and would have had no reason to doubt it. **And the staleness concealed a real gap:** the adapter runtime is the only module genuinely unported, and it is also the only one with no phase assigned to it. Phases 1–6 are numbered and none of them covers the manifest interpreter or its sandbox; the table was the sole record that the module existed, and it was listed as "must be ported" rather than as "has no owner". Corrected rows carry the file and the increment; the missing phase is now the open decision, and §12's `rquickjs`/`boa` question is what decides it | **Stale** | **Fixed** 2026-09-24 |

| **D26** | The adapter runtime's size, cited as `adapter-runtime.ts:60` — "**60 lines**" — as the unit of work for the last unported module, and the same module's absence from §2.2's "what must move" arithmetic | `docs/dev-book/10-headless-service.md` §2.1 (the Adapter runtime row) and §2.2's line-count block | **The citation names the dispatch and omits the work, measured 2026-09-24.** `adapter-runtime.ts` is 60 lines / 2,462 bytes and contains only the `kind === "code"` branch that chooses an implementation. The implementations are `code-adapter.ts` at **615 lines / 24,820 bytes** and `manifest-interpreter.ts` at **491 lines / 21,903 bytes**. So the cited figure understates the module by roughly **14× in lines and 18× in bytes**, and points at the one file in the cluster that is not the work. **This is the causal half of D25, recorded separately because the defect is different in kind.** D25 is a status that went stale; this is a *size* that was wrong when written, and it is why the missing phase looked acceptable: a module described as "60 lines" needs no phase, while a module described as 24.8 KB of QuickJS host plumbing and a 491-line manifest interpreter obviously does. The two compound — the understatement made the module look too small to need an owner, and the table's staleness meant nothing revisited that judgement. The same omission ran through §2.2, where "what must move: ~2,100 lines" and "net new Rust: ~2,500 lines" were both computed from a module list the adapter runtime was not on. Confirmed by reading the files rather than the table: `wc -l -c` on all three | **False** | **Fixed** 2026-09-24 — the row cites all three files with byte counts, §2.2 itemises the ~1,100 lines and states that both arithmetic lines were derived from the incomplete list, and §2.1.3 records the spike that answered the module's open question. **Closed further 2026-09-24:** the module now has the phase this entry exists to explain the absence of — §7's **Phase 4b**. The number is lettered rather than renumbered because [11](11-cross-platform-tech-choice.md) shares this plan's numbering and its Phase 5 is the same "delete the bridge" step, so moving a number would have traded this drift for a new one |

| **D27** | The test for an unparseable modality pattern, named *"does not throw on an invalid regex — an unparseable rule simply never matches"* | `packages/router-core/test/modality.test.ts:19-20` | **The name and the assertion contradict each other**, measured 2026-09-24 while porting the module. The body asserts `.toThrow()`, and the body is right: `new RegExp("[")` throws, and **nothing catches it** — `manifest-interpreter.ts:230` and `code-adapter.ts:134` each return `tagModalityFrom(...)` directly, and neither consumer (`model-catalog.ts:54`, `contract-suite.ts:103`) wraps the call. So an unparseable pattern takes down catalogue building in the reference, loudly. **The wrong half is the half a porter reads first**: implementing the name gives a silent non-match, which demotes every model of that provider to `text` — and a modality rule is the one place a wrong answer routes an image request to a text model. The Rust port (`core/modality.rs`, increment 17) takes the assertion and returns `ModalityError::Pattern`; the TypeScript is left exactly as written, so the reference the port is measured against does not move under it | **False** (the name, not the assertion) | **Recorded** |

| **D28** | "Adding it would be a genuinely new **runtime** dependency of `aiproviderd`" — the price of §10 decision 5, and the reason `modality.ts` was deferred | `docs/dev-book/10-headless-service.md:1663-1664` (increment 15's note), and the same claim restated as §10 decision 5's first option | **False, and the measurement behind it was taken in the wrong configuration**, measured 2026-09-24. `cargo tree -e normal -i regex --no-default-features` does print *nothing to print* — but `--no-default-features` is not how `aiproviderd` ships. Under the default feature set the same command lists **three** parents: `tauri-utils` (through `tauri`), `urlpattern` (which `tauri-utils` also takes), and the direct edge increment 17 added. `regex` was therefore already in the runtime graph, and the option's true cost was **zero new crates**: `cargo tree --edges normal` resolves **307** with the dependency and **307** without it, set difference empty. The flag was in the command because the crate has a Tauri-free configuration to check (D15, D17) — and it silently changed the question from "what does the shipped binary link" to "what does the Tauri-free binary link". That is an error of **scope**, not of arithmetic, and the scope error is the harder kind to notice precisely because the command reproduces: anyone re-running it gets the same confident zero. Two further claims in the same passage were wrong for an unrelated reason: the feature set is not the dependent's to choose (Cargo unifies features per crate and `tauri-utils` takes `regex` with defaults on, so `default-features = false, features = ["std"]` was **inert**), and `aho-corasick` is in the graph rather than absent. Both fixed by measurement — `Cargo.toml` now declares a plain `regex = "1"`, because the requirement that must not be lost is `unicode`: with it off, `.` becomes byte-oriented and `^dall-e-.*$` stops compiling | **False** | **Fixed** 2026-09-24 |

| **D29** | "Rust host today: 25,612 lines across 25 modules" — stated as a current measurement, under a section titled "The line count" | `docs/dev-book/10-headless-service.md:360` (now `:375`) | **Stale**, measured 2026-09-24 while landing increment 18. No definition reproduces the pair. `apps/desktop/src-tauri/src/**/*.rs` is **42,696** lines across **51** files counted by `wc -l`; `src/core/*.rs` alone is **37,615** across 40; and `src/core/*.rs` with every `#[cfg(test)]` block excluded is **19,089**. The row is a planning snapshot written when the tree was smaller — increments 1–18 added roughly 17,000 lines to it — so it drifted without any single change making it false, which is precisely why no increment's gate caught it. The deeper defect is the **missing basis**: a line count that does not say whether it includes `#[cfg(test)]` blocks, or which directory it covers, is not checkable, and an uncheckable number in a row labelled "today" will rot again. **Fixed** by re-measuring and stating the basis in place | **Stale** | **Fixed** 2026-09-24 |

| **D30** | "`AdapterInstance: Send + Sync` (`core/adapter.rs`) is satisfiable, but only conditionally: `rquickjs` declares `Send`/`Sync` for `Runtime` and `Context` **under its `parallel` feature only** … Without it both are `!Send` and no `Arc<dyn AdapterInstance>` can hold one" | `docs/dev-book/10-headless-service.md` §2.1.3, the mapping table's paragraph (increment 14b's spike write-up) | **Half**, measured 2026-09-24 while adding the dependency. The stated half is true and reproduces: `parallel` does give `Runtime` and `Context` both bounds. The missing half is that **nothing that holds a guest value has them**, so enabling the feature does not by itself make the seam satisfiable. A probe crate asserting the bounds failed to compile with `error[E0277]`, for two independent reasons: `Persistent<T>` carries `rt: *mut JSRuntime` (`persistent.rs:37`) so the *wrapper* is `!Send`/`!Sync` whatever `T` is, and `T` itself — `Object<'static>` → `Value<'static>` → `Ctx<'static>` — reaches `NonNull<JSContext>` (`context/ctx.rs:74`) and `*mut c_void` inside `JSValue`'s `JSValueUnion`. `Ctx`, `Value`, `Object`, `Function` and `Persistent<T>` are therefore all `!Send` and `!Sync` in **both** directions. The consequence is architectural rather than cosmetic: an adapter cannot hold its `Runtime`, its compiled guest, or its parked resolvers in the struct that implements the trait, so the `Send + Sync` handle must be a **channel to a thread that owns them** — an actor. The original sentence reads as if the question were closed by a feature flag, and a reader planning the port would have written the field-holding shape first and discovered this only at the first `Arc<dyn AdapterInstance>` coercion | **Half** | **Fixed** — the actor landed as increment 19b (2026-09-24). `JsSandbox` is the `Send + Sync` handle and holds only a command channel and a `Mutex<Option<JoinHandle<()>>>`; every guest value — the `Runtime`, the compiled module, the parked resolvers — stays on the `js-sandbox` thread and crosses the boundary as a `Persistent` or a dumped `serde_json::Value`. `the_handle_is_send_and_sync` is the compile-time assertion |
| **D31** | "the reference's `emit` is where the per-operation line cap is enforced, and it records the failure to throw from the driver loop rather than from the callback … the same split is achieved by ignoring the budget error in the callback and letting the driver see it; `note_emitted_line` is charged either way, **which is what makes the cap bite**" | `core/js_host.rs`, `make_emit`'s doc-comment, as landed by increment 20a (`eed5b8d`) | **False**, measured 2026-09-24 with the Grep tool: `note_emitted_line`'s only call sites outside its own definition were **its own tests** (`sandbox.rs:1029`, `:1032`). `make_emit` truncated each chunk to `LOG_LINE_CAP` and pushed it, and never touched `budget.lines` — so `EMIT_LINES_PER_OP` was **unenforced** and a guest could emit without bound, while the prose beside it said the cap bit. The comment described the intended split accurately; the code implemented neither half of it. **No gate could see this**: `clippy`, `fmt` and all 971 tests were green, because the only artefact claiming the cap was the sentence claiming it | **False** | **Fixed** — increment 20b (2026-09-24): `make_emit` charges `note_emitted_line` and records its message for the driver, which also deletes a second copy of the limit's message string. `the_line_cap_ends_the_stream_and_keeps_what_was_already_emitted` pins it; the probe that drops the charge delivers **401** lines where the cap allows 400 (`left: 402, right: 401`) |

| **D32** | The hot-swap order in `AdapterRuntime.register` — "`const superseded = this.byProvider.get(providerId); if (superseded) void superseded.dispose?.(); this.byProvider.set(providerId, this.build(manifest));`", i.e. the superseded adapter is disposed **before** its replacement is built | `packages/router-core/src/adapter-runtime.ts:29-31` against `core/adapter_runtime.rs`'s `register`, as landed by increment 21 (2026-09-24) | **The order is an artifact of a discarded promise, and porting it would import a race as a determinism.** Two facts about the source, both readable rather than inferred. `dispose?.()` is `void`-ed, so it is fire-and-forget and **asynchronous** — the old adapter's teardown races the new one's construction instead of preceding it, which is not a stated "kill first" policy. And `this.build(manifest)` is evaluated as the *argument* to `set`, so a manifest that fails to build throws **before** `set` runs: `byProvider` keeps the old adapter, now disposed, and every later `forProvider` returns it and fails with `"adapter disposed"` — the provider is dead until something re-registers it. On the Rust side `dispose` is synchronous and **joins the actor thread** (`js_host.rs:473-479`), so the same order would additionally pay a join on a path that then fails. **The port therefore builds first, swaps, then disposes**, and a failed `register` leaves the previous adapter serving with the reason in its `Err`. The lock is released before the dispose runs, because the join may wait on an in-flight operation. `a_manifest_that_fails_to_build_leaves_the_previous_adapter_serving` pins the divergence, and the probe that restores the reference's order reddens exactly that test — the rest of the suite stays green, so the two orders are separable by one observation rather than by a general claim. The TypeScript is left exactly as written, so the reference the port is measured against does not move under it | **Divergence** (the reference's ordering, deliberately not ported) | **Recorded** — with the test and the probe that hold it |

| **D33** | The response-path tool-name restore for Claude Code — "Track renames in `_toolNameMap` for response restoration", and "Restore tool names: TitleCase -> lowercase via `_toolNameMap`" | `docs/gateway-flexibility-plan.md:113` and `:311`, against `packages/router-core/src/gateway-normalizer.ts:513-516` (`CLAUDE_REVERSE_MAP`, built and never read) and `:518-531` (`getRequestToolNameMap` / `trackToolName`) | **The restore was never implemented, and neither map has a reader.** `remapClaudeToolNamesInRequest` records every rename in `_toolNameMap` on three paths (`:557` tools, `:575` message `tool_use` blocks, `:588` `tool_choice`), and the module also builds a global `CLAUDE_REVERSE_MAP` by inverting the rename table (`:513-516`) — but a Grep for both names over `packages/router-core` finds **no read site**: the only consumer of `_toolNameMap` in the whole repository is `gateway-normalizer.test.ts:380`, the test for the writer, and `CLAUDE_REVERSE_MAP` is written at `:515` and read nowhere. So a Claude Code client that sends `bash` is remapped to `Bash` on the request and receives `Bash` back, because nothing restores the response. **No gate could see it** — the map is written, every test passes, and the only artefact claiming a reader is the plan's prose. Same signature as D31: a write whose only caller is its own test | **False** | **Fixed in the port** — `core::gateway_normalizer::NormalizedRequest::tool_name_map` carries the per-request map out of band (increment 22, 2026-09-24), so the response path Phase 5c builds has a consumer to read. The global `CLAUDE_REVERSE_MAP` is **not** ported: it would rewrite any TitleCase name, including ones the client never sent, where the per-request map records only the names *this* request renamed. `tracks_renames_in_the_returned_map` pins the content and `the_tool_name_map_never_leaks_into_the_body` pins the carrier. The TypeScript is left exactly as written, so the reference the port is measured against does not move under it |

| **D34** | The `MARKERS` array order, asserted in the port's own doc comment: "**The order is part of the specification.** When two markers begin at the same offset the reference keeps the earlier one (`at < startIdx`, a strict comparison), so the array order is the tie-break." | `apps/desktop/src-tauri/src/core/assistant_stream.rs:49-58` (the claim, now corrected), against `apps/desktop/src/lib/assistant-stream.ts:8-16` (the seven families) and its selection loop | **The tie-break is unreachable, so the order decides nothing.** Two families can match at one offset only if one start token is a prefix of the other; measured over all 7 start tokens, **0 prefix pairs**, so no tie can occur and `at <= s` behaves identically to `at < s`. This was found by trying to falsify the claim rather than by reading it — the *reachable* half (the earliest match wins, not the last) is real and is pinned by `handles_several_blocks_in_one_response`, so the defect is narrow: a documented rule about a state that cannot be entered. Same signature as D33 — a mechanism whose only evidence is prose — but here the prose is the port's own, which is why it was checkable | **Half** | **Fixed in the port** (increment 23, 2026-09-24). The comment now states the measurement, and the correction is a **guard rather than a rewording**: `the_marker_order_is_not_load_bearing` asserts the prefix-freeness across all 7 families, so adding one that *can* tie reddens it and the tie-break becomes live again. The TypeScript is left exactly as written |

| **D35** | Phase 5d's scope, as the plan states it: "Delete `EventBridge`, the worker, `gateway.html` and `app_nap.rs`, and switch `build_core`" | `docs/dev-book/10-headless-service.md`, the Phase 5 split table, row 25, against `core/gateway.rs:44`, `:65`, `:1140`, `:1299`, `:1373` and `tauri/gateway_cmds.rs:105`, `:889` | **The four files are real, and they are not the whole phase.** What they implement is a *liveness subsystem* that exists only because the worker is a webview the OS can suspend: `HEARTBEAT_STALE_MS` = 6 s and `HEARTBEAT_STALE_HIDDEN_MS` = 30 s (`core/gateway.rs:44`, `:65`), `beat_is_fresh()` (`:1140`), `FIRST_MSG_TIMEOUT` = 30 s (`:1299`), the `gateway_heartbeat` command (`tauri/gateway_cmds.rs:889`), `ensure_bridge_window` (`:105`) with its worker-warmup window, the background-mode bounds, the re-warm on the request path, and the three `r1_*` tests — plus `app_nap.rs`, whose entire reason to exist is that a hidden webview's JS stops beating. **The measured hazard:** the webview beats every 2 s (`gateway-bridge.ts:130`), `beat_is_fresh()` is false after 6 s, and a request arriving on a stale beat is *waited out and then 503s* (`core/gateway.rs:1373`). So deleting the worker without retiring the liveness gate makes every request a 503 six seconds after the app stops beating — and the deletion row does not name the gate. `RouterBridge` cannot simply beat instead: `ReplyHandle` deliberately does not hold the core (the cycle-avoidance of increment 12), so the beat has no source unless one is designed | **Half** | **Fixed in 24c** (2026-09-24). The beat's source is `Bridge::ready(&self, beat: Beat) -> bool`: the bridge answers for itself, and `Beat` carries the age and the visibility so the bound stays the bridge's own rule. **Two departures from the shape this cell first proposed, both measured.** (1) **No default body.** The two answers are opposites — a webview bridge is ready exactly when its beat is fresh, a bridge in this process always — and a default of `true` would let a *future* webview-backed bridge silently inherit "always ready", which is the hazard itself. The compiler's `E0046` immediately justified the choice by surfacing a **sixth** implementor (`core/context_scope.rs:1336`) that a Grep for `impl Bridge for` had missed, because it spells the path in full. (2) **No `warm()`.** The re-warm hook and its rate limiter are core-owned state, and `request_warm` is already a no-op with no hook installed, so a bridge-side duplicate would be a second spelling of it. `EventBridge` delegates to `webview_ready`, the shared function the three doubles also call — `EventBridge` is the one implementation no test can construct, so putting the decision in a shared function is what lets the R1 heartbeat tests guard it. **Superseded 2026-09-24 by 25f, and deliberately.** The seam was deleted, and not because the design was wrong: its entire content was the *difference* between the two answers, and the webview's answer left with the webview. With `EventBridge` gone, every remaining implementor (`RouterBridge`, the headless bridge, the three doubles) would answer `true`, so `try_slot`'s "core unavailable" branch would have been reachable only from a test double — the "branch that cannot fire in production" class (D31, D33, D34). Keeping the seam with a constant answer would have preserved the shape and destroyed the meaning. So `try_slot` now has one refusal, `!core.is_running()`. **The hazard this entry is about is closed by the deletion rather than by the seam:** the 6-second stale beat and the 503 behind it no longer exist in the process, because nothing in the process can be suspended. Deleted with the seam: `Beat`, `Beat::is_fresh`, `webview_ready`, `await_core`, `CORE_RECOVERY_GRACE`, both heartbeat bounds, the warm hooks, the whole `core_recovery_tests` module, the four `Bridge::ready` tests and the three `r1_*` tests. `webview_ready`'s coverage argument — that it put the decision where the doubles could reach it — is answered by there being nothing left to decide |

| **D36** | The Phase 5 reconnaissance sentence, in the present tense: "A Grep for `normalize_gateway_request`, `detect_client`, `parse_assistant_stream`, `to_wire_tool_calls`, `registry_to_openai` and `MAX_TOOL_ITERATIONS` over `src-tauri/src` returns **no matches**, and `tauri/tools.rs` holds no OpenAI tool schemas at all." | `docs/dev-book/10-headless-service.md`, the Phase 5 preamble, against the tree after increments 22, 23, 24a and 24b-i | **The measurement was true when it was taken; the tense was not scoped to it, so all six names now resolve.** `normalize_gateway_request` (`core/gateway_normalizer.rs:1056`) and `detect_client` (`:109`) landed in 22; `parse_assistant_stream` (`core/assistant_stream.rs:346`), `to_wire_tool_calls` (`core/tool_wire.rs:77`) and `registry_to_openai` (`core/tool_registry.rs:58`) in 23; `MAX_TOOL_ITERATIONS` (`core/bridge_policy.rs:92`) in 24b-i. This matters more than a stale line usually would, because the sentence is the *justification* for splitting Phase 5 into increments: a reader meeting it now would conclude the split had not happened. The conclusion itself survives — the bridge was the only place the client-facing pipeline lived, which is why the split was needed — but the evidence has to be dated to stay honest | **Half** | **Fixed in 24b-i** (2026-09-24). The sentence is scoped to the reconnaissance and carries the increment each name arrived in, so it reads as the finding it was rather than a current fact |

| **D37** | Two forward references naming "Phase 2" as the phase that replaces the webview bridge: the `Bridge` trait's own doc comment ("Phase 2 of the headless plan will put a Rust router core here") and `bin/aiproviderd.rs`'s module note ("Phase 2 ports the router core to Rust and replaces `HeadlessBridge` with it") | `core/gateway.rs`, on the trait itself, and `bin/aiproviderd.rs`, against the phase list in `10-headless-service.md`, where Phase 2 is "Port the execution engine" and the bridge is Phase 5c | **Both point at the wrong phase, and the reader they mislead is the one looking for the work.** Phase 2 landed the execution engine — a real phase, correctly named — and did not touch `HeadlessBridge`; a contributor following either comment would search the engine's commit range for a bridge replacement and find none. The trait comment is the worse of the two, because it sits on the seam itself and is the first thing anyone implementing a bridge reads. This is D36's family — a claim whose *scope* was wrong rather than its measurement — and it is the second in two increments | **Half** | **Fixed in 24c** (2026-09-24). Both now name Phase 5c, which is where the bridge actually lands |
| **D38** | The Phase 5 split table's row 24b-ii: "`core/router_bridge.rs` — the driver: a Rust-native `Bridge` running the tool loop against `ModelRouter` and `AdapterRuntime`, writing to `ReplyHandle`" | `docs/dev-book/10-headless-service.md`, the Phase 5 split table, row 24b-ii, against `core/router.rs:551` (`generate_text(&mut self, …)`), `core/engine.rs:894` and `:614` (`health: &mut HealthTracker`), and `core/router.rs`'s `health`, `cursors` and `ledger` fields | **The row names one deliverable and no prerequisite, and the code imposes one: a `Bridge` must be `'static` and serve requests concurrently, and `ModelRouter` cannot.** Three measurements. (1) `execute_text` and `execute_image` take `&mut HealthTracker` and hold it for the length of a request, so two concurrent requests cannot share one tracker — the borrow checker makes "two at once" unrepresentable, and nothing surfaces that until something tries to serve two clients. (2) `generate_text` and `generate_image` take `&mut self`, so two concurrent requests cannot share one router. (3) The three pieces that *must* be process-wide — the circuit breaker, the key cursors, the ledger — are private fields of a router that would have to be rebuilt per request. **Both ways of writing the driver against the row as it stands are defects, and neither is loud.** A `Mutex<ModelRouter>` serialises every model call in the process, which makes §3.5's two semaphores and their 40 permits decorative while every test still passes. One router per request gives each request a private breaker, a private ledger, and a cursor that never advances past zero — so the round-robin that spreads load across a provider's keys stops existing, silently, and no existing test notices because **every one of them builds a single router** | Half | **Fixed in 24b-ii-a** (2026-09-24). `HealthTracker` gains a `Mutex` inside it, so the engine takes `&HealthTracker` and the lock is per *record* rather than per request; `SharedRouterState` bundles the breaker, the cursors, the ledger and the limiter behind one `Arc`-backed handle; `ModelRouter::with_shared` builds a request-scoped router from it. The round-robin claim is pinned by `two_routers_from_one_shared_state_share_the_key_cursor` **and** by its control, `two_routers_without_shared_state_keep_their_own_breaker` — the pair is what makes the sharing a claim rather than a description. Six falsification probes, all red, both files byte-exact (`1b5ac27f…`, `fd01c1f7…`); **P5 reddens exactly one test**, the end-to-end one, which is the measurement that the mechanism-level tests would not have caught a private tracker on the request path |
| **D39** | The Phase 5 split table's row 24b-ii-b: "`core/router_bridge.rs` — the driver: a Rust-native `Bridge` running the tool loop against `ModelRouter` and `AdapterRuntime`, writing to `ReplyHandle`" | `docs/dev-book/10-headless-service.md`, the Phase 5 split table, row 24b-ii-b, against `core/router.rs:124` (`RouterStore::hydrate`, whose only callers are its own tests), `core/persist.rs:51`/`:199`/`:324`/`:404` (the four row readers, all `#[cfg(feature = "app")]` and `State`-shaped), `core/egress.rs:347` (`stream`, `app`-gated on a `tauri::ipc::Channel`) and `core/persist.rs:505` (`ledger_insert`, a plain `&Connection` and still `app`-gated) | **The row names one deliverable and three prerequisites it does not have, and the consequence is that the driver cannot be installed.** Three measurements, each a line of code. (1) **Hydration.** `RouterStore::hydrate` is called **only from `router.rs`'s own tests** — a Grep for its call sites finds no production one — and the four readers that would feed it are `#[cfg(feature = "app")]` and take `State<'_, Arc<Store>>`, so a headless launch has no way to build the store the router reads. (2) **Streaming egress.** `egress::stream` is `app`-gated because its last argument is a `tauri::ipc::Channel<StreamEvent>`, and `generate_text` always streams (`router.rs:717`) — so **without it there is no text path at all**, not a degraded one. (3) **The ledger row.** `persist::ledger_insert` takes a plain `&Connection` and is nonetheless `app`-gated, and no production `LedgerSink` exists (`trait LedgerSink` at `ledger.rs:50`, `UsageLedger::with_sink` at `:82`) — so even a served request writes nothing. **What this is not:** a defect in the driver. The driver is written against seams (`Arc<RouterStore>`, `Arc<dyn AdapterFactory>`, `Arc<dyn BridgeHost>`), so it is fully testable and its interface is fixed; what is missing is the three concrete implementations the seams are waiting for. **No gate can see this** — `clippy`, `fmt` and all 1160 tests are green, because a module with no caller compiles and a seam satisfied only by a test double compiles too. Same signature as D31 and D33: the only artefact claiming the capability is prose | Half | **All three closed, and a fourth prerequisite this cell did not name** — the module header names the gaps, so they sit on the artefact rather than in a commit message. Porting `egress::stream` off `tauri::Channel` landed in **25a**; gating `persist`'s non-Tauri readers on `core` rather than `app` landed in **25b**; the store-backed `LedgerSink` landed in **25d** — `persist::ledger_insert` is `pub` and un-gated, and `core::ledger::StoreLedgerSink` is the **first production `LedgerSink`**, every implementor before it having been a `#[cfg(test)]` double. It writes on the store's own connection with no command in the path, sanitises with `ui_db_error` because `RouterError::Ledger` answers **502** (`router_bridge.rs:602`) and the raw rusqlite text would put SQL and absolute paths on the wire, and writes synchronously under the ledger's own lock (`ledger → conn`, which cannot deadlock because nothing in `persist` reaches back for the ledger). **25b also found a prerequisite that was in none of the three: the router settings had no core reader at all.** `BridgeHost::settings` — the seam that exists precisely so the bridge never holds a second copy of the operator's toggles — had no production source. The row lives at `settings.key='router'` as camelCase JSON that the webview writes (`store.ts:655`) and applies with `Object.assign` inside a `try`/`catch` that keeps the defaults (`:390-397`), and the only reader in Rust was the generic `settings_get` command. Closed in 25b by `persist::setting_value` plus `RouterSettings::from_store` / `from_value`, which copies `perProviderConcurrency` through **un-clamped** so that `clamp_concurrency`'s corruption branch stays reachable from the one path that can produce a corrupt value |

| **D40** | The plan's own account of the `HttpPort` seam, in the future tense: `core/http_port.rs:4` says "**`egress.rs` will implement this trait; `interpreter.rs` consumes it**", and `docs/dev-book/10-headless-service.md:1791` says "**`egress.rs` will implement it and `interpreter.rs` consumes it**" | `core/http_port.rs:4` and `docs/dev-book/10-headless-service.md:1791`, `:1976`, against all of `src-tauri/src` after increments 20a–24b-ii-b | **The trait had no production implementor, and the consequence is larger than the sentence: the Rust provider path has never been connected in production, in either build.** `impl HttpPort` occurs **seven** times and every one is inside a `#[cfg(test)]` module (`interpreter.rs:1218`, `:2265`; `js_host.rs:1185`, `:1563`; `code_adapter.rs:431`, `:753`; `adapter_runtime.rs:269`). Four more constructors have no production caller: `AdapterRuntime` (its only mention outside its own module is a doc comment at `router.rs:1441`), `ModelRouter::new` (61 call sites — every one in `router.rs`'s tests, plus `router_bridge.rs:163`), `RouterStore::hydrate` (tests only), and `RouterBridge` (none, by construction — D39). So `ModelRouter → AdapterRuntime → HttpPort → egress` is a chain in which **every link is built and tested and no link is connected**; the plan's own `:1976` says "Phase 5 is what reaches it", and Phase 5's row 25 reaches nothing — it deletes. Meanwhile `bin/aiproviderd.rs` installs `HeadlessBridge` with `ready() == false`, so every completion there is a deliberate 503, and the desktop app's only working text path is the **webview** doing provider HTTP itself through the `egress_stream` Tauri command. **The second half of the finding is that the egress's network functions had no test at all:** `egress.rs`'s three test modules are pure — allowlist, secret injection, host scanning — and no test in the crate performed an HTTP request, so `request`, `stream` and `fetch_image` were exercised only in production, by a webview. **What this is not:** a defect in any of those modules. Each is written against a seam and is individually sound; what was missing is the edge. **No gate can see it** — `clippy`, `fmt` and all 1160 tests are green, because a trait whose only implementors are test doubles satisfies the compiler and a function with no caller satisfies it too. This is D39's family one level up: D39 found three gaps in the *driver's prerequisites*; this finds that the driver's *dependencies* have no production instance either | False | **Fixed in 25a–25e** (2026-09-24). `core/egress_port.rs` implements `HttpPort` for `EgressPort` — the first production implementor. Unary maps to `egress::request`; streaming spawns `egress::stream` and consumes its events through an `mpsc` channel as the pull stream the trait promises, with the status and headers read out of band before the response is built. `egress::stream`'s sink is now an un-gated `UnboundedSender<StreamEvent>` instead of a `tauri::ipc::Channel`, so **`egress.rs` carries no `cfg(feature = "app")` at all** and the headless build compiles it; `tauri/commands.rs:80` keeps its `Channel` and forwards through one task. Five tests, the first in the crate to drive the egress over a real socket (a loopback axum server), plus two falsification probes — the `>= 400` mapping and the cancellation watcher — both red, file byte-exact. `AdapterRuntime` is **half** closed: 25c's `core::activation::activate` is `register`'s first production caller, but `AdapterRuntime::new` still occurs only in `#[cfg(test)]` code (`activation.rs:149`, `adapter_runtime.rs:347`) — so the constructor's first production caller is the install, 25e, and the tally below stays at two, because a method's caller is not a constructor's. **25b** closed the `RouterStore::hydrate` row: `RouterStore::from_store(store: &Store)` is its first production caller, built on the four now-un-gated `&Store` readers (`providers_rows`, `api_keys_rows`, `models_cache_rows`, `aliases_rows`) with the `#[tauri::command]` wrappers left behind as one-line delegates — and `RouterSettings::from_store` gave `BridgeHost::settings()` the source it never had (D39). **All five constructors now have production callers**: `HttpPort` in 25a, `RouterStore::hydrate` in 25b, `AdapterRuntime` in 25e (`aiproviderd.rs:148`), `ModelRouter::new` and `RouterBridge` in 25e (`router_bridge.rs:172` and `aiproviderd.rs` respectively) — the chain `ModelRouter → AdapterRuntime → HttpPort → egress` is now connected in the headless binary |

| **D41** | The Phase 5 split table's row 25c: "The manifest activation path — read `manifests.body_json` and call `AdapterRuntime::register`" | `docs/dev-book/10-headless-service.md`, the Phase 5 split table, row 25c, against `apps/desktop/src/store.ts:367-381` (the reference's activation loop) and all of `src-tauri/src` after increment 25c | **The row names the fallback and not the branch the reference tries first, and that branch has no Rust port.** `bootstrap`'s loop runs **over providers**, and for each one it first tries `PROVIDER_PROFILES[p.slug]` — three builtin manifests (`openrouter`, `opencode`, `b.ai`) built from the two ported templates — `continue`ing on a hit and only *then* falling back to that provider's active manifest row. **There is no Rust `PROVIDER_PROFILES`**: the name does not occur anywhere in `src-tauri/src`, and the `openai_compat` / `anthropic_compat` values in `core/manifest_view.rs:284`, `:349` are `#[cfg(test)]` fixtures used by two tests. So `core::activation` iterates **manifest rows** rather than providers. **Measured against the installed database on 2026-09-24, the two agree**: **2** providers, both `type='manifest'`, both carrying an active row, and **no builtin provider installed** — so nothing on this machine depends on the missing branch, and that is exactly why it was invisible. They would not agree on an install that had one: `Providers.tsx:305` creates providers with `type: "builtin"` and a builtin slug, and such a provider with no manifest row is served by its profile in the reference but would go **unregistered** here, answering `no active manifest for provider …` on every attempt. **No gate can see it** — `clippy`, `fmt` and all 1178 tests are green, and the two activation paths agree on the only database anyone has measured. `core::activation`'s module note states the divergence *and* the measurement rather than leaving 25e to find it | Half | **Closed** 2026-09-25 (25j) — `core/builtin_templates.rs` is the port, and `activation` now iterates **providers** with the profile tried first. **Two facets, and D41 recorded only the first.** *(1)* **A builtin provider with no manifest row went unregistered** — reachable, because `addProvider` writes the provider row **before** the manifest row (`store.ts:495-502`) and its `catch` rolls back in-memory state only, so a failure between the two leaves a row-less provider permanently. *(2)* **A builtin provider *with* a row was served the stale stored one** — the row is written once, at creation, with `version: 1`, and nothing re-seeds it on upgrade, while the reference ignores that row for a builtin slug and serves the *current* profile. Facet 2 needs no failure to reach: it holds for every builtin provider from the second release onwards. **The key is the slug, not `type`** (`store.ts:368` is `PROVIDER_PROFILES[p.slug]`), so keying on `type` would have diverged in the other direction, and invisibly, for the same reason this one was. **The profile dials `providers.base_url`** — the reference's `withBaseUrl(profile(), p.baseUrl)` — which is the column the allowlist is derived from, so a builtin provider **cannot trip D46**. **One additive divergence, taken deliberately:** a provider with neither a profile nor a row is now *reported* at activation, naming the slug and the write order; the reference's loop falls off the end silently, and that silence is what made this invisible — the symptom arrived later, from a different module, as `no active manifest for provider …` on every request. `Skipped.version` became `Option<i64>` so "no row involved" is `None` rather than a `0` sentinel. **Four probes, one at a time:** removing the profile branch reddens the 5 profile tests **alone**; making `provider_profile` ignore its `base_url` argument reddens 8 across both modules; removing the no-row report reddens its own test **alone**; inverting precedence reddens the 2 tests that are about precedence — the 2 further failures there were artefacts of the probe's simplified row path, not of precedence, and are recorded as such rather than claimed. **D49** — a builtin provider was *served* by its profile and *reported on* from its row:  read  directly, so after D41 facet 2 a builtin with a stale row that lacked  would report  about an adapter that in fact forwarded them.  (25k) is the one function both consumers now read; the profile branch is the same, the outer join is load-bearing for the no-row case, and three probes each redden exactly their own test |

| **D42** | Four webview-era statements left in Rust doc comments, in the **present tense**, after the webview was deleted: "The webview's `onUsage` forwards two fields to `gateway_usage` (`gateway-bridge.ts:299-305`) and that is what reaches the response body"; "`core/gateway.rs` **today** forwards a client's body to a Tauri WebView, where the TypeScript normalizer runs (`gateway-bridge.ts` → `packages/router-core`)"; "The worker has already applied a deliberate whitelist (`gatewayStatus()` in `gateway-bridge.ts`)"; and "`router_bridge.rs` **will be** the driver … The reference is `apps/desktop/src/gateway-bridge.ts` (421 lines)" | `core/usage.rs:14-15`, `core/gateway_normalizer.rs:12-13`, `core/gateway.rs:1595-1596`, `core/bridge_policy.rs:3` and `:6`, measured after 25f | **Stale**, and every clause is falsifiable by one command. A Grep for `gateway-bridge.ts`, `gatewayStatus()`, `gateway_chunk` and `gateway_usage` over `src-tauri/src` returns **four files, all of them prose** — no code. `apps/desktop/src/gateway-bridge.ts` no longer exists; `gateway_usage` and `gateway_chunk` are not among `commands.rs`'s `generate_handler!` entries (they left with the eight reply commands); and `router_bridge.rs` is not a *future* driver — it **is** the driver, landed in 24b-ii-b and installed in the binary by 25e. **The `bridge_policy.rs` pair is the D40 family** (a future tense in a module header that has since become past), and it is the more costly of the four because `bridge_policy.rs` is the module a reader opens first when asking what the bridge decides. Nothing could catch these mechanically: `build-dev-book` validates structure and links, not prose, and a doc comment naming a deleted file is not a broken link. **This is D13's class at a new address** — the split moved modules and left `file:line` citations pointing at nothing; 25f deleted files and left `file:` citations pointing at nothing | **Stale** | **Fixed** 2026-09-24 — each re-tensed to the past with the deletion named, and `bridge_policy.rs`'s header now says the driver **is** `router_bridge.rs` and that its reference is a deleted file |

| **D43** | The gate test's own doc comment, offered as the guard on `try_slot`'s refusal: it would "fail loudly if a wait is ever reintroduced on this path" | `core/gateway_tests.rs`, on `a_stopped_gateway_is_refused_without_waiting_for_a_bridge` (as written before 25f's correction) | **False, measured 2026-09-24 by the probe that was supposed to confirm it.** Inserting `tokio::time::sleep(Duration::from_secs(5))` before `try_slot`'s `if !core.is_running()` refusal left the test **green in 0.02 s**. The cause is a property of the request path rather than of the gate: **all six** call sites of `try_slot` (`gateway_handlers.rs:72`, `:282`, `:384`; `gateway_anthropic.rs:351`; `gateway_gemini.rs:208`; `gateway_responses.rs:256`) are preceded by `check_gateway_key` (`gateway.rs:1389`), which refuses a stopped gateway at `:1394` *before* `try_slot` runs. So the test asserted the **auth** gate's status and the auth gate's timing, while its comment claimed to guard the **slot** gate — and the wait it named (`await_core`, `CORE_RECOVERY_GRACE`) lived inside `try_slot`, a function this path cannot reach. **A timing assertion taken only through the request path pins whichever refusal answers first**, which is the general form; this is D18's family (a test whose claim is broader than its check), found the same way D18 was — by trying to falsify it rather than by reading it | **False** (the comment, not the assertion) | **Fixed** — the end-to-end test is kept with its comment recording the measurement, and a **direct** `try_slot(&s.core)` test (`the_slot_gate_refuses_a_stopped_core_without_waiting`) pins the slot gate's own refusal, reachable because `gateway_tests.rs` is `#[path]`-included as a child module of `gateway`. Falsified before it was trusted: the same five-second sleep reddens the new test and leaves the end-to-end one green, so the two now observe different gates |

| **D44** | Four present-tense claims about machinery 25f deleted, in files 25f did not touch: "The gateway's availability is bound to the webview's *process* … a reloading or crashed renderer means `503` for every client"; "the `Bridge` trait itself is untouched — `aiproviderd` still installs `HeadlessBridge`, which discards every dispatch, so the 503 contract is unchanged"; "**This row stays, and Phase 1 is why** … The gateway's availability is still bound to a webview until Phase 2 ports the router core to Rust"; and, in §1.1, "The gateway worker window stays alive. The heartbeat bound relaxes from 6s to 30s … App Nap is fought with a native heartbeat" | `docs/dev-book/09-status.md:237-263` (three paragraphs of the headline "Needs improvement" section), `docs/dev-book/10-headless-service.md:28-32` (§1.1), `scripts/soak-gateway.sh` (its entire header), `scripts/repro-restore.sh` (its rationale) | **Stale, and this is the most expensive class of the four 25f entries** because it is the *headline* limitation rather than a line number: a reader opening the status page was told the gateway was still webview-bound and that the standalone service discarded every dispatch, both of which 25e and 25f had ended. Measured 2026-09-24 after 25f with the Grep tool: `is_available`, `HeadlessBridge`, `WATCHDOG_STARTED`, `Beat` and the `worker window ready in Nms` log line no longer occur in `src-tauri/src`; `gateway_cmds.rs:208`/`:211` keep `enable: starting` and `enable: listener bound in Nms` and nothing between them. `soak-gateway.sh` was the worst of the four — a working 50-line harness whose stated purpose was "correlate against the heartbeat watchdog", which no longer exists, so it would have run green and correlated against nothing. **The meta-finding, and the reason this is its own entry:** 25f's Pass 4 swept the Phase 5 table, the increment narratives, the register and the Rust doc comments — but not the status page's headline limitations or the scripts — because the sweep was scoped to **the files the increment edited**, and the files a deletion *falsifies* are a strictly larger set. A deletion increment must grep for the *names* it removes, not walk the list of files it touched | **Stale** | **Fixed** 2026-09-24 — the three status paragraphs rewritten (the third now records that the row is closed and why the paragraph is kept), §1.1 carries a dated scope note, `repro-restore.sh`'s rationale names the removed stage, and `soak-gateway.sh` was **retargeted rather than deleted** because the mechanism outlived the rationale. Remaining hits in `DECISIONS.md`, `diagrams/audit-2026-09-18.html` and `AGENT_PROMPT_HEADLESS_SERVICE.md` are dated records, left as history under D13's rule |

| **D45** | The 25e row's claim, and D40's closing verdict offered as its evidence: "The binary now answers `ready() == true` and **serves completions** through the Rust-native router core", on the strength of "all five constructors now have production callers" | `docs/dev-book/10-headless-service.md`, the Phase 5 split table, row 25e; `bin/aiproviderd.rs:120` | **False, and only a run against a real provider could say so.** Measured 2026-09-24, first end-to-end run: `aiproviderd` on 8800 with the keychain master key, `POST /v1/chat/completions` for `agnes-2.5-flash` → **502**, `{"error":{"message":"all attempts failed for agnes-2.5-flash [agnes/key-01:NETWORK -> agnes/Key-02:NETWORK]"}}`. The cause is one line: `let allow = Arc::new(AllowList::default())`. `AllowList` is `#[derive(Default)]` over a `HashSet` (`egress.rs:57-58`), so the default is **empty**, and `check_url` (`:148`) refuses every non-local host not in it — the module's own test asserts exactly that (`check_url(&AllowList::default(), "https://attacker.example/v1").is_err()`, `:631`). The app fills the list from provider CRUD (`persist::recompute_allow`, called at `persist.rs:137` and `:166`); `aiproviderd` called it **nowhere** — a Grep for `recompute_allow` over `src-tauri/src` returned the definition and those two call sites and nothing else. So every outbound provider call was `HostDenied`, which the attempt layer reports as `NETWORK`: **a 502 that blames the upstream for a purely local policy refusal**, which is why the first diagnosis was "is the provider reachable" rather than "is the allowlist populated". **Why no test could see it, and why D40's criterion was the wrong one:** every constructor *did* have a production caller, `AllowList::default()` being empty is intended behaviour rather than a defect, and the app populates the list on a path (`persist`) the service does not use. The defect lives in the **composition** of two correct pieces — the exact class D40 named and could not test. "Every link has a production caller" asks whether the chain is *assembled*; only a request asks whether it *conducts* | **False** | **Fixed in 25f verification** 2026-09-24 — `persist::recompute_allow(&egress_state, &store)` at boot, before the adapter runtime is built. Re-verified end-to-end: `POST /v1/chat/completions` → **200**, `{"content":"\n\npong"}`, `usage {prompt_tokens: 1721, completion_tokens: 19}`, **and a durable ledger row re-read from the store** (id 1532, `status=ok`, `provider_id=8234f8af…`, `tokens_in=1721`, `tokens_out=19`, `key_id=2f4b37eb…`, `latency_ms=10448`) — the row rather than the response body, because a 200 alone would not prove the store-backed sink was reached. Also measured: `/v1/models` → 200, **467** models across both providers; a bad key → **401** `invalid_api_key`; no header → **429** |

| **D46** | "cannot widen the allowlist (only `provider_upsert`/`provider_delete` mutate it, host-side)" — the security property that a compromised webview cannot reach a host the operator never registered | `docs/ARCHITECTURE_AUDIT.md:113-114`, against `core/persist.rs:137`/`:166` and `bin/aiproviderd.rs:135` | **Stale in its parenthetical — and measuring it turned up a sharper hazard than the one it mis-states.** There are now **three** call sites of `persist::recompute_allow`, not two: `aiproviderd.rs:135`, the line D45's fix added. The security *conclusion* survives, because a webview still cannot write `providers`; this is an incomplete enumeration, not a hole. What the same measurement exposed is the divergence the enumeration conceals: **the allowlist is derived from `providers.base_url` while the adapter calls the URL in `manifests.body_json`'s `endpoints`, and the two can disagree.** Repointing **only** `providers.base_url` at `http://127.0.0.1:8799/v1` left the active agnes manifest still naming `https://apihub.agnes-ai.com/v1`; **56 of 56** `POST /v1/chat/completions` then answered `502 … [agnes/key-01:NETWORK -> agnes/Key-02:NETWORK]` in 14–19 ms, while the stub's own request counter stayed at **30** — all of them the harness's direct calls, none of them the gateway's. Repointing the manifest too made every call **200** and the counter climb. `check_url` (`egress.rs:148`) denies any non-local host absent from the list, so the refusal is local policy reported as `NETWORK`: **D45's misleading 502 reached by a second route.** The app keeps the two in step only because provider CRUD writes both, so the trap is latent — it fires for anyone who edits one without the other, which is what a custom-base-URL or proxy feature would do | **Stale** | **Closed** 2026-09-24, in two halves. **(25h — the boot path)** **Candidate 2, taken as written**: `activation::activate` takes the egress allowlist and refuses a manifest whose host it will not permit, so the divergence is reported **once, at launch, by name**, instead of once per request as a `502 … :NETWORK`. `egress::host_is_permitted` is the shared predicate, so the assertion and the enforcement cannot drift apart — the failure mode of any second spelling of "is this host allowed". Reachability is unchanged, which is the argument that made this safe: a provider whose host is not allowlisted could not be dialled before either, so the fix changes **when and how it is said**, not what works. **(25i — the runtime path)** The half 25h left open, closed on its own terms: a provider edited *after* launch does not re-activate, so a mismatch created at runtime still reaches `check_url` — but it no longer reports `NETWORK`. `AttemptError::Blocked` and `ErrorClass::EGRESS_DENIED` are new, and the interpreter's one mapping asks `HttpError::is_denied()` — the *kind*, never the message — so a refusal by our own policy is no longer filed as evidence about the provider. **The cross-language divergence 25h predicted is taken rather than deferred:** `ALL_CLASSES` is still walked against the TypeScript union spelled out verbatim, and the extra token is recorded in `RUST_ONLY_SPELLINGS`, so a *second* divergence must be added there by name in a reviewed diff instead of being absorbed by a loosened assertion. The class is asserted **not** drift, **not** retryable with the next key (every key of a provider shares its host), and **not** recorded against the key. **Candidate 1 was rejected on measurement, not on taste:** deriving the allowlist from the manifest would let a manifest widen it, and constraining the adapter's destinations to hosts the operator registered is the one property the allowlist exists to hold |

| **D47** | The guard comment on the unary/streaming branch: "`args.stream && ep.stream` — a caller may ask for a stream from a manifest that declares none, and **the source then answers with a unary response**" | `core/interpreter.rs:419-421` (the comment, now corrected at `:419-428`), against `:427` (`values.insert("stream", Value::Bool(stream))`), `:701` (`unary_text`'s `serde_json::from_str`), and the reference `manifest-interpreter.ts:273`/`:306` | **The second half is false, in the reference as much as in the port**, measured 2026-09-24 while turning the latency harness into an assertion. The guard decides only how the response is **read**; it does not decide what is **asked for**. `values.stream` is the *caller's* flag — the reference's `stream: args.stream` (`:273`) verbatim — so a manifest that declares no `stream` block still renders `"stream": "{{stream}}"` as `true`, and a server that honours it answers with SSE. The interpreter then takes the unary branch, parses that SSE with `responseMap.text`, fails inside `serde_json`, and returns `AttemptError::Transport` — which the engine classifies as `NETWORK`, so the client receives `502 … [stub/k1:NETWORK]`. **This is the third route to D45's misleading 502, and the second that involves no allowlist at all**, so neither of D46's two candidate repairs reaches it. **How it was found is the point:** the end-to-end test's fixture manifest omitted the `stream` block, the stub answered the resulting `stream: true` with SSE, and the failure presented as an unreachable upstream. Three probes were needed to place it — a direct `egress::request` returned `200`, an `EgressPort` call with `stream: true` printed all six deltas, and three `eprintln!`s inside `run_text` printed **nothing**, which is what proved the failure sat *past* the egress rather than in it. **Reachability, measured against the installed database:** 3 manifest rows, all 3 declaring a `stream` block with `tpl.stream = '{{stream}}'`, so the trap is **latent here** and fires for a hand-written, imported or Generator-produced manifest — D41's shape, and invisible for the same reason. **No gate could see it:** `streaming` is a local and the branch it guards is unreachable while every manifest declares the block, so the comment was the only artefact making the claim | **False** (the comment's second half) | **Recorded** 2026-09-24 — the comment now states the measured behaviour. The **code is deliberately left as the reference has it**: binding the template value to the computed `streaming` rather than the caller's flag would be a behavioural divergence no increment has authorised, and it would put the port at odds with the reference it is measured against. The candidate repair is named here rather than taken |

| **D48** | The unreachability claim on `streaming`'s fallback arm, and the message it prints: *"The sender dropped with nothing sent: the driver failed before it could classify … this is the unreachable residue"*, reporting `"the egress ended before reporting response headers"` | `core/egress_port.rs` (`streaming`'s first `recv()`), against `core/egress.rs` (`egress::stream`'s `build(state, req).await`), measured 2026-09-24 while giving the refusal its own class (25i) | **The arm is reachable, and for exactly the case 25i exists to fix.** `egress::stream` calls `build` — which is where `check_url` refuses — **before** its send loop, and it used to return on that error **without sending anything**. So a denied host on the *streaming* path emitted **no event at all**: the sender dropped, the consumer's first `recv()` returned `None`, and the consumer reported the one thing certainly false — *"the egress ended before reporting response headers"* — from the arm whose own comment claimed to be unreachable. Nothing was dialled, so nothing could have ended. **This is the third message in the family to name the wrong party**, and the only one that names no party at all: D45, D46 and D47 each produced a `502` that blamed the upstream for a local refusal, while this one produced no status and blamed a *stream* for ending when no stream had started. The shape is the register's recurring one — a comment asserting the reachability of the branch beneath it, unmeasured, and wrong | **False** (the comment, and the message it justifies) | **Fixed** 2026-09-24 (25i) — `egress::stream` now sends `StreamEvent::from_egress_error(&e)` **before** returning, so the classification reaches the consumer instead of being dropped by `egress_port`'s spawned driver, which discards the driver's `Result`. `a_streaming_refusal_is_reported_as_a_refusal_not_as_headers_that_never_came` pins it: removing the send reddens that test **alone**, with the false sentence itself as the failure text. `StreamEvent::Error` also gained `denied: bool`, `#[serde(default, skip_serializing_if = "std::ops::Not::not")]` so the wire stays additive, which is what carries the kind across the IPC boundary rather than only across the port |

| **D50** | The Tests row of the status page: "**1182** Rust" | `docs/dev-book/09-status.md`, the Tests row under "Working and verified" | **Stale by 19 tests, measured 2026-09-25 while landing 26a.** `cargo test` answered lib **1201**, binary **5**, before 26a added anything. The row's itemisation stopped at increment **25d** (1182) and was never extended, so 25e–25k's 19 tests were invisible to the only page that states a total. **The row's own closing paragraph names this class and undercounts it**: "the documented test counts had drifted twice before this page existed" — this is the third, and unlike the first two it was not found by re-measuring but by needing the number. **Nothing can catch it mechanically**: `cargo test` prints its own counts, the gate asserts only that the run is green, and a total written in prose is not a link `check-doc-links` can resolve. **The defensible part:** the number was right when written and aged rather than erred, which is why this is one row and not a pattern of carelessness — but a count that ages silently is indistinguishable from one that was never checked | **Stale** | **Fixed** 2026-09-25 — headline now **1216**, itemisation extended to 26a. **25e–25i are folded rather than itemised, and the row says so**: their per-increment deltas were never recorded here and reconstructing them from memory would be a guess, so a wrong number with a citation is refused in favour of a right one with a stated gap. The deltas that are itemised are the ones measured in the increment they belong to: **+16** (25j), **+4** (25k), **+15** (26a). The headless run is stated alongside (**1122 → 1152**), because a count with no configuration is a claim without its scope — D28's lesson. **Re-opened and re-closed the same day, and that is the finding worth keeping:** the fix landed at 1216 while 26c–26i were still adding tests, so it was **stale before the session ended**. Measured 2026-09-25 at 26l: lib **1278**, headless **1214**, binary **5**, browser **108** — 26l's own share is **+8**, so **54 of the 1216 → 1278 delta were never itemised by anyone**. **A prose total cannot be kept current by fixing it once**; it ages with every increment, and no gate can see it, so the row now names the 54 it cannot break down rather than inventing one. This is D50's own closing sentence applied to D50. **Applied a third time on 2026-09-25:** increment 26q moved the row to lib **1286** / headless **1222** / binary **5**, and the row now names **1278 → 1286** as a stated gap rather than inventing a split — the same rule, one increment later. |

| **D51** | The §10 pure-HTTP decision's enabling premise, stated in the module that implements it: "`check_gateway_key` … so a caller needs the master key (or an app key) … **The UI holds the master key in memory for the session**" | `core/gateway_admin.rs` (module header), against all of `apps/desktop/src` measured 2026-09-25 while starting the TypeScript migration | **The last sentence is false, and the migration cannot begin until it is resolved — every `/admin/*` route built in 26c–26h is unreachable from the webview.** TypeScript is key-blind by construction (invariant 2): a Grep for `master` over `apps/desktop/src` returns **no variable holding a key**, and `store.ts` handles `secretRef` (`store.ts:43`, `:245`, `:565`) — a reference — never a secret. The UI's three master-key operations are all `invoke` calls that never return it: `gateway_key_copy` copies host-side to the clipboard, and `gateway_key_generate` / `gateway_key_revoke` return nothing (`Gateway.tsx:242-250`). The screen says so in the operator's own words: "Shown once on generation; reveal = copy to clipboard (**never shown here**)" (`:238`), and `Control.tsx:563` repeats it — "it is never rendered in this window". A Grep for `fetch(` over the whole tree returns **one** call, in `Assistant.tsx`; there is no HTTP client, because there has never been a credential to send. **So the decision and its routes are sound while its stated enabler does not exist**: 26c–26h built ~75 routes that answer 401 to the only client the decision was written for. **Why no gate caught it**: `cargo test` exercises the routes with `Bearer sk-aip-test` supplied by the harness, which is precisely the thing the UI cannot do — a test that supplies a credential cannot discover that its caller has none. **Open** — needs a decision, not a patch. Three options, in order of soundness: (1) **host-mediated session credential** — the host mints a short-lived, revocable local key at gateway start and hands it to the webview; this preserves invariant 2's *purpose* (TS never holds a **provider** credential) while giving it something to send; (2) **loopback-only auth exemption** for `/admin/*`, which is simple and wrong — any process on the machine could then admin the gateway; (3) keep admin on IPC, which concedes the pure-HTTP decision. Nothing was built around this: the migration was stopped at the client rather than producing 43 call sites that 401 | **False** | **Closed** 2026-09-25 — host-mediated session credential chosen (option 1); `core/ui_session.rs` mints `ak-ui`, `gateway-client.ts` caches it in `let` (never persisted), and the provider/key/memory/context/tools groups are migrated to `fetchAdmin`. ~12 `invoke` calls remain for app-owned UI state and commands with no HTTP route yet. |

| **D52** | Three claims in the headless plan that the code had already outgrown: *"The UI holds the master key in memory (it already does, for the one-shot reveal)"*; *"the gateway is on `http://127.0.0.1:8800`"* (twice — §5.2's CORS bullet and §10 decision 2's "the transition is seamless"); and *"~12 `invoke` calls remain for app-owned UI state and commands with no HTTP route"* | `docs/dev-book/10-headless-service.md:659` (§5.3), `:636` (§5.2), `:2428` (§10.2), `:3653` (§13 Next action) | **All three measured false on 2026-09-25 while landing 26l, and none of them is a file 26l edited.** **(1) The master-key claim was already known false** — D51 measured it, and 26i had *replaced the premise* with the `ak-ui` host-minted session credential; the paragraph asserting the old premise simply survived the fix, so the document simultaneously described the solution and the defect. **(2) 8800 is `aiproviderd`'s port, not the app's** — `gateway.rs:38` is `pub const DEFAULT_PORT: u16 = 8787`, and the same document explains the split at `:208-209` ("persisted setting first, then 8800 — **not** `DEFAULT_PORT`, which is 8787 and collides with AI Hub v2"). Two places described a single-port world that never shipped; a reader of §5.2 alone would dial a closed port. **(3) The migration count was off by 6×** — a node script over `apps/desktop/src` counts **73** distinct `invoke` commands and **39** `fetchAdmin` call sites over **30** `/admin/*` templates, against §10's own corrected target of ~75 HTTP / ~34 IPC. | **False** (all three) | **Fixed** 2026-09-25 (26m) — §5.2 names 8787 and says why the two ports differ; §5.3 states the `ak-ui` model and **quotes the sentence it replaces rather than silently overwriting it**; §10.2 reads the port from `gateway_status` instead of hardcoding one; §13 carries the measured 73 / 39 / 30 and the ~13 split (5 client-only, 8 needing a route). **The class is the point:** all three were correct when written and aged — D50's pattern, twice more, in the same file D50 lives in. Found by *needing* the numbers, exactly as D50 was. **And the sweep was itself incomplete:** 26n found a **fourth** instance of the same master-key premise — §10 decision 2's own *Authentication model* bullet, still reading "sending the master key with each request … held in UI memory for the session". That is D44's lesson arriving *inside the fix for that class*: **a fix's sweep covers the places the falsified claim appears, not the places the fix touched.** Recorded rather than quietly patched, because the second failure is the more instructive of the two |

| **D53** | D51's recorded claim, still standing verbatim at the location D51's own *Location* cell names: "**The UI holds the master key in memory for the session**" | `core/gateway_admin.rs`, module header — the exact file and region D51's Location cell cites — against `core/ui_session.rs` and `lib/gateway-client.ts`, measured 2026-09-25 while landing 26o | **D51 was closed as Fixed and the sentence it recorded was never edited, so the register held both the defect and its closure at the same address.** 26i built the fix (D51 option 1: `ak-ui`, cached in a TypeScript `let`, never persisted) and the *dev-book* was swept twice for the same premise — three claims in 26m, a fourth in 26n — but the module header of the file D51 names still read *"The UI holds the master key in memory for the session"*. That is contradicted by the module 26i added and by a Grep over `apps/desktop/src`, which returns no variable holding a key. **The class is new, and it is not D52's:** D52 is *a claim ages where nothing edits it*; this is *a closure does not sweep its own citation*. A register entry carries a Location cell precisely so a reader can go and check the claim — closing the entry retired the obligation to check it without removing the text. D51's closure cell compounds it: it still carries *"~12 `invoke` calls remain"*, the sentence D52 measured false by 6×. | **False** | **Fixed** 2026-09-25 (26o) — the header now states the `ak-ui` model, cites D51, and records that this is the **fifth** place the premise survived (three in 26m, one in 26n, this one — the first in source rather than documentation). **The rule it earns: a drift entry's closure must sweep the entry's own Location cell**, because that cell is a promise that the claim was checked *there*. D51's closure cell is left as written and annotated here rather than rewritten: 26n's lesson was that the fix is the interesting artefact, and this entry is the evidence that "Fixed" was true of the mechanism and false of the sentence. |

| **D54** | *"the 32 MB heap ceiling cannot be enforced in-process without the power to kill the host, so either the whole adapter runtime runs under a supervisor or that one limit does"* — and the same conclusion, unhedged, in two further places | `10-headless-service.md` **§10 decision 1** (the bullet carrying that sentence), **§8's risk-register row** for the adapter runtime, and `09-status.md`'s adapter-runtime spike row — cited by section rather than by line, because this entry's own fix edits two of those files and a line-numbered citation would be stale the moment it was written (D53). Against `.workbuddy-ai/spikes/js-engine/src/main.rs`, probes S6h/S6i/S6j, measured 2026-09-25 while running increment 26p | **S6i fences the fault and the process keeps working.** S6i is S6h's guest with `siglongjmp` armed — one variable — and after the `SIGSEGV` the faulting frame is abandoned and a *fresh* `Runtime` is created and evaluates `1+1`. S6h, the same guest with a handler and no jump, reports `si_addr = 0x20`, `si_code = 2`, faulting frame `build_backtrace` at `+0xCCC` — the instruction after `bl _JS_DefineProperty`, i.e. the `JS_DefinePropertyValue(…, JS_ATOM_stack, …)` that closes `build_backtrace` (`quickjs.c:6792`), reached from `JS_CallInternal`'s `exception:` label (`:17439`). So the crash is a **NULL dereference at `+0x20` inside QuickJS's own error-decoration path**, not the guest's allocation, and not a stack overflow (`0x20` cannot be a stack address). S6j validates the instrument rather than assuming it: a deliberate read of `0x1234` reports `si_addr = 0x1234`. **The source claim hedged, and the quotations did not.** §2.1.3 wrote *"It has not been run, and nothing here claims the fence works"* and made its recommendation contingent on a probe — honest, and correctly scoped. The three restatements dropped the hedge and asserted the conclusion as a property of the platform, which is how a hypothesis came to read as a fact for two increments. | **Half** — the `SIGSEGV` is real and reproducible 3/3 and remains the reason this boundary needs care; the *inference* drawn from it was never measured | **Fixed** 2026-09-25 (26p) — all four sites corrected, and **S6g added to the §2.1.3 table**, where it had been missing since 19b documented it only in its own section. **The rule it earns: a hedge does not survive being quoted.** A conditioned claim becomes an unconditional one the first time it is restated without its condition, and the restatements are where readers arrive. When a finding's conclusion is contingent on an experiment, the contingency belongs in the conclusion's own sentence, not only in the paragraph above it. |

| **D55** | The install path's success claim and the fixture that made it unfalsifiable: *"Permissions come along: `fs::copy` copies the mode, and the bundled binary is already executable. A `chmod` here would be a second spelling of a fact the source already carries"* — beside a test fixture documented as *"A source binary to install from, with the one property that matters: it exists"* | `apps/desktop/src-tauri/src/core/service.rs` — the comment above `fs::copy` (HEAD `:231-232`), `install`'s `Ok(())` (HEAD `:262`), and the `source()` fixture (HEAD `:399-404`) | **`install` returned `Ok(())` for a binary launchd could never exec, and the suite's own fixture is why it could not see it.** `std::fs::write` creates the fixture at `0644`, so all 15 tests installed a **non-executable** source: the fixture's comment named *existence* as "the one property that matters" while the property that decides whether the job runs is the **executable bit**, which it never set and never asserted. `fs::copy` faithfully preserved `0644`, `bootstrap` accepted the plist — it registers a job, it does not exec the program — `launchctl` reported the job *loaded*, and every spawn failed `EACCES`, which `KeepAlive` retries until launchd throttles the job. The operator's view is "installed". **Falsified before fixed:** with the fixture untouched, `install` on a `0644` source returned `Ok(())`; the new assertion `assert!(rec.calls().is_empty(), …)` panicked with `not an install: ()`. **The class is the register's own, one level down.** D47 and D48 were *comments* asserting the reachability of the branch beneath them; this is a *test fixture* asserting an invariant that is not the load-bearing one — and a fixture is worse, because its wrong claim is executed 15 times and reads as coverage. | **False** | **Fixed** 2026-09-25 (26q) — `verify_executable` runs immediately after `fs::copy` and **before any launchd state is touched**, so a non-executable source is refused with `not executable (mode 0644)` naming the path and the consequence, and `rec.calls()` is empty — nothing reached `launchctl`. The fixture now sets `0755` explicitly and the module's "permissions come along" claim is **asserted** rather than assumed; `non_executable_source()` supplies the hostile `0644` for `install_refuses_a_binary_launchd_could_never_exec`. **The rule it earns: a fixture's comment is a claim about the invariant the test depends on, and a wrong one makes the defect invisible from inside the suite written to catch it.** |

| **D56** | The dependency invariant's stated evidence, and the size figure it travelled with: *"`cargo tree --no-default-features --edges all \| grep -ci 'webkit\|wry\|gtk'` → **0**; the feature-less release binary is **380 KB smaller** (4,073,968 B vs 4,454,336 B, the difference being Tauri not linked)"* — and the same figures restated in Phase 6 step 2 and the 26a note | `09-status.md` (the dependency-half paragraph), `10-headless-service.md` Phase 6 step 2, and the 26a note in §11 — cited by section rather than line, because this entry's fix edits all three (D53) | **Two claims, failing in different ways. (1) The command cannot fail.** `grep -ci 'webkit\|wry\|gtk'` without `-E` is BSD **BRE**, where `\|` is **literal**: the pattern is the string `webkit\|wry\|gtk`, which appears in no tree. Run against the **default-features** tree — which holds **34** real matches (`wry`, `tauri-runtime-wry`, `wry feature "os-webview"` …) — it printed **0**. So the row's evidence for "the Tauri-free build pulls no WebKit" was a command returning 0 whether or not WebKit is present: the conclusion is true and the instrument is null. **(2) The size figure was wrong by 22×, against a build that no longer exists.** Re-measured 2026-09-25, same session, same profile, one variable: default features **8,603,760 B** (12 dylibs, **includes `WebKit.framework`**) vs `--no-default-features` **8,586,864 B** (8 dylibs, no WebKit) — a delta of **16,896 B**, not 380,368. **WebKit is linked dynamically, so it costs no file size**; the whole delta is Tauri's own Rust code, which is why the old figures were plausible before Phases 2–5 moved the engine into `aiproviderd` and Tauri's code was a large fraction of a much smaller binary. **The class is D50's — a measurement that aged — with D54's sting: the number was quoted into a decision, and the direction survived while the magnitude did not.** | **False** (both) | **Fixed** 2026-09-25 (26r) — all three sites corrected, and the invariant now rests on **two instruments that can fail**: `cargo tree` with `-iE` (**34** nodes vs **0**) and `otool -L` (`WebKit.framework` present vs absent). The decision step 2 actually needed is taken: bundle the Tauri-free build, **for the linkage, not for the 16.5 KB** — and `tauri build` cannot produce it, since it builds the default-features target. **The rule it earns: a command that returns 0 unconditionally is not evidence, and an absence claim needs an instrument that has been shown to report presence** — run it against the case where the thing *is* there. |

| **D57** | D56's own closing instruction, not executed: *"an absence claim needs an instrument that has been shown to report presence — run it against the case where the thing is there"* | `07-drift-register.md` D56 (its closing sentence), `09-status.md` and `10-headless-service.md`'s step 2 build step, measured 2026-09-25 while landing 26s | **The instrument D56 named (`otool -L` on the binary) was the *contrast* a check could be built on, but no check had been built, and the substitution step 2 needed was still open.** Two facts, one consequence. **(1)** The BRE command D56 retired still lived only in the register's text; the durable guard for "the bundled `aiproviderd` links no WebKit" did not exist, so a `tauri build` that overwrote the bundled binary with the WebKit-linked one would be **silent** — no gate read the bundle. **(2)** The build step 26r left open — producing the Tauri-free binary and substituting it into the bundle — was still to do, so even a correct gate would have been checking a bundle that had not yet been produced. **The class is new and is not D56's:** D56 was *an instrument that cannot fail* (a command returning 0 on any input); this is *an instrument that had not been pointed at the artefact* (a contrast with no check behind it). D56 earned the rule "run it against the case where the thing is there"; the rule was recorded and not executed until the artefact it was meant to guard was still in flux. | **False** (the guard was absent) | **Fixed** 2026-09-25 (26s) — `scripts/substitute-tauri-free-aiproviderd.sh` (a post-bundle step, since `tauri build` cannot produce the Tauri-free target) and `scripts/check-bundled-aiproviderd-links.sh` (`otool -L` + `grep -icE 'WebKit\|wry\|gtk'`, the ERE the register demanded — no backslashes; the BRE form is what D56 retired). The instrument's fail-capability was **proven rather than assumed**: pointed at the default-features binary it exits 2 and names the `WebKit.framework` line; pointed at the Tauri-free one it exits 0. **The rule it earns: a guard is not the discovery of an instrument, it is the instrument pointed at the artefact it guards** — D56 found the right test; 26s ran it, on the bundle, as a gate. |
| **D59** | 26o's own closing caveat, not heeded: *"A total without its method cannot be rechecked"* — yet the 26o figures (**65** `invoke` commands, **39** `/admin/*` templates, **7** still on `invoke`) were quoted as current through 26p, 26q, 26r, 26s and 26t, five increments after the measurement | `10-headless-service.md` §13 "next action" (the 65/39/7 block), §12 open-item list, the 26l and 26n notes, measured 2026-09-25 while landing 26u | **The tree had moved under the figures, and the register's caveat was the warning that should have stopped the propagation.** 26o wrote the 65/39 count *with its method* and then, correctly, declared it **cannot be rechecked** — which is a statement about the number's durability, not a licence to keep quoting it as current. The next five increments re-stated it (or the "7 still on `invoke`" it implied) without re-running the grep, while in the same span: task #8's `manifest_stage`/`manifests_history` extraction **landed** (the 26o "7 remaining" is no longer the live figure), the vitest suite **grew from 108 to 181**, and the two `/admin/settings/{…}` templates that look like "still on `invoke`" are in fact served by the generic `/admin/settings/{key}` route. Measured on 26t's tree: **~17** `invoke` commands remain (7 in `store.ts`, 10 across screens), **all** in the must-stay-on-IPC category; **23** `/admin/*` templates are used by the UI and every one has a Rust route. **The class is the register's own, one level down:** D56 retired *an instrument that cannot fail*; D57 retired *an instrument not pointed at the artefact*; this is *a measurement whose caveat was recorded and then ignored by the readers* — the number's own warning ("cannot be rechecked") is what the next reader should have treated as "re-check before re-quoting". | **False** (the figures were quoted as current after the tree moved) | **Fixed** 2026-09-25 (26u) — §13 and §12 corrected to the measured inventory (~17 `invoke`, 23 `/admin` templates, task #8 done, suite 181/181), with the re-check method written out so a reader can re-run each in one grep; task #8 closed. **The rule it earns: a figure that carries its own "cannot be rechecked" caveat is a claim about the *next* reader, not a reason to keep quoting it — the caveat is the instruction to re-measure, and the next increment should have measured.** |
| **D58** | The two-process port race, designed before it was measured: *"the launchd agent and the desktop app both bind the same persisted port; whichever binds second silently loses"* — a collision 26l made live and 26s declared unmeasurable because it "needs both processes up" | `10-headless-service.md` §12 open question + the 26s "leaves open" note, `core/gateway.rs` `probe_port`/`app_bind_decision`/`app_listener_action`, `tauri/app.rs` auto-start, measured 2026-09-25 while landing 26t | **The race is real, but the design needed no two processes to pin it.** D58's hazard — the *silent* second bind — is a property of the *decision*, not of two OS processes, and the decision is reachable without either: `probe_port` is a `connect_timeout` to loopback (a free port answers `ECONNREFUSED` in microseconds, a serving one accepts), and `app_listener_action` maps the result to `Some(port)` (delegate) or `None` (bind). **The falsifying probe is the one that does not need the agent:** `probe_port` on a *free* port must return `Err`; a function that answered `Ok` unconditionally would delegate on **every** launch and the app would never start its own gateway — green tests, no listener, invisible until the first HTTP call. **The class is new and is the inverse of D57:** D57 was *an instrument with no artefact*; this is *the artefact's decision, extracted so it can be falsified without the artefact's partner*. The asymmetry is load-bearing and is named where it lives — **only a confirmed "in use" is a reason not to bind; a broken probe must read as "free", because a dead agent and a dead app is the worse of the two states.** | **Open** (designed; end-to-end measurement pending) | **Landed** 2026-09-25 (26t) — the probe-and-delegate policy is in `core/gateway.rs` and wired into `app.rs`'s auto-start; the `launchd_live` harness measuring both processes up is the follow-up. **The rule it earns: a race between two processes is a property of the *decision* one of them makes, and that decision can be extracted and falsified alone — you do not need both up to prove the one that binds second would have lost.** |

| **D60** | Two CI mirrors and five dated docs still read "the keychain is the vault" — `ci.yml`'s `headless-service` job comments cite "no keychain approval" as the reason `--version` cannot flake; `ci-local.sh` and `release.yml` carry no `otool` step on the `checks` job; `MASTER_PROMPT.md` says keys live "in the OS keychain (via the Rust host's `keyring` crate)"; `AUDIT_REPORT.md` L6 says "key-deletion → keychain cleanup"; `DECISIONS.md` 2026-09-16 records the `keyring v3→v2` downgrade as a live decision | `.github/workflows/ci.yml` (headless job), `scripts/ci-local.sh`, `.github/workflows/release.yml`, `docs/MASTER_PROMPT.md`, `docs/AUDIT_REPORT.md`, `docs/DECISIONS.md`, measured 2026-09-26 while landing the vault replacement | **D53's class arriving in the vault change's own sweep: the fix's sweep covered where the claim *appears*, not where the fix *touched*.** The commit that removed `keyring` from `Cargo.toml` and rewrote `vault.rs` swept `09-status.md`, `10-headless-service.md` §13, and the `ci.yml` comment that named the keychain — but it did not sweep `MASTER_PROMPT.md`'s current-state §6 ("API keys are stored in the **OS keychain**"), the `DECISIONS.md` entry that records the v3→v2 downgrade as a *live* decision, `AUDIT_REPORT.md` L6, or the two CI mirrors that still say "no keychain approval" as the reason `--version` is safe (now false for the stronger reason that the vault is a file and `--version` never opens a store). **The class is D53's, not new: a closure does not sweep its own citations — except here the *fix* is the citation, not the entry, so the sweep is owed to the fix, not to a register row.** | **False** (the five docs read keychain-as-vault after the file vault landed) | **Fixed** 2026-09-26 — `MASTER_PROMPT.md` §6 and §7 now name the file-backed store and cite the DECISIONS.md entry; `ci.yml`'s `checks` job gained the two `otool` steps (substitute + gate) that `release.yml` already carried; `ci-local.sh` mirrors both; `DECISIONS.md` gained the 2026-09-26 decision entry that supersedes the 2026-09-16 one; `AUDIT_REPORT.md` L6 reworded to "key-deletion → vault cleanup". **The rule it reinforces: a mechanism change's doc sweep must cover every *current-state* doc, not just the ones it edits** — D53 made the same point for register closures; this is its first arrival on a live mechanism change. |

| **D61** | The login-item card's buttons update the card — each of the three `service_*` handlers ends `.then(() => refreshGateway())`, which reads as "re-read what this card shows" | `Control.tsx:247-253` (as written before 2026-09-26), where `refreshGateway` reads and merges only `gateway` and `spend`; `service` appears in neither its `Promise.all` nor its `setData` merge | **A button whose success is invisible is indistinguishable from one that did nothing.** `service_install` and `service_uninstall` both re-read a slice that did not contain the row they change, so clicking Install left the card reading "Not installed" until something else ran `load()` — a mount, or a `tick` bump that none of the service buttons performed. The tab's own clock was no help: it calls this same function, so the row was stale on every tick too, not merely after a click. The web-test spec could not see it because it *arranges* the state through `__webTest.serviceStatus` and then reads the copy — it never clicked a button, so the whole path from click to re-read had no test. | **False** (the card did not reflect a service action until an unrelated `load()` ran) | **Fixed** 2026-09-26 — `refreshGateway` now reads and merges `service` alongside `gateway` and `spend`, so every service button and every tick updates the row. Its doc-comment records why the service belongs in a poll at all: the pid arrives *after* `bootstrap` returns, so a single read taken straight after Start sees `loaded: true, pid: null` — the same shape a throttled restart leaves, which a card that could not tell them apart would report as stuck. Five new web-tests, three of which click a button and assert a call log — the path the old spec could not reach; the other two assert the copy for `loaded: true, pid: null` and that a refusal surfaces. |

| **D62** | Where a secret lives, as the app tells its user — "stored in your OS keychain" across five screens, and on Providers' first-run hero *"this app never writes them to disk"* | `Providers.tsx:196-197`, `:225-227`, `:455`; `Onboarding.tsx:7`, `:129`, `:600`, `:636`, `:645`; `Gateway.tsx:14`, `:242`; `Settings.tsx:302`; `lib/gateway-client.ts:51`; `ipc-client.ts:204`; `store.ts:638`; `web-test/shim.ts` (the in-memory secret map and its comments); `web-test/ui.spec.ts:240-250` — all measured 2026-09-26 while adding the service Start/Stop buttons | **D60's class one surface further out, and this instance is worse than the docs one because one clause is *inverted* rather than stale.** Providers' first-run hero told a new user *"Keys are stored in your OS keychain — this app never writes them to disk"*. After `e27503e` that second clause is not merely out of date, it is **backwards**: the vault *is* a file (`<data_dir>/.secrets.json`, mode 600). The rest name a mechanism the app no longer has but are not inverted. D60's own sweep covered *documents* — `MASTER_PROMPT.md`, `DECISIONS.md`, `AUDIT_REPORT.md`, two CI mirrors — and its rule was "cover every *current-state* doc, not just the ones it edits". What it did not consider is that the app's **own user-facing strings and its test names are current-state claims too**, and they were the largest surviving set: a reader meets the first-run hero long before they meet `MASTER_PROMPT.md`. | **False** (the UI described a removed mechanism; one clause of it inverted) | **Fixed** 2026-09-26 — every site re-worded to "a local secrets file (mode 600), not the database", the inverted clause **replaced rather than deleted** (it still carries the true half — nothing leaves the machine), the `ui.spec.ts` test renamed `keychain-resident` → `file-resident` with its assertion regex updated, and the harness's own `keychain` map renamed to `secrets` so the stand-in stops naming the retired mechanism. Verified by `grep -rni keychain apps/desktop/src apps/desktop/web-test` → **0**, with a control grep for `secrets` → 9 and 24 — because an absence claim needs an instrument proven to report presence (D56). **The rule it adds to D60's: a mechanism change's sweep must include the strings a user reads and the names a test asserts on. Those are claims, and they are the ones a reader arrives at first.** |

| **D63** | 26y's handover, described in its own note as a safe improvement: *"Start performs the handover … that instruction is now the button's own first step"* | `Control.tsx` `serviceAction` as 26y wrote it (`if (before) await before(); await action();`) and its `Start` call site, plus `10-headless-service.md` 26y's "Start performs the handover" paragraph; measured 2026-09-26 against a live operator report | **A compensating step with no compensation.** The handover is *destructive-first*: `gateway_disable` releases the port before `service_start` is attempted, because the service cannot bind while the app holds it. Nothing put the listener back when the start failed, so the failure path ended with the app gateway **off and** no service — the one outcome with nothing serving, strictly worse than either state Start was pressed from. 26y's note reasoned carefully about *why* the handover must happen and about not persisting `enabled: false`; it never considered what happens when the second half fails. **Found by the operator, not the suite:** the browser spec arranged the gateway-running state and asserted `["gateway_disable", "service_start"]` — an assertion that *stops at the failure*, so it could not see what followed it. **The class is D55's one level out:** D55 was a fixture asserting an invariant that was not load-bearing; here the *assertion* stopped exactly where the defect began, so the suite had a test for the step that worked and none for the step that did not. | **False** (Start could leave nothing serving) | **Fixed** 2026-09-26 (26aa) — `serviceAction` now takes `{ run, undo }` and runs the undo when `action` throws after `run` succeeded; `Start` undoes with `gateway_enable` (runtime-only, symmetric with `gateway_disable`) and **not** `toggle(true)`, which would persist an `enabled: true` the handover never changed. A failed undo is reported *alongside* the original error rather than replacing it. The new test asserts the full three-call log and was **falsified** by neutering the undo, which reddens it. |

| **D64** | `gateway_disable`'s own comment, as written and as still true: *"the UI's session credential dies with the gateway, so a token minted for one session cannot keep authenticating after Stop"* | `tauri/gateway_cmds.rs:292-297` (`gateway_disable` → `ui_session::revoke`), `lib/gateway-client.ts` (the module-level `_cached`, `uiSessionKey`, and `clearUiSession` — which has **no call sites**), `core/ui_session.rs`; measured 2026-09-26 against a live `401 invalid gateway key` on `POST /admin/api-keys` | **A revocation with no re-mint, on a path the UI had just started taking by itself.** The revoke was correct and deliberate; the other half was simply never wired. `uiSessionKey()` caches in a module-level `let` and returns it forever, and `clearUiSession()` — written for exactly this case, its comment reading *"Called on gateway stop so the next start mints a fresh one"* — is called by **nothing**: a Grep for it returns its own definition and the test fake and no third site. So 26y's handover, which made this a *single button press* rather than an operator's deliberate Stop, revoked the credential the webview was still presenting, and every `/admin/*` call answered 401 until the webview reloaded. Measured on the live machine rather than inferred: the vault held no `key:ak-ui`, and `gateway_keys` held only the operator's own key — the credential was gone from both halves while the client kept sending it. **The class is D63's mirror image:** D63 was a compensating step with no compensation; this is a *teardown step whose paired setup was never wired*, invisible for as long as the path was operator-initiated and rare, and made routine by the increment that automated it. | **False** (every admin call 401s after a handover) | **Fixed** 2026-09-26 (26ab) — `fetchAdmin` drops the cached credential and retries **once** on 401, re-minting against the new listener. Once, not in a loop: a second 401 is a real refusal, and re-minting against it would dress a refusal up as a retry. Four unit tests assert the retry's `Authorization` header differs (a retry that re-sent the same key would satisfy "two calls" while fixing nothing), that a second 401 is not retried, that a 500 is not retried, and that a healthy call mints once; **falsified** by disabling the retry, which reddens the two retry tests and leaves the other two green. |

### Notes on the entries

**D5 — closed.** The claim was corrected first, deliberately, and the script was aligned in a later pass once
the doc-link step was being added to both mirrors anyway. The audit now runs second in both, so a bad advisory
fails the local gate in seconds rather than after the whole suite has run.

That two-step sequence is the point worth keeping: a false *claim* and a divergent *script* were two separate
defects. Correcting the claim did not fix the script, and recording the residual in the register is what kept
the second one from being forgotten.

**D6 — the module map, closed as an addition.** `ARCHITECTURE.md` is a spec document with a dated provenance,
and §1.2 is a spec-era module map. Rewriting it to match the shipped app would erase the record of what was
planned, so a **shipped-screens table was appended below the map instead** — six new screens plus the one
rename, each with its relation to the original row stated explicitly.

Checking the map against `ScreenId` *before* writing that table corrected this entry's own wording. It had said
Activity was "absent from the map"; Activity in fact **replaced** `screen-usage`, which no longer has a file.
An absence and a rename are different facts, and a register is the wrong place to be approximately right.

**D8 — closed, and the closing taught something.** The prompt-cache measurement exists to accumulate
`cached_tokens` across real traffic. Migration 0015 had added the column; the installed bundle simply had not
been rebuilt since it landed, so the column was absent and the measurement could not record a single row.

Rebuilding was necessary but, on its own, **not sufficient** — which is the part worth keeping. A migration that
adds a column proves nothing about whether anything writes to it. So before calling D8 fixed, the whole producer
chain was traced, not just the schema:

```
manifest-interpreter.ts   reads prompt_tokens_details.cached_tokens (OpenAI) and Anthropic's cache blocks
  → model-router.ts:353   carries the value through as-is, `undefined` included
  → usage-ledger.ts:38    `cachedTokens?: number` — optional, never defaulted
  → store.ts:118          `e.cachedTokens ?? null` — `undefined` becomes SQL NULL, `0` stays `0`
  → persist.rs:440        INSERT … cached_tokens …
  → store.rs:830          ALTER TABLE ledger ADD COLUMN cached_tokens INTEGER (no default)
```

That chain was already complete. The gap was purely that the schema on disk sat one version behind the schema
in the source.

The measured state after the relaunch is exactly what the column was designed to produce: `schema_version = 15`,
`ledger` at 17 columns, **1530 rows preserved and every one of them `NULL`**. All 1530 predate the column, so
`NULL` is the correct value for them — and it is a different statement from "the providers reported zero cached
tokens". The open question is now empirical rather than structural: under real traffic, is `cached_tokens` mostly
`0` or mostly `NULL`? That needs traffic, not another rebuild.

**D10 — the docs understated the product.** This entry is unusual: the code is right, the UI is right, and
`DECISIONS.md` is right — only two prose claims lagged, and they lagged in the direction of making the app sound
*less* capable than it is. `ARCHITECTURE.md` told a reader the gateway dies when they close the window; in fact
background mode is on by default and the gateway keeps serving, which is the entire point of R1.

The failure mode is worth naming: **a doc describing a limitation you have already removed is as harmful as one
describing a feature you never built.** Both send the reader to the wrong conclusion, and this one would have
had a user keeping a window open for no reason.

**D14 and D15 — one class, twice.** Both are gates that could not fail on the thing they existed to catch, and
both were found by *running what the gate claims to run* rather than by reading the gate. D14 was a command that
never invoked the bundler; D15 was a command that invoked it with the wrong feature set. Neither was caught by
any other gate, and neither could have been caught by re-reading the workflow — D15 in particular could not
exist until the `app` feature did. The general form: **a gate is evidence only for the narrowest thing it
actually does**, and the way to establish what that is, is to change the thing it is meant to protect and watch
whether it goes red.

It surfaced while reading the lifecycle in `lib.rs` to assess the backlog — not by looking for drift. That is
the honest argument for keeping this register beside the code rather than trusting a periodic sweep.

It also settles two things:

- **The "main structural risk" framing survives, narrowed.** The gateway is still bound to the webview's
  *process*: a reloading or crashed renderer is `503`, and quitting the app takes it down. That is the real
  limitation, and it is now stated as such instead of being conflated with closing a window.
- **`hide_on_close` was untested — and my first reason for leaving it that way was wrong.** I wrote that
  pinning it "needs a window-lifecycle harness, not a unit assertion". That is half true, and the wrong half
  was the one I acted on. The end-to-end behaviour (close → hidden → still serving) does need a harness. The
  *default*, which is the part that decides whether the headline feature works, is a pure parse over one
  settings string — and this repository already had a house pattern for exactly that split.

  So the note recorded a limitation that did not exist. Fixed rather than re-recorded: the decision moved into
  `hide_on_close_from(raw: Option<&str>)` and a 12-input table test now pins it. Both arms were falsified
  before being trusted — flipping `None` fails the first case, flipping `unwrap_or` fails on `key absent`.
  Rust suite 472 → **473**, clippy clean.

  The lesson is the register's own: **a reason is a claim too.** "Cannot be tested" deserved the same evidence
  as "is not tested", and it did not get it.

**D24 — the same lesson one step further out, and it is the sharper form.** D10 corrected a claim about what the
code *does*. D24 corrects a claim about what the code *should be given* — and that claim turned out to describe
a construct the language rejects. Two things follow, and the second is the one worth carrying forward:

- **"Add a guard" is an answer to a question nobody asked.** The TypeScript needs `skipCompression` because its
  summarizer closes over the singleton it runs inside. Rust's borrow rules make that shape a compile error, so
  the correct work is a *seam* the router calls and does not own — which is already this crate's pattern for
  `AdapterFactory` and the usage callbacks. Reaching for the reference implementation's mechanism is the mistake;
  the reference's *problem* is what travels.
- **An unfalsifiable prescription survives every review.** No test, no compiler error and no Grep could have
  contradicted the sentence, because the code it warns about cannot exist. It read as careful — it named the
  failure mode and called the omission "invisible until the first summary recurses" — and that tone is exactly
  what kept it from being checked. The check was cheap: write the shape and see whether it compiles. It took one
  file and one `cargo build`.

  So the generalisation: **when a doc says "remember to add X", the first question is whether X's absence is
  even representable.** If it is not, the sentence is not a warning — it is a misdirection with a citation.

**D31 — a claim about a mechanism, written in the file that owns the mechanism.** Every other entry here is a
document drifting from the code. D31 is the code's own doc-comment drifting from the code *beside it*, which is
the harder case to catch for a plain reason: a design document is read as a description of the system, but a
comment on a function is read as a description *of that function* — by the same person who has just read the
function. The comment and the omission were written in the same edit, which is the ordinary way this happens:
the prose records the intent, the code records what got typed.

**And no step in the gate could fail on it.** `cargo fmt`, `cargo clippy --all-targets -- -D warnings` and all
971 tests were green, because none of them can ask whether a *sentence* is true. The mechanical check that did
find it is the cheapest one available: a Grep for the enforcing function's call sites, which returned two hits
and both of them were inside the test module that tests the function itself. That is the signature — **a limit
whose only caller is its own test is not enforced.**

So the generalisation: **a cap is enforced where its function is called, not where it is described.** When a
comment says a limit bites, grep for the call. The cost is one search; the alternative is what happened here,
which is that a containment rule the whole sandbox rests on was prose for an increment while the module's other
limits — the http budget, the stack, the memory cap — were all real.

**D32 — a reference ordering that is a discarded promise rather than a policy.** D31 was this port's own prose
disagreeing with its own code. D32 is the other direction: the defect is in the reference, and the port
deliberately does not reproduce it. The mechanism is worth naming because it will recur.

`void superseded.dispose?.()` reads as *dispose it first* and is in fact *dispose it whenever*, because the
promise is dropped on the floor. And `this.build(manifest)` sits **inside** the `set` call, so the map is
mutated only if the build succeeds — which means the failure path leaves the old adapter in the live map with
its teardown already started. Neither fact is visible from the line's shape; both are visible from its
*evaluation order*. **So the question a port must ask is not whether the reference's order is written down, but
whether it was chosen.** An async call nobody awaits is a race, and a race that a single-threaded event loop
usually wins becomes a determinism the moment the port's equivalent is synchronous — here, a `dispose` that
joins a thread.

What makes the divergence safe to take is that it is *separable*: restoring the reference's order reddens
exactly one test and leaves the other thirteen green, so the two orders are distinguished by one observation
rather than by a general argument. The TypeScript is left exactly as written, so the reference the port is
measured against does not move under it.

**D33 — the plan was the only evidence, and it was writing a cheque the code never cashed.** D31 was a
doc-comment drifting from the code beside it. D33 is one step further out again: the *claim* lives in a
planning document two directories away, and the code contains a faithful, tested implementation of the
*writing* half of it. Every artefact a reader would check agrees that the feature works — the map is
populated, its test is green, and the plan states the restore as a delivered row rather than as an
intention (`:311` is written in a present-tense "what ships" table).

What was missing is the half that produces a user-visible effect, and its absence is invisible for a
structural reason: **a write with no reader changes nothing observable, so no test can be written that
fails.** The mechanical check is the same one D31 used and costs one search — grep for the writer's name
and count the readers. Here it returns one hit, and that hit is the writer's own test. That is the
signature worth carrying: **if a map's only consumer is the test that fills it, the map is not a
feature.**

The port's divergence is narrow on purpose. It carries the per-request map (a `BTreeMap` field on the
returned value) and drops the global reverse map, because the two are not equivalent: the global map
would rewrite *any* TitleCase name, including one the client never sent, while the per-request map
records only what this request actually renamed. Choosing the stricter carrier is what makes the Phase 5c
restore safe to build rather than merely possible.

## The control that was added

D9 is the strongest argument in this file for a mechanical check. A claim about links was made, verified, and
still wrong — because the verification only looked at `.md` targets. The same is true of any property asserted
by hand.

**And it recurred the same day.** Adding [08 Flows](08-flows.md) introduced the identical bug — `../diagrams/`
written from a chapter two levels deep resolves to `docs/diagrams/`, which does not exist. Seven references,
caught only by re-running the scan. **Two instances in one day is the argument: a property you check by hand is
a property you will get wrong.**

**Partly built — 2026-09-22.** `scripts/build-dev-book.mjs` now enforces exactly this for the book itself:
it resolves every relative link *and image*, checks every in-page anchor against a real `id`, and fails on a
miss. Run it with `pnpm docs:book`. It is not a general `.md` linter — it validates the *rendered* book, so it
catches a broken reference inside `docs/dev-book/` and nowhere else.

Two further defects surfaced the moment it ran, both invisible to a by-eye check:

- **Colliding SVG ids.** Every diagram in `diagrams/` defines its own `<marker id="arrow">`. Inlining three of
  them into one page makes `marker-end="url(#arrow)"` resolve to whichever definition comes first, so the
  others render with the wrong arrowhead — silently. The generator now namespaces every id, and every
  `url(#…)` / `aria-labelledby` reference to it, per diagram.
- **A duplicate `id="next"`.** Seven chapters end with a `## Next` heading, and an unscoped slug gives all
  seven the same id. Heading ids are now scoped to their chapter, and disambiguated within it.

Neither is documentation drift. Both are the same lesson as D9 in a different medium: a property nobody checks
mechanically is a property that is already wrong.

**Now built — 2026-09-22.** `scripts/check-doc-links.mjs` resolves every relative link *and image* in every
markdown file in the repository, including non-`.md` targets and bare directories, and fails the gate on a
miss. It is wired into both mirrors, so this is the one entry in this file whose fix is mechanical rather than
editorial. Two details are worth keeping:

- **It strips fenced blocks and inline code spans before looking.** The memory logs and
  `PRODUCT_COMPLETION_PLAN.md` *quote* link syntax in order to discuss it, and those quotations are not
  references. A checker that reports them is a checker people learn to ignore.
- **It refuses to report success on an implausible scan** — fewer than 10 files or 20 links and it exits 1
  rather than print a confident zero. This repository has already produced wrong conclusions from searches
  that never reached the directory.

`scripts/build-dev-book.mjs` stays the stricter check for the book: it also validates in-page anchors against
generated ids and refuses duplicate ids, which the repo-wide check cannot see.

## Adding an entry

```
| **D<n>** | the claim, quoted | file:line | what you measured, and with what | False / Half / Stale / Correct | Open / Fixed |
```

Three rules for an entry:

1. **Quote the claim.** A paraphrase cannot be verified later.
2. **Carry the evidence, not the reasoning.** "`Cargo.toml:33` says `keyring = "2"`" is checkable. "The docs
   are out of date" is not.
3. **`Correct` is a valid verdict.** D7 is in the register precisely because it was *checked and found fine* —
   recording that stops the next person spending the same effort.
