# 2026-09-26

## v1.2.0 tag (commit `831c269`, tag `v1.2.0`, pushed)

Six manifests bumped, CHANGELOG `[1.2.0]` section covering:
- OS keychain → file-backed `.secrets.json` (one-time migration: re-mint keys, re-enter provider keys)
- Five `aiproviderd` subcommands (`install`, `uninstall`, `status`, `self-install`, `mint`)
- Phase 6 end-to-end: Tauri-free binary in the `.app` bundle, CI mirror otool gates, `launchd_live` harness for D58

**D58 note**: the `launchd_live` harness is the D58 end-to-end measurement. It ran from Terminal.app on 2026-09-25 (the label collision fixed in `387d3a8` made the two tests coexist). Tushu confirmed it passed — test 1 green, test 2 green via the SKIP path. A re-run from a fresh Terminal.app session that shows `action = Some(8800)` rather than `SKIP: no Aqua session` closes D58 fully. The test remains `#[ignore]`d by design (no Aqua session in CI).

## Increment 26w — Phase 6 step 2, verified end-to-end against a fresh `tauri build`

**Context**: the 26s flow (substitute + gate) had never run on a fresh bundle. The commit
`e27503e` (vault rewrite, subcommands) made the bundled binary stale.

**Verification** (2026-09-26, 06:39–06:50 UTC):
- `pnpm tauri build`: `.app` built in 4m08s; DMG step failed on `hdiutil` (known sandbox limitation,
  recorded in `09-status.md` — not a regression).
- `bash scripts/substitute-tauri-free-aiproviderd.sh`:
  - Tauri-free `aiproviderd`: WebKit/wry/gtk match count **0**.
  - Default-features `aiproviderd`: match count **1** (the D56 contrast — the instrument reports presence).
  - Post-substitution bundle: match count **0**. Gate passed.
- Ran the substituted binary directly from `Contents/MacOS/` with `mint`: it rotated the master key
  and wrote `~/Library/Application Support/dev.aiprovider.router/.secrets.json` — proving the subcommands
  work in the Tauri-free target (the one the bundler would otherwise ship with WebKit linked).

**CI mirrors gained the two `otool` steps they were missing**: `ci.yml`'s `checks` job and
`ci-local.sh` both run `tauri build --bundles app` but neither ran the substitute or the gate.
Both now run `scripts/substitute-tauri-free-aiproviderd.sh` then
`scripts/check-bundled-aiproviderd-links.sh` after the `tauri build` step, gated on `otool`
(macOS-only, matching the substitute script's own guard). `release.yml` already carried both.

**D60 — the vault sweep the register owed**: `MASTER_PROMPT.md` §6/§7 still read "OS keychain",
`DECISIONS.md` 2026-09-16 recorded the `keyring v3→v2` downgrade as a live decision, and
`ci.yml`'s `headless-service` job cited "no keychain approval" as the reason `--version` is safe.
All corrected; `DECISIONS.md` gained the 2026-09-26 entry that supersedes the 2026-09-16 one.
`AUDIT_REPORT.md` L6 reworded to "key-deletion → vault cleanup".

**Gates**: `cargo check` clean; `check-doc-links` 53/126; `docs:book` 216 ids. No test counts
change — this increment adds two CI steps and one register entry.

## Increment 27b — aiproviderd subcommands: install / uninstall / status

**Context**: launching the headless agent required a manual plist + copy + `launchctl bootstrap`
dance from Terminal.app. The sandbox blocks `launchctl` because it needs the user's Aqua session.

**Changes**:
- Added `aiproviderd install`, `aiproviderd uninstall`, `aiproviderd status` subcommands in
  `src/bin/aiproviderd.rs`. Each calls `service::install`/`uninstall`/`status` internally.
- `install` copies the running binary to `~/Library/Application Support/dev.aiprovider.router/bin/aiproviderd`,
  writes the plist with `AIP_DATA_DIR` set, and runs `launchctl bootstrap gui/$(uid)`.
- No keychain prompt, no manual plist editing, no Terminal.app. Run `aiproviderd install` from
  any shell.

**Note**: the subcommand dispatch happens before `#[tokio::main]`'s store setup, so they work
with `--no-default-features` (no Tauri, no keyring, no store).

## Increment 27a — keychain removed; secrets moved to `.secrets.json`

**Context**: Tushu found the keychain prompt annoying on every rebuild. Decision: replace the OS
keychain with a file-backed store in the app data dir. No keyring dependency, no code-signing
dependency, no SecurityAgent prompt.

**Changes**:
- `vault.rs` rewritten: `keyring::Entry` → JSON file (`<data_dir>/.secrets.json`, mode 600).
  The file is written atomically (tmp + rename) to prevent partial writes.
- `set_data_dir(&Path)` exported; `aiproviderd` calls it before opening the store.
  `tauri/app.rs` calls it right after resolving `app_data_dir`, before `Store::open`.
- `keyring` removed from `Cargo.toml`.
- 503 message in `gateway.rs` updated: "the secrets file could not be read" instead of
  "the OS keychain did not respond; approve the keychain prompt".

**Migration**: existing keychain items are abandoned, not migrated. The master key is re-minted
on the next `ensure_master_key` call; provider keys need one re-entry in the UI. Acceptable for
a local-only tool in development.

## Increment 26z — launchd_live label collision found on third Aqua run

**Context**: the second Terminal.app run (after `ee435d3`) showed test 1 green
(`marker written = true`) but test 2 green via the `SKIP: no Aqua session`
early-return path — it never installed, never probed port 8800, never ran
`app_listener_action`. The exit code was 0 but the real assertions never fired.

**Root cause**: both tests called `launchctl bootstrap gui/501` under the same
label (`dev.aiprovider.router`) in the same gui domain. Whichever test registered
its plist second got I/O error 5 — the *identical* error code a genuine
no-Aqua-session failure produces. The `no_aqua_session` check matches on that
message, so test 2's `bootstrap` rejection (label already loaded) was
misclassified as "no Aqua session" and the test SKIPped.

**Fix** (commit `387d3a8`):
- Added a `label: String` field to `Paths` in `service.rs`, defaulting to
  `LABEL` ("dev.aiprovider.router"). Production callers are unaffected.
- `render_plist`, `install`, `uninstall`, and `status` now read `p.label`
  instead of the hardcoded constant.
- Test 1 uses `service::LABEL`; test 2 sets
  `paths.label = "dev.aiprovider.router-test"` so the two jobs coexist.
- `clean_stale` now takes the label explicitly.

**Gates**: 1230 lib tests pass, clippy clean, fmt clean, `aiproviderd`
binary builds.

## 26z green run (02:53)

Both `launchd_live` tests pass on a real Terminal.app run:
- Test 1: `loaded = true, pid = Some(13707), marker written = true`
- Test 2: `probe: Ok(())`, `app_listener_action: Some(8800)`,
  stdout: `aiproviderd 1.0.0 listening on 127.0.0.1:8800`

Task #4 (the human Aqua-session harness run) is now complete. The Phase 6
step 4 delegation (26t) is verified against a live listener.

## Second live run from Terminal.app (12:50, against v1.2.0 / `831c269`)

Re-ran `cargo test --test launchd_live -- --ignored --nocapture` in a fresh
Terminal.app session, against the **1.2.0** build (the 26z run was on 1.0.0).
True pass, not a skip — neither test printed `SKIP: no Aqua session`:
- Test 1 `install_produces_a_job_launchd_actually_runs`: `loaded = true,
  pid = Some(18817), marker written = true`; `uninstall: Ok(())`;
  `after uninstall: plist_present: false, loaded: false, pid: None`.
- Test 2 `agent_serves_health_and_app_delegates`: `probe: Ok(())`,
  `app_listener_action: Some(8800)` (the 26t delegation pinned live),
  `loaded = true, pid = Some(18823)`; agent stdout:
  `aiproviderd 1.2.0 listening on 127.0.0.1:8800`; clean uninstall.

Host-side re-verification: both scratch `LaunchAgents` dirs empty, and
`launchctl print gui/501` reports `Could not find service` for both labels —
the test's own uninstall left no stale job. 26t delegation confirmed against
a live 1.2.0 agent on 2026-09-26 (second independent run).

## Final gate run + clippy 1.98 fix (12:56)

Stamped the v1.2.0 release. All four gates green:
- `key-leak-grep`: OK
- `check-doc-links`: 53 files, 126 links, all resolve
- `docs:book`: 12 chapters, book.html built
- `cargo clippy --all-targets -D warnings`: clean **after a fix**
- `cargo clippy --no-default-features --bin aiproviderd -D warnings`: clean
- `cargo test --lib`: 1298 passed

**The clippy fix (5 sites, clippy 1.98 / rustc floating):** clippy 0.1.98 (2026-09-01)
introduced `io_other_error` and `get_first`/`redundant_closure` firings on pre-existing code —
the "a -D warnings gate on floating stable goes red with no code change" case from the working
memory. Fixed rather than `#[allow]`'d:
- `core/vault.rs:124`: `std::io::Error::new(std::io::ErrorKind::Other, e)` → `std::io::Error::other(e)` (io_other_error)
- `bin/aiproviderd.rs` x3 (install/uninstall/status): `.map(|uid| service::domain(uid))` → `.map(service::domain)` (redundant_closure)
- `bin/aiproviderd.rs` main: `args.get(0).map(|s| s.as_str())` → `args.first().map(String::as_str)` (get_first)
No semantic change. fmt applied. The two aiproviderd lints only surfaced because `--all-targets`
compiles the `--no-default-features` binary target, which the earlier green default-features pass
hadn't touched.

## Push + retag (13:08)

Pushed `main` (831c269..16b18cc) and force-retagged `v1.2.0` from `831c269` to `16b18cc`
so the release tag includes the clippy 1.98 fix + Cargo.lock sync. Verified via
`git ls-remote --tags origin v1.2.0` → `refs/tags/v1.2.0` at `16b18cc`, matching local
`v1.2.0` and `origin/main`. Repo `github.com/begumporshi-alt/ai-provider-ide`.
Release fully stamped and published.

## Increment 26y — the login-item service gets Start/Stop in the UI (14:45)

**The request.** "i want to run it from the ui. we should have a feature to run it from the ui."
26b shipped Install/Remove with nothing between: the state `plistPresent: true, loaded: false`
rendered "Installed, not running" with only **Remove** beside it, so bringing the service up still
meant `aiproviderd install` from Terminal — the command 26b existed to make unnecessary.

**What landed.**
- `core/service.rs`: `start()` = `bootstrap` when launchd does not hold the job, `kickstart -k` when
  it does (`bootstrap` refuses a held label); `stop()` = `bootout`, **not** `kill`, because
  `KeepAlive` resurrects a killed process. +6 tests.
- `tauri/service_cmds.rs` + `commands.rs`: `service_start` / `service_stop` registered.
- `store.ts`, `Control.tsx`: Start/Stop buttons; `up = loaded && pid !== null` (was `loaded`, which
  is also the shape a throttled `KeepAlive` leaves — a down gateway read as "Running"); Start issues
  `gateway_disable` **before** `service_start`.
- `shim.ts` + `service-status.spec.ts` 4 → 9 tests.

**Why `gateway_disable` and not `toggle(false)`:** `toggle` also persists `enabled: false`, recording
that the operator switched the gateway off when what they did was hand over the port. Leaving the
preference alone means the next launch restores the app gateway, finds the agent on the port, and
delegates (26t).

**D61** — every `service_*` handler ended `.then(() => refreshGateway())`, but `refreshGateway`
read/merged only `gateway` and `spend`. Clicking Install left the card reading "Not installed" until
an unrelated `load()` ran, and the tab's own tick used the same function, so the row was stale on
every tick too. Fixed by merging `service` into the read.

**D62** — the browser suite put `gateway: the master key is keychain-resident and never rendered` on
screen: a **test name** asserting a mechanism `e27503e` removed. Following it found 11 user-facing
strings across five screens still naming the OS keychain, and Providers' first-run hero *"this app
never writes them to disk"* was **inverted**, not stale — the vault *is* a file. D60 had swept the
*documents* for exactly this and left the app's own copy and test names untouched. All re-worded;
`ui.spec.ts` test renamed `keychain-resident` → `file-resident`; the harness's `keychain` map
renamed `secrets`.

**Three defects found in my own docs, all by measurement rather than reading.**
1. `10-headless-service.md` and D61's row both said "four new web-tests". The spec went 4 → 9 = **+5**,
   and comparing test *names* against HEAD showed two of the four originals were **replaced in
   place**, not added. Corrected to five, and the wiring claim narrowed (three are wiring, two are
   state/copy).
2. `09-status.md` claimed "26q–26v moved both" totals. Tracing `test(` counts across
   `9633d5c` → `544a03f` showed the spec bytes are **identical** from 26q to HEAD — the browser
   figure did not move in that span; only Rust did (1286 → 1298 → 1304).
3. `book.html` still carried the old "Four new web-tests" text — confirming it is generated, not
   source.

**A gate defect found while running them.** `key-leak-grep` took **4m19s**; `check-ts-version`
**4m17s** — finishing 2ms apart despite being unrelated scripts, which argued for contention rather
than file-walking. Isolated A/B, one variable: the script's **second** grep lacks the
`--exclude-dir=target` its first grep has, so it walks the cargo build tree — **40s vs 1s** on a
built tree. Fixed; the script now runs in 1s. The cost is invisible until someone runs a
`tauri build`.

**Instrument note: `timeout` does not exist on this machine** (`rc=127`). My first "bounded to 45s"
probe exited 127 without running the grep at all — a probe that measured nothing while looking like
a clean result. Re-ran without it.

**Environment trap, again.** Playwright's `webServer` probe needs `HTTP_PROXY`/`http_proxy`/
`HTTPS_PROXY`/`https_proxy` **unset**, or the readiness probe to `127.0.0.1:1430/web-test/` goes
through the sandbox proxy and 404s. And the suite's teardown trips
`[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED]` on `test-results` (2470 files vs threshold 50) —
run `pnpm web-test:clean` first, which *moves* the dir to `/tmp`.

**Measured totals (2026-09-26):** Rust lib **1304**, browser **113**, vitest **181**.

## Increment 26aa — Start's handover gets an undo, and the EIO that is not a defect (14:55)

**Naming note:** this first went in as "26z", which was already taken by the earlier
`launchd_live` label-collision entry above. Increment labels live in **two** places — this log and
`docs/dev-book/` — and the check for a free one has to cover both.

**Found by the operator in the first minute of real use.** Clicking **Start** with the app gateway
running turned the gateway off and *then* failed:

```
launchctl bootstrap gui/501 /Users/…/dev.aiprovider.router.plist failed (5): Bootstrap failed: 5: Input/output error
```

leaving the app gateway **off and** no service — nothing serving, worse than either state Start was
pressed from.

**The real defect (D63).** 26y's handover is destructive-first by necessity — the service cannot bind
while the app holds the port — but it shipped with **no undo**. `serviceAction` now takes
`{ run, undo }` and runs the undo when the action throws *after* `run` succeeded. Start undoes with
`gateway_enable` (runtime-only, symmetric with `gateway_disable`) and **not** `toggle(true)`, which
would persist an `enabled: true` the handover never changed. A failed undo is reported *alongside*
the original error rather than replacing it.

**Why the suite missed it:** the spec asserted `["gateway_disable", "service_start"]` — an assertion
that **stops at the failure**, so it could not see what followed. The class is D55's one level out:
D55 was a fixture asserting a non-load-bearing invariant; here the *assertion itself* ended exactly
where the defect began. New test asserts the full three-call log, and was **falsified** by neutering
the undo (which reddens it).

**The EIO is not a defect — three measurements, in this order:**
1. A **trivial** plist (`/usr/bin/true`, `plutil -lint: OK`, sharing nothing with the app) bootstrapped
   into the same `gui/501` fails **identically** → rules out the app's plist, its binary and its code
   in one step.
2. The project's own `launchd_live` harness, run from this shell, prints
   `SKIP: no Aqua session in this process` with the identical EIO — and the same two tests **pass**
   from Terminal.app (the 12:50 run).
3. So the domain refuses *every* bootstrap from a process outside a real GUI session. The app under
   test had been started by me from an agent shell (`pnpm dev`), so it inherited exactly that.
   **Operator's fix: launch the app normally.** Nothing to repair in `service.rs`.

**Reusable diagnostic order:** reproduce with a control that shares nothing with the suspect, then
hand the question to an instrument that already distinguishes the two cases — rather than reasoning
about which cause is likelier.

**Also:** `pnpm dev` needs `PATH="$HOME/.cargo/bin:$PATH"`; without it `cargo metadata` fails with
"No such file or directory" and tauri exits immediately.

**Measured:** full browser suite **114 passed / 0 failed** (113 + the new rollback test). The earlier
112/1 was `ui.spec.ts:87` dying in `browserContext.close` on the sandbox file broker — it passes in
isolation *and* on the next full run, so it was a teardown flake, not the product. Rust unchanged at
1304.

## Increment 26ab — the handover revoked the UI's credential, and nothing minted it back (15:20)

**Live symptom:** after a successful Start, every admin call from the app answered
`401 {"code":"invalid_api_key","message":"invalid gateway key"}` — e.g. `POST /admin/api-keys`.

**Diagnosis, each step measured rather than inferred:**
1. `lsof` on the port → the listener was **`aiproviderd`** (the agent), not the app. So Start had
   actually **worked** — launched from the operator's own session, exactly as 26aa concluded it must
   be. The 401 was the app's *own* admin calls being rejected by the agent.
2. The secrets file held `masterkey` and two `key:` entries but **no `key:ak-ui`**.
3. `gateway_keys` held **only** `ak-fc85350a2fb1dc67` ("Work buddy") — no `ak-ui` row.
4. `gateway_disable` calls `ui_session::revoke` (`gateway_cmds.rs:296`), and the handover calls
   `gateway_disable`.

**Mechanism.** `uiSessionKey()` caches the credential in a module-level `let` and returns it for the
life of the webview. `clearUiSession()` — whose own comment reads *"Called on gateway stop so the
next start mints a fresh one"* — had **zero call sites** (Grep returns its definition and the test
fake, nothing else). So the credential was revoked host-side, correctly and by design (D51), while
the client went on presenting it. The revoke was never wrong; the re-mint was never wired.

**Why 26y is what exposed it.** `gateway_disable` was previously reachable only by the operator
deliberately switching the Gateway off — rare, and followed by a switch back on that also reads as a
fresh start. 26y made it the **first half of one button press**, so the ordinary act of bringing the
service up began silently invalidating the UI's own credential. D64, and it is **D63's mirror
image**: D63 was a compensating step with no compensation; D64 is a teardown step whose paired setup
was never wired.

**Fix.** `fetchAdmin` retries **once** on a 401 after dropping the cached credential, re-minting
against whichever process now holds the port — the point of `ak-ui` being an ordinary app key. Not a
loop: a second 401 is a real refusal, and re-minting against it would dress a refusal up as a retry.
A non-401 failure is not retried. The tests assert the retry's `Authorization` header **differs**
from the first attempt's — a retry that re-sent the same stale key would satisfy a call count while
fixing nothing — and were **falsified** by disabling the retry, which reddens exactly the two retry
tests.

**Instrument note (again).** `grep 'a\|b'` in bash returned a **false zero** while searching for
`clearUiSession`; BRE `\|` alternation matches nothing. The Grep tool found it at once. The working
memory already records this trap, and it still cost a step — the lesson is that the *first* search
for an absence claim must be the instrument that is known to report presence.

**Measured:** vitest **185** (181 + 4). Rust 1304, browser 114 unchanged.

## Increment 26ac — the 429 the retry didn't cover, and the boot screen's destructive advice (15:30)

**Live symptoms (three, in sequence).** After 26ab's 401 fix: `POST /admin/api-keys` → 401 (resolved
by 26ab). Then on next boot: `GET /admin/api-keys` → **429 too many failed auth attempts — backing
off**, and the boot screen displayed its unconditional *"restore from the newest dated backup"* line
for a transient rate limit.

**Diagnosis.** The 429 is a **consequence of the 401 storm**, not an independent fault. The
brute-force backoff is checked *after* the key match (`core/gateway.rs:1487`), so a valid credential
never reaches it — pinned by `r4_valid_key_not_throttled_by_another_callers_failures`. A 429
therefore means the credential was *still* bad. 26ab's retry covered 401 but **not** 429, so the
first poll after the re-mint presented the new key, the server matched it and cleared the window —
but only if the retry was triggered by the *first* 429. Without 429 in the retry condition, the app
locked itself out for the backoff duration (caps at 32 s).

**Fix.** Two parts:
1. `gateway-client.ts:116`: retry condition extended from `res.status === 401` to
   `(res.status === 401 || res.status === 429)`.
2. `boot-failure.ts` (new): `bootFailureHint(error)` chooses advice from the error string —
   429 → "reload, the backoff clears by itself"; 401 → "reload mints a fresh credential";
   5xx → "check the log"; no listener → "the gateway did not answer"; unknown → backup advice,
   now **hedged** rather than asserted. Wired into `App.tsx:77`.

**Tests.** Two new in `gateway-client.test.ts` (429 retry + second-429 no-retry), five in
`boot-failure.test.ts` (one per branch + hedged fallback). 429 branch **falsified**: disabling it
reddened both 429 tests, 401 and 500 tests stayed green.

**Docs.** D65 added to the drift register; 26ac increment section added to `10-headless-service.md`;
`09-status.md` Tests row updated (464 → 471 TS unit; desktop 185 → 192).

**Measured:** vitest **192** (185 + 7). Typecheck clean. key-leak-grep OK. doc-links 126 resolved.
Book rebuilt (220 ids). Rust 1304, browser 114 unchanged.

## Increment 26ad — the launchd error now says what it means (16:00)

Pays the debt 26aa filed: *"the card surfaces `Bootstrap failed: 5: Input/output error` verbatim."*

**Two measurements changed the shape of the fix.**
1. **"Map status 5" is the wrong key.** launchctl *exits* 5, and launchd separately embeds **its own**
   status code inside the message (`Bootstrap failed: **5**: …`). The same number appears twice meaning
   two different things → key on the **pair** (exit status **and** message text), not the number.
2. **The verbatim problem was never confined to `bootstrap`.** Live `launchctl bootout gui/501/<label>`
   of a job launchd doesn't hold → exit **3** `No such process`, not 5. A mapping keyed on 5 alone would
   have left `stop`'s most common failure just as opaque.

**A fixture was pinning a shape launchctl cannot produce.** `a_stop_launchd_refuses_…` used
`(5, "Bootout failed: 5: Input/output error")` — copied from the `bootstrap` fixture above it. Green
against a shape the tool cannot emit; it could not have caught *any* real `bootout` failure. D61's
instrument class in a new place: not "an instrument that cannot report presence" but "a fixture
asserting a property of a fabricated input".

**Design.** One `describe_failure(what, out)` at all three sites (`service.rs:351/419/444`). Note is
**appended, never substituted** (a wrong cause is discountable; a dropped raw message is not) and
**hedged** — "Most common cause: this process has no GUI session (Aqua)" — because the cause was
established by *excluding* alternatives (trivial plist failed identically; `launchd_live` printed
`SKIP: no Aqua session`), not by observing a success under it.

**Falsification, both directions.** Disabling the note reddened the 3 positive assertions and left the
2 negative ones **green** — which is exactly why the negative tests needed their own probe: **an absence
assertion passes trivially when the thing is absent for the wrong reason.** Making the note unconditional
reddened both. **My first attempt at that probe was worthless**: it returned `"PROBE"`, which does not
contain the substrings the negative tests assert on, so it could not have failed them. Lesson:
**a probe must be built to fail the assertion it targets**, not merely to differ from the correct code.

**A defect of my own, found by the sweep.** The D65 row I added earlier was **split across two lines** —
line 147 ended complete and line 149 was an orphaned duplicate fragment (` | **False** (a 429 boot
failure…`) with no D-id, rendering as a broken table row. My earlier "verify" greps matched the wrong
copy. Removed it in the same edit that inserted D66.

**Measured:** Rust lib **1304 → 1306**, all green. typecheck / fmt / clippy clean. doc-links 126.
Book 221 ids. Docs: D66, 26ad section, `09-status.md` Rust row, and 26aa's "what it still owes"
paragraph rewritten (it was present-tense and my change falsified it).
