# 2026-09-22

## The repo went public — pushed after a leak scan

59 files / 6 commits pushed to `origin/main` (`4b65e5c..4381212`) on
`github.com/begumporshi-alt/ai-provider-ide`, which the user has made **public**.

Because public exposure reaches everything already on `origin/main` — not just the new commits — I scanned
before pushing rather than after:

- `pnpm key-leak-grep` (a gate step) → OK.
- Secret-shaped strings in tracked files: all synthetic fixtures. `AKIAIOSFODNN7EXAMPLE` is AWS's documented
  example key; the rest are `sk-abcdef…` test strings in `capture.rs`, `onboarding.test.ts` and
  `adapter-generator.test.ts`.
- Emails: only `your@email.com`, `pw@cdn.example`, and `128x128@2x.png` (an icon filename).
- No `.env`, `.pem`, `.key` or credential files tracked; no `target/`, `node_modules/`, `dist/` or
  `test-results/` tracked (262 tracked files total).

**One residual, not fixed:** `/Users/tushershikder/...` and `~/Library/Application Support/...` paths appear in
six tracked files (the memory docs, `context_scope.rs`, `assistant-stream.test.ts`, `shim.ts`). That publishes
the macOS username. Low severity, but it is the only personal identifier in the repo.

Recorded in `MEMORY.md` as a standing rule, bought by compressing the four-shapes rule to a pointer (the file
is at its 8,000-char injection ceiling: **7,982 bytes / 39 rules**, 18 bytes headroom).

## Build and install (03:37–03:43)

Built and installed the macOS app on the freshly public repo's tree, following the recorded procedure in
REFERENCE.md §Verifying on an installed build.

`./node_modules/.bin/tauri build --bundles app` → **exit 0 in 2m 36s**, signed with
`AI-Provider IDE Dev Signing` (`Identifier=dev.aiprovider.router`, TeamIdentifier not set — expected for the
local dev identity; notarization skipped, no APPLE_ID). Bundle at
`src-tauri/target/release/bundle/macos/AI-Provider Router.app`.

Installed by moving the existing `/Applications` app to `/tmp/old-app-<ts>` (never `rm -rf`) and `cp -R`.
Verified the installed binary is **byte-identical** to the bundle (shasum-256, 11,409,344 bytes) — so the
app in `/Applications` is provably this build, not a stale copy.

Smoke: `open -a` → pid alive; `gateway.log` shows `startup: setup complete`, `enabled on port 8800`,
`auto-restore: restored on port 8800`, `workbuddy sync: 11 published`; curl → **401** (listener up, auth
enforced).

**Two recorded claims measured and corrected:**
1. The "mandatory" `mv dist /tmp/...` before `tauri build` **is no longer needed** — `build:clean` does it
   inside `build`, which is what `beforeBuildCommand` runs. Proven by building with a 17-file `dist` present
   and not touching it.
2. The verification curl used port **8787**, which has no listener — the live gateway is **8800**
   (`settings.gateway`; 8787 is only `DEFAULT_PORT` in code). And `--noproxy '*'` alone is **not** enough:
   with the proxy vars still exported the probe returns 000. Unsetting them turns it into 401. A 000 here is
   a proxy artefact, not a dead gateway — worth remembering, because 000 looks exactly like a crash.

## The frontend actually shipped (checked, not assumed)

The gate never bundles a Tauri app, so "green gate" says nothing about what is in `/Applications`. Followed
the recorded method (§Checking that a webview change actually shipped): **do not grep the `.app`** — the
frontend is embedded and compressed, so plaintext is absent — grep the `dist` that got embedded, and always
pair the probe with a **control string from the same file**.

`dist` was rebuilt at 03:39 by `beforeBuildCommand` (so `build:clean` really did move the stale one aside —
independent confirmation of correction 1 above), then compiled into the 03:41 bundle.

| String | Where | Meaning |
|---|---|---|
| `No repair is running in this session` | `main-*.js` | shape 4's card copy — did not exist before today |
| `could not be recorded` | `main-*.js` | `TrailWriteWarning` |
| `unrecorded` | `main-*.js`, `store-*.js` | the ending marker |
| `Drift suspected` (**control**) | `main-*.js` | pre-existing, same file — proves the search works |

All present, so the installed app carries this week's frontend work. Had the control come back 0, the absence
of the others would have been unreadable.

## Carried forward

- The class sweep is closed: the two-part test (a swallowed write **plus** a surface claiming something the
  missing write falsifies) selected three surfaces — the trail, the Agents dashboard, the repair card — and all
  three are fixed, covered and falsified. A further sweep would be a new survey.
- `main` == `origin/main` at **`4df1e73`** (10 commits from `4b65e5c`: 1 Rust, 1 memory/gateway, 1 Control
  screen, 1 trails, 1 docs, 3 records of the push/build/install). Nothing outstanding to push.
- **Bisectability of the intermediate commits is still unverified** — the tree was green as a whole before the
  split; each commit was not separately gated. A future `git bisect` landing inside `8865a2b`..`8091721` may
  hit a state that does not build.

## Live gateway test against the running app (results in `LIVE_GATEWAY_TEST_2026-09-22.md`)

Probed the real gateway on `127.0.0.1:8800` with credentials read from the keychain (service
`ai-provider-router`; accounts `masterkey` and `gwkey:<id>`). Probe scripts live in `/tmp/aip-live/` — outside
the repo, so no key can be committed.

**Confirmed working:** auth (401/429 for bad creds; master, per-app key, `x-api-key`, `x-goog-api-key` all
accepted); backoff measured at 0.55 → 1.05 → 2.03 → 4.03 → 8.04 → 16.04 → 32.01s (exact 500ms doubling, cap
32s), and **a valid key is never throttled**; 458 models; real chat round-trip in 0.79s; SSE streaming (3
frames, usage nested at `choices[0].usage`); all four wire dialects; capture → `memory_pending` +1 with
redaction applied; CRLF header smuggling blocked; the 15ms memory deadline degrades to `reason=deadline`;
cancellation on hard disconnect with no slot leak.

**The one that matters:** every request without `aip-session` lands in the **same session**
(`s-096fcb7345906f50`) regardless of which credential presented it — visible in `session_turns`, and a model
answer quoted earlier unrelated prompts verbatim. The memory layer is guarded against this contamination; the
session layer is not. Fix: derive a default session from the resolved principal.

**Smaller:** `aip-memory: write` reports `reason=client_off` (misleading — the client asked for capture-only);
upstream `429 RATE_LIMITED` carries no `Retry-After`; unknown routes answer `404` before auth (a route-existence
oracle); `405` has an empty body; ~1700-1900 prompt tokens of fixed overhead per request.

**Not reachable live:** the 8+32 capacity gate — upstream rate-limits at roughly 4 concurrent before in-flight
can build to 40, so those `429`s are the provider's, not the gateway's. Per-principal memory denial and the
master-switch-off path have no HTTP surface to drive.

**Trap for next time:** `agnes-2.5-flash` returns **empty content** at `max_tokens` ≤ 32 (hidden reasoning eats
the budget) while still reporting `finish_reason=stop` and billing tokens. It looks like the gateway dropped
the text; it did not. Use `max_tokens` ≥ 64 when a probe needs visible output.

## Fixes #1 and #2: implemented, gated, built, installed, verified live

**#1 session isolation.** `session_context.rs` now hashes `principal|user|project|agent`. The principal comes
from `key_principal_for`, already computed for the policy check, so it cost nothing new on the hot path; it is
threaded into the three `resolve_session` call sites (`freeze_key`, `live_context`, `prepare_capture`). It is
`None` when memory is off, which is inert because no session is read or written then.

**#2 write_only.** New `SkipReason::WriteOnly` → `"write_only"`, returned when the client asked for
`MemoryMode::Write`. Plus the plain-language label in `Control.tsx`, which would otherwise have rendered the
raw token.

Tests +3 (433 → 436). **Falsified properly:** dropping the principal back out of the key collapses the master
and app-key sessions to the same `s-6f7ff06f5efdc319` and `two_principals_in_the_same_scope_do_not_share_a_session`
fails; restoring it passes. Gate **ALL GREEN** (98 browser, Rust, typecheck, build, leak scan).

**Verified against the rebuilt app:** master key → `s-64ff32ac307f0593`, per-app key → `s-233fcaf2e7aba642`
(was one shared `s-096fcb7345906f50` with 247 turns). `aip-memory: write` → `reason=write_only`; `off` still
→ `client_off`. The `write_only` label is present in the shipped bundle (`dist/assets/main-DYi6nvga.js`).

### Reinstall workflow — what actually works here (durable)

- `osascript`/AppleEvents are **denied** in this sandbox (privilege violation -10004), and `ps` is denied too.
  Quit the app by taking the PID from `lsof -nP -iTCP -sTCP:LISTEN | grep 8800` and `kill`ing it.
- Back the old bundle up (`mv` to `/tmp/...`) rather than deleting, then `cp -R` the new one in.
- `tauri build` **fails at the DMG step** (`bundle_dmg.sh`, hdiutil) — the `.app` is already built and signed
  by then, so the failure is cosmetic. Don't read it as a broken build.
- **The first requests after a relaunch return 503** — stale worker heartbeat, not the keychain. `/v1/models`
  succeeds while `/v1/chat/completions` 503s, which is what makes it look like a credentials problem. Wait a
  few seconds and retry before suspecting the keychain ACL.

## Second pass — gateway findings C, D, E (fixed, gated, installed, verified live)

**C. Upstream 429 had no `Retry-After`.** One middleware (`gateway.rs::ensure_retry_after`) over the whole
router instead of ten dialect call sites — a dialect that forgot would drift with no failing test. It only
fills a *missing* header, so the gateway's own values survive. `1` is a floor matching the core's key-cooldown
floor, not the provider's window. Live: auth backoff still `30`, upstream 429 now `1`.

**D. The route table was an oracle.** `unknown_route` now authenticates first, and axum's `405` (empty body)
is replaced by `method_not_allowed` with an OpenAI-shaped body. Both matter because a 404/405 is itself a
statement that the route exists. Live: anonymous unknown route 404 → **401**; known caller still 404;
`GET /v1/chat/completions` → `405 unsupported_method`.

**E. The image finding was understated, and it is configuration — not code.** `select modality, count(*) from
models_cache` → `text|455`, **zero image rows**. Both providers (`agnes`, `cline`) declare
`capabilities: {text: true, image: false}`, their manifests have no `modalityRules.image`, and neither has a
`generateImage` endpoint. So `tagModality` tags every model text — including the 15 advertised with "image" in
the id — and `plan(model, "image")` is empty for all of them. **The model ids were never wrong.** No routing
code changed; the fix is `model-router.ts::whyNoImage`, which splits one misleading message into three real
ones. Keeps the phrase "no route" because `gatewayStatus` maps it to 404.

All six new tests **falsified first**, each for the right reason — the 405 test fails with
`EOF while parsing a value`, which is literally axum's empty body.

### Durable gotchas found this pass

- **Reinstalling invalidates the keychain ACL.** Replacing the binary makes macOS ask again before the app may
  read `ai-provider-router`/`masterkey`. Until approved, **every** request (even unauthenticated) answers
  `503 master key unavailable … approve the keychain prompt`, `Retry-After: 5`. `pgrep SecurityAgent` is the
  tell. Approve it and it recovers at once — 455 models on the next request. Do NOT confuse it with the
  post-relaunch stale-heartbeat 503, which clears on its own. Same status, different message.
- **`memory_enabled` is in-memory only and off by default after a restart** (`gateway.rs:842`). So after a
  reinstall `aip-memory: write` reports `reason=disabled`, not `write_only` — `Disabled` outranks `WriteOnly`.
  That is correct precedence, not a regression; the live `write_only` observation from pass 1 was on a build
  where the switch had been turned on. Re-checking needs the Memory screen's master switch, not HTTP.
- **The probe harness returns lowercase header names.** `dict(r.getheaders())` keeps the server's casing, and
  axum sends lowercase, so `h.get("Retry-After")` is always `None` while `h.get("retry-after")` works. That
  nearly produced a false "the header is missing" conclusion. Dump all headers before claiming absence.
- `tauri build` embeds the frontend in the binary, but string-searching it can miss: the reliable marker is
  the **asset filename hash** referenced once inside the binary (e.g. `store-CBy7P-dH.js`) — if it matches the
  freshly built `dist/assets/`, the bundle is current.

## F. The §3.5 concurrency bound was never enforced (found while closing the last open item)

`ARCHITECTURE.md` §3.5 says "max concurrent routed requests (default 8) with a bounded queue (default 32)".
The gateway had **one** semaphore — `const MAX_TOTAL: usize = 8 + 32` — and `try_slot` dispatched as soon as
it admitted a permit. So there was no queue and no cap of 8: **up to 40 requests could be routed at once**.
Nothing asserted either number. It also explains the first pass's "upstream rate-limits first": 40
simultaneous calls to one provider draw a 429 long before a 40-deep gateway gate.

**Fix:** two tiers. `permits` (40) admits; a new `dispatch` semaphore (`MAX_CONCURRENT` = 8) is awaited before
the request reaches the worker — that wait *is* the queue. `Semaphore::acquire_owned` is cancel-safe and hyper
drops the future on client disconnect, so a queued request never holds a slot for a caller that has gone.
`Slot` now holds both permits so a routed request keeps its slot until it finishes.

Two tests, both falsified: 20 concurrent with a slow worker -> the worker must be handed exactly 8 (reverting
the cap fails with "the worker was handed 20"); holding 8+32 permits -> the next request is 429 + Retry-After.
Gate ALL GREEN, rebuilt, installed, pass-2 live checks re-verified.

**Live proof of the ceiling is not reachable against `agnes`.** 20 concurrent: 4 × 200, 16 ×
`429 all attempts failed … [agnes/key-01:RATE_LIMITED]` — the provider's rate limit, then its key cooldown.
The upstream caps before the gateway does. Needs a slow local upstream, never a real single-key provider.

### Also corrected

- Per-principal memory denial is **not** a coverage gap — it is unit-tested in both directions (recall and
  capture), including "either identity may deny" and the master key's own name. Only *live* exercise is
  missing, and `memory_principal_policy` has no HTTP surface to populate.
- My first capacity probe was contaminated by my own earlier probe: 18_verify_pass2 trips the 30s auth
  backoff deliberately, and I ran 19_capacity immediately after. 16 instant 429s looked like capacity and
  were the backoff. Sequence probes, and read the 429 body — `RATE_LIMITED` (upstream) vs
  "too many failed auth attempts" (backoff) vs "router at capacity" are three different things.

## Manual provider entry added (AddProviderModal)

**File:** `apps/desktop/src/screens/Providers.tsx` — `AddProviderModal` gained a **Manual** tab beside
**Quick add**. Previously the only ways in were the 3 builtin profiles or the guided wizard; there was no way
to register a provider whose contract you already know.

Manual fields: name, base URL, auth type (Bearer / `x-api-key` / custom header+prefix), dialect
(`openai-chat-v1` | `anthropic-messages-v1`). `buildManifest()` composes a valid declarative
`AdapterManifest` and the row is registered `type: "manifest"`, status `draft`. Slug comes from the existing
exported `uniqueSlug()` in `store.ts` — I first hand-rolled `name + "-" + Date.now()`, then replaced it;
`uniqueSlug` gives `my-provider-2` on a duplicate instead of a raw epoch. Text-only, no image endpoint —
image support still needs the wizard or a later edit.

Typecheck passes; 193 desktop tests pass.

### Rebuild + reinstall (two builds, because of the slug refactor)

Built twice (`tauri build --bundles app`, ~2m30s each, signed `AI-Provider IDE Dev Signing`, notarization
skipped as always). Installed by `mv`ing the old bundle to `/tmp/prev-app-<ts>` then `cp -R`.

Verified the installed app really is this build, three ways (all passed):
1. `shasum -a 256` — installed binary **byte-identical** to the bundle (11,409,424 bytes).
2. Asset hash inside the binary == fresh `dist/assets/`: first build `main-CN0bNDKM.js`, after the slug
   refactor `main-BZ3NL4j-.js` — the changed hash is itself proof the second edit shipped.
3. New strings present in `dist` (`Add any OpenAI- or Anthropic-compatible provider`, `Quick add`,
   `x-api-key header`) **paired with a control** (`Drift suspected`) so an absence would be readable.

### Keys

- The app lives at **`/Applications/AI-Provider Router.app`** (with a space; `ls /Applications | grep -i
  provider` returns nothing because the match is "AI-Provider").
- After install the gateway answered **503 `master key unavailable`** with SecurityAgent running — the
  documented reinstall behaviour: replacing the binary invalidates the keychain ACL, and the master-key check
  precedes auth so *every* request 503s until the prompt is approved. Needs a human click; it is not a bug.
- Before that approval the earlier build of the same change answered **401 invalid gateway key** — so the
  ACL had not yet been invalidated by that first swap. 401 vs 503 both mean "listener up"; only 503 blocks.
- Probe hygiene: `unset` all five proxy vars (`all_proxy`/`ALL_PROXY` too) *and* use `env -u`; a partial
  unset still yields curl `000` with no body, which reads like a crash but is a proxy artefact.

### Captured as a skill

The rebuild → reinstall → prove-it-shipped loop is now `~/.workbuddy-ai/skills/tauri-macos-rebuild-verify/`
(kill by port, `tauri build --bundles app`, back-up-then-`cp -R`, three-way verification, and the 000/401/503/429
probe table). Run twice today and several times before — the traps are stable, so it belongs in a skill rather
than only in the daily log.

### MEMORY.md was over its injection ceiling

It had reached **10,970 bytes against an 8,000-char hardcoded limit** (`app.asar`), so it was being truncated
on load and the tail — including the "repo is PUBLIC" rule — was silently dropped. Compressed to **7,190 bytes**
(810 headroom) keeping all 46 rules: rationale clauses cut and pointed at REFERENCE.md. Watch the size when
adding; the tail is the first thing lost.

## External coding-agent compatibility audit (zcode / Claude Code / Codex)

Question: will the gateway work with outside coding agents? Answered from the route table and the three
ingress translators, plus a live probe (`curl` with a bogus key → **401**, i.e. healthy, per the known trap).

**Route table** (`gateway.rs:1716`) — dial 8800. `GET /v1/models`, `POST /v1/chat/completions`,
`POST /v1/images/generations`, `POST /v1/messages`, `POST /v1/responses`, `POST /v1beta/models/{*tail}`.
Auth accepts **Bearer / x-api-key / x-goog-api-key** (`presented_key`, `gateway.rs:1319`), so all three
clients can present a credential in the dialect they natively use. Gateway bodies do **not** use
`deny_unknown_fields` (only `memory.rs:142` does), so unknown fields are dropped, not rejected — which is
why these failures are silent rather than 400s.

**Verdict: `/v1/chat/completions` is the only fully working path.** It streams, emits `tool_calls` +
`finish_reason: "tool_calls"` + `[DONE]` (`gateway_handlers.rs:145`). So **zcode via "OpenAI Base URL"
and Codex with `wire_api = "chat"` work today.**

**Three blockers on the other two dialects, all the same class — tool blocks flattened on ingress:**

1. **Anthropic `system` as a block array is dropped.** `to_chat_body` reads
   `req.get("system").and_then(Value::as_str)` (`gateway_anthropic.rs:31`). Claude Code always sends
   `system: [{type:"text", …, cache_control:{…}}]`, so the whole system prompt vanishes.
2. **`tool_use` / `tool_result` blocks from prior turns are flattened to `""`** — the content-block joiner
   only picks `.text` (`gateway_anthropic.rs:38-45`). Same for `function_call` / `function_call_output`
   items in the Responses path (`gateway_responses.rs:33-46`). The agent loop loses its own history.
3. **Anthropic streaming never terminates a tool turn.** When `has_tool_calls`, the
   `if !has_tool_calls` guard (`gateway_anthropic.rs:281`) skips `content_block_stop`, and
   `message_delta` / `message_stop` are never emitted — so no `stop_reason: "tool_use"` reaches the
   client. Non-stream is correct (`stop_reason` set at `:378`); only the SSE branch is wrong.
   Also `stop_reason` inside that guard is dead code — it can only ever be `end_turn`.

**Secondary gaps:** `/v1/responses` forwards `tools` **verbatim in Responses flat shape**
(`{type,name,parameters}`) to a chat-completions provider that expects nested
`{type:"function",function:{…}}` — the Anthropic path converts, this one does not (inconsistency,
`gateway_responses.rs:56`). Missing `input` → 400, so `previous_response_id` continuation fails.
`thinking`, `cache_control`, `temperature`, `top_p`, `store`, `parallel_tool_calls` are silently dropped
on both non-chat dialects.

**Live probe confirmation:** `curl -H "Authorization: Bearer probe-invalid" :8800/v1/models` → 401
`invalid gateway key`. `:8787` (AI Hub v2) → 502, unrelated service.

**Fix order if taken up:** (a) system-array + tool-block ingress in `gateway_anthropic.rs`,
(b) the `!has_tool_calls` streaming guard, (c) Responses tool-shape conversion. Each needs a
falsification-first test — flip the fix off and watch the spec fail.

## FIXED — Anthropic streaming never terminated a tool turn (blocker 3 of 3)

`gateway_anthropic.rs`, SSE branch of `messages_h`. A tool turn emitted `content_block_start` index 0 (text),
then tool blocks at 1..n, then **stopped** — no `content_block_stop` for index 0, no `message_delta`, no
`message_stop`, so no `stop_reason: "tool_use"`. That is exactly the signal Claude Code reads to decide to run
tools. Non-stream was already correct; only the SSE branch was wrong.

**Fix (22 lines, two edits):**
1. In the `BridgeMsg::ToolCalls` arm, close the text block (`content_block_stop` index 0) *before* opening the
   first tool block. Anthropic orders content blocks strictly, so closing index 0 afterwards is out of
   sequence — and leaving it open hangs the client's parser.
2. Removed the `if !has_tool_calls` guard around the terminal events. `content_block_stop` index 0 is now
   emitted under that guard only for plain text turns; `message_delta` (with `stop_reason`) and
   `message_stop` are emitted **always**. The old `let stop_reason = if has_tool_calls { "tool_use" } ...`
   was dead code — it sat inside `if !has_tool_calls`, so it could only ever be `end_turn`.

**New test:** `gateway::tests::anthropic_stream_tool_turn_stops_with_tool_use` (`gateway_tests.rs:1282`),
next to the two pass-through tool tests. Drives `/v1/messages` with `stream: true` + a declared tool,
`answer_with_tool_calls(true)`, then asserts on **parsed** SSE payloads: a `tool_use` content block, a
`content_block_stop` at index 0, `delta.stop_reason == "tool_use"`, and a `message_stop`.

**Falsification:** the test failed before the fix (stream ended at `content_block_stop` index 1). After the
fix it passed, then flipping `stop_reason` to a hardcoded `"end_turn"` made it fail again — restored.

**Trap hit:** my first assertions were raw substrings like `"type":"content_block_stop","index":0`. They
failed against correct output because **`serde_json` emits object keys in sorted order** — the real payload
is `{"index":0,"type":"content_block_stop"}`. Rewrote to parse each `data:` line and compare fields.
Recorded in MEMORY.md. Also: bash `grep` on a redirected file returns empty here (shim) — use the Grep tool
or `tail`.

**Verification:** `cargo test --lib` → **442 passed, 0 failed** (was 441). `cargo check --manifest-path
apps/desktop/src-tauri/Cargo.toml` clean. No TS touched, so typecheck/vitest/Playwright unaffected; CI runs
`cargo check` + `cargo test` and neither gates on fmt/clippy.

**Remaining, larger:** blocker 1 (`system` as a block array dropped, `gateway_anthropic.rs:31`) and
blocker 2 (`tool_use`/`tool_result` flattened on ingress), plus the Responses tool-shape mismatch.

## FIXED — Anthropic ingress now maps the tool protocol instead of flattening it (blocker 2)

`gateway_anthropic.rs`. Extracted the message loop out of `to_chat_body` into a new
`messages_to_openai(req) -> Vec<Value>` plus `tool_result_text(block)`, then rewrote it as a real
protocol mapping.

**Mapping now performed:**
- assistant `tool_use` blocks → `tool_calls[]` on that same assistant message, with
  `function.arguments` as a **JSON string** (OpenAI) built from Anthropic's `input` **object**;
  missing `input` becomes `{}`, never null.
- user `tool_result` blocks → their own `{"role":"tool","tool_call_id":…,"content":…}` messages.
  `content` may be a string, an array of blocks, or absent — `tool_result_text` handles all three.
- prose mixed into a tool-result turn is **flushed before** the tool message, so block order is
  preserved in both directions (`[text, result]` and `[result, text]`).
- a turn that was *only* a tool result emits **no** extra empty user message. That empty turn was
  the concrete defect — it is what made providers reject the body and left the assistant turn
  dangling. `answered_a_call` is the flag that suppresses it.
- still dropped, deliberately: `thinking` / `redacted_thinking` / `image` (no OpenAI equivalent
  here). Note Anthropic *requires* thinking blocks echoed back when extended thinking is on, so
  that path can 400 rather than degrade — separate job.

**Tests (5 unit + 1 e2e):** `a_tool_result_becomes_its_own_tool_message`,
`an_assistant_tool_use_becomes_openai_tool_calls`, `a_block_shaped_tool_result_keeps_its_text`,
`text_and_a_tool_result_keep_their_order`, `a_plain_string_transcript_is_untouched` (regression
guard, passes under both old and new code), and
`gateway::tests::anthropic_tool_transcript_reaches_the_worker_intact`.

**Falsification, both directions:** first landed `messages_to_openai` as a verbatim copy of the old
flattening and watched the 4 behavioural tests fail (output showed the exact symptom:
`[{"content":"","role":"user"}]`) while the regression guard passed. After the rewrite all passed.
Then flipped `"role":"tool"` → `"user"` and watched the e2e test fail, then restored.

**Two traps hit:**
- `assert_eq!(out, json!([...]))` does not compile — `Vec<Value>` has no `PartialEq<Value>`.
  Compare against `vec![json!(...), json!(...)]`.
- `cargo test --lib <filter>` matches **test names, not function names**. Filtering on
  `messages_to_openai` ran 0 tests; filter on the module (`tool_conversion_tests`) instead.

**Verification:** `cargo test --lib` → **448 passed, 0 failed** (was 442). `cargo check
--manifest-path …` clean. No TS touched.

**Still open:** blocker 1 (`system` as a block array dropped, `to_chat_body` reads `.as_str()`),
and the same flattening in the Responses dialect (`to_chat_body_responses` — `function_call` /
`function_call_output` items collapse the same way), plus the Responses `tools` shape mismatch
(flat Responses shape forwarded to a chat-completions provider that expects nested
`{type:"function",function:{…}}`).

## FIXED — Responses dialect: same tool-protocol mapping plus the tools shape (blocker 4)

`gateway_responses.rs`. Extracted the item loop into `items_to_openai` + `item_text` +
`function_output_text`, then rewrote it to match the Anthropic fix.

**Mapping now performed:**
- `function_call` items → `tool_calls` on an **assistant** message. `arguments` is already a
  string in Responses (unlike Anthropic's object) but an object is stringified defensively.
  **Consecutive** `function_call` items are merged into ONE assistant turn — that is how parallel
  tool use arrives, and splitting them would desynchronise the tool messages that follow.
- `function_call_output` → `{"role":"tool","tool_call_id":call_id,"content":…}`; `output` may be a
  string or an object.
- `reasoning` items are **dropped, not turned into a turn**. They carry no role and no content, so
  the old code emitted `{"role":"user","content":""}` — inventing a turn the client never sent.
- unrecognised items with no role/content are skipped for the same reason.
- **tools shape fixed**: Responses declares tools FLAT (`{type,name,description,parameters,strict}`);
  they are now nested under `function` for chat completions. `responses_tools_to_openai` passes
  through anything already nested. `responses_tool_choice_to_openai` nests only the object form
  that names a tool; bare strings (`auto`/`none`/`required`) are identical on both wires.

**Tests (5 unit + 1 e2e):** `a_function_call_becomes_openai_tool_calls`,
`a_function_call_output_becomes_a_tool_message`,
`a_reasoning_item_is_dropped_not_turned_into_an_empty_turn`,
`a_flat_responses_tool_becomes_a_nested_openai_function`,
`a_plain_message_transcript_is_untouched` (regression guard), and
`gateway::tests::responses_tool_transcript_reaches_the_worker_intact`.

**Falsification:** old behaviour extracted verbatim first — 3 of 4 tests failed (a `function_call`
came out as `{"role":"user","content":""}`). Tools test written against the existing
`to_chat_body_responses` entry point so it compiled and failed *before* the conversion existed
(`function.name` was Null). After implementing, reverted the tools wiring to `.cloned()` and
watched the e2e fail with the flat shape in the dump (`"name":"Bash"` at top level) — restored.

## FIXED — Anthropic `system` as a block array (blocker 1)

`to_chat_body` read `req.get("system").and_then(Value::as_str)`, so Claude Code's array form — the
only form it sends — dropped the entire system prompt. Now a `match` on `Some(Value::String)` /
`Some(Value::Array)`; the array is joined from its `text` blocks. `cache_control` markers are still
discarded, so the prompt arrives but is not cached. Test `a_block_array_system_prompt_survives`
fails first (dump showed no system message at all), passes after.

**Verification (end of session):** `cargo test --lib` → **455 passed, 0 failed** (was 442 at
session start). `cargo check --manifest-path …` clean. No TS touched.

**Still open:** `thinking`/`redacted_thinking`/`image` blocks are dropped on both dialects
(Anthropic *requires* thinking blocks echoed back when extended thinking is on, so that path can
400); `cache_control` ignored so no prompt caching; `/v1/responses` with `previous_response_id`
and no `input` still 400s (no server-side response storage).

## Correction — the thinking-block 400 risk was overstated

I wrote earlier that dropping `thinking` blocks "can 400 rather than degrade". That is wrong as
stated. `to_chat_body` never forwards the `thinking` **parameter**, so the upstream provider never
enters extended-thinking mode, so there are no thinking blocks to echo back and nothing for
Anthropic to reject. The 400 is **latent, not active**: it only materialises if we start forwarding
`thinking`, at which point preserving the blocks becomes mandatory in the *same* change. Treating
them as one coupled feature is the rule to carry forward — do not forward one without the other.

Today the real cost of dropping them is quality only: a client resuming a session whose history
already contains thinking blocks loses that reasoning context.

## Also unverified: `/v1/messages/count_tokens`

Claude Code calls it for context accounting. It does not exist in this app — no route, no handler,
no string anywhere in `apps/desktop` (grepped). Whether a 404 there is fatal or merely degrades the
context meter is **unconfirmed**: web search returned only low-quality blog posts, none
authoritative. Do not build it on speculation — settle it in the live smoke test first.

## Live ZCode run — first real external client through the Anthropic ingress

Tushu pointed ZCode at the gateway (Anthropic-compatible) and ran a task. It worked for ~27s, then
failed with our own error: `all attempts failed for agnes-2.0-flash [agnes/key-01:RATE_LIMITED ->
agnes/Key-02:RATE_LIMITED]`. Confirmed ours by grep (`gateway-bridge.test.ts:332`,
`engine.test.ts:74` — same "all attempts failed" / `→ RATE_LIMITED` format family), not ZCode's.

**Not a compatibility bug.** The Anthropic ingress worked end to end. Diagnosed from the live DB
(`~/Library/Application Support/dev.aiprovider.router/ai-provider-router.db`, opened `?mode=ro`):

- `ledger` shows six `source='gateway'` requests with `status='ok'`, `tokens_in` 97k–101k, against
  provider `agnes`, alternating between its two keys.
- The failures are 429 `RATE_LIMITED` in **bursts** (15:02:15–15:02:28 seven in a row; also 15:04:39)
  with `fallback_chain_json` showing both `key-01` and `Key-02` returning `RATE_LIMITED`.
- Interleaving is the tell: ok 15:02:13 → seven 429s → ok 15:02:35 → ok 15:04:20 → ok 15:04:37 →
  429 15:04:39. Classic token-per-minute window exhaustion and recovery.

**Cause: a ~97k-token agent context against a 2-key provider.** One request consumes roughly a whole
window, so you get one or two successes per window and 429s in between. Nothing in the router is
wrong — it correctly tried both keys, both were limited, and returned a clean 429 with Retry-After.

**Diagnostic technique worth reusing:** to diagnose any gateway failure, read `ledger`. A row with
`status='error'` and **empty `provider_id`/`key_id`** means no key was selectable — every key was
already in cooldown. `fallback_chain_json` then gives the per-key class. `tokens_in` on the
neighbouring `ok` rows tells you whether size is the driver.

**Follow-up this motivates:** prompt caching (`cache_control`) is no longer just a cost item — a 97k
context re-sent every turn is also what drives the rate limiting. Also worth verifying: when *all*
keys are in cooldown we return `Retry-After: 1` (only filled in when absent), which tells a client to
retry in 1s into another 429. Reporting the longest remaining cooldown instead would let the client
back off properly rather than failing the turn. Needs measurement before changing.

## Refinement — the ZCode 429 vs WorkBuddy success comparison

Tushu re-ran the same prompt through the same router in WorkBuddy: **24 consecutive `ok` requests,
zero 429s**. Pulled the ledger for the window. The earlier diagnosis was directionally right but
missed two things:

1. **It was not the same model.** ZCode used `agnes-2.0-flash`; WorkBuddy used `agnes-2.5-flash`.
   The ledger `requested_model` column says so plainly. So the comparison is not apples-to-apples.
2. **The driver is throughput, not context size alone.** tokens-per-minute ≈ context × rate.
   WorkBuddy: 41k→65k tokens/request, ~3 requests/min (24 requests over 8 min), all ok.
   ZCode: 97k–101k tokens/request, up to ~30 requests/min (7 requests in 13s), 429 bursts.
   That is roughly a 20× difference in tokens per minute.

**Conclusion unchanged but better grounded: the router is fine.** Both clients work through it —
ZCode made 6 successful large requests before its provider window ran out, WorkBuddy made 24 with
no errors. The 429 is client behaviour (context size × cadence) against the provider's window,
possibly compounded by a tighter limit on the 2.0 model than the 2.5 one.

**De-prioritises prompt caching as a fix for the 429.** WorkBuddy was fine at 65k tokens, so the
threshold is above that; caching is a cost measure, and I have no evidence cached tokens escape the
rate-limit window. Reducing context below ~65k, or using the 2.5 model in ZCode, is the reliable fix.

**Diagnostic note:** `ledger.requested_model` is the column that catches "same model" assumptions.
Check it before comparing two runs.

## FIXED — the router discarded the provider's Retry-After entirely (stage 1)

Found while measuring the ZCode 429s. The engine's own header comment claims §3.6 "backoff
honoring Retry-After". It was never implemented — the value was dropped at two points:

1. `ManifestHttpError` (`manifest-interpreter.ts:152`) carried only `status`, `body`, `kind`.
   **Response headers were never captured**, so the provider's `Retry-After` was unreachable.
2. `AttemptOutcome.retryAfterMs` existed in the interface but was **never assigned**
   (`execution-engine.ts:122` built the outcome without it), so `health.recordResult(..., undefined)`
   hit `Math.max(undefined ?? 0, 1000)` — a flat **1 second** cooldown for every rate-limited key,
   regardless of what the provider asked for.

Net effect: keys came back after 1s and the gateway answered `Retry-After: 1`
(`ensure_retry_after` fills it in only when absent), so a client retried straight back into a
window the provider had told it to wait out. That is the seven-429s-in-thirteen-seconds pattern.

**Fix (TS only, 3 files):**
- `manifest-interpreter.ts`: added `headerValue` (case-insensitive — `HttpPort` returns a plain
  `Record`, not a `Headers`), `parseRetryAfter` (RFC 9110: seconds *or* HTTP-date; unreadable →
  undefined so the caller keeps its floor), and `retryAfterFrom(headers)`.
- `ManifestHttpError` gained a 4th ctor arg `retryAfterMs?`; both `res.status >= 400` throw sites
  now pass `retryAfterFrom(res.headers)`.
- `execution-engine.ts:122` now propagates `e.retryAfterMs` into the outcome.

**Tests:** new `packages/router-core/test/retry-after.test.ts` — 3 tests driving a real
`ManifestInterpreter` over a fake `HttpPort` that 429s with a header: 60-second delay, HTTP-date
form, and an absent header (regression guard for the 1s floor). Built the HttpPort fake on the
pattern in `concurrency.test.ts` (`headers` is `Record<string,string>`, not `Headers`).

**Falsification:** 2 failed / 1 passed before the fix. After: 3 passed. Then reverted just the
propagation line to `retryAfterMs: undefined` and watched 2 fail again — restored.

**Verification:** `pnpm --filter @aiprovider/router-core test` → **237 passed, 20 files**. Root
`pnpm typecheck` clean across all three workspaces.

**Stage 2 still open:** the *client* still gets a flat `Retry-After: 1` when all keys are cooled,
because `BridgeMsg::Error` carries only `{status, message}` — no retry-after crosses the Tauri
boundary. Making the client back off properly needs that plumbing (Rust + TS), then reporting the
longest remaining cooldown instead of the constant 1.

## Plan, re-ranked by today's evidence

1. Retry-After: stage 1 DONE; stage 2 (client-facing) next.
2. `count_tokens` — still unverified whether Claude Code needs it; ZCode never called it.
3. `cache_control` — cost/throughput measure; WorkBuddy was fine at 65k tokens so it is NOT the
   fix for the 429s. Lower priority than I said this morning.
4. `previous_response_id` — verify Codex actually sends it before building a response store.
5. `thinking` blocks — coupled to forwarding `thinking`; quality only, latent not active. Last.

## SETTLED — count_tokens: the router does NOT implement it, and Claude Code DOES call it

**Investigation:**

1. **Gateway route table** (`gateway.rs:1716-1722`): six routes exist — `/v1/models` (GET),
   `/v1/chat/completions` (POST), `/v1/images/generations` (POST), `/v1/messages` (POST),
   `/v1/responses` (POST), `/v1beta/models/{*tail}` (POST). No `/v1/messages/count_tokens`.
   Any hit on that path falls through to `unknown_route` (`gateway_handlers.rs:274`) which
   authenticates then returns 404 `{"route not found", "not_found", "unknown_route"}`.

2. **Ledger**: no `path` column at all — it records `source`, `provider_id`, `requested_model`,
   `status`, `http_status`, `error_class`, `tokens_in/out`, `latency_ms`, `cost_estimate_micros`,
   `fallback_chain_json`. So I cannot use the ledger to detect whether a client *tried* to call
   count_tokens. The gateway.log is lifecycle-only (watchdog, sync, startup) — no HTTP request
   logging. Unknown routes do emit `tracing::warn!("unknown gateway route hit")` but that goes
   to the tracing subscriber, not the log file.

3. **Codebase grep**: zero matches for `count_tokens` anywhere in the project (Rust or TS).

4. **External evidence**: `fuergaosi233/claude-code-proxy` (a community Claude Code proxy)
   implements `POST /v1/messages/count_tokens` with a 4-chars-per-token estimation. This
   confirms Claude Code CLI calls this endpoint — a proxy wouldn't implement it otherwise.

**Conclusion:** Claude Code calls `/v1/messages/count_tokens` and our router 404s it. ZCode
apparently did not (or silently swallowed the 404), which is why the 429s were the visible
symptom. But any Claude Code client hitting our gateway gets a 404 on every token-count request.

**Severity:** Low–medium. Claude Code likely falls back to its own estimation on 404 (it has
to handle the real API being unavailable). But returning 404 on every count_tokens call is
noisy and unprofessional, and a client that doesn't fall back would break.

**Fix sketch (cheap, ~20 lines Rust):** add a `.route("/v1/messages/count_tokens", post(count_tokens_h))`
handler that authenticates, parses the same `AnthropicMessagesRequest` body, sums the text
content, and returns `{"input_tokens": N}` using the 4-chars-per-token approximation (same as
the community proxy). No upstream call needed — this is pure estimation. Alternatively, forward
to the real provider's count_tokens if the provider supports it, but that is more work for
no clear benefit over the estimation.

## DONE — POST /v1/messages/count_tokens endpoint

**What:** Added a local token-count estimate endpoint so Claude Code (and other Anthropic-SDK
clients) no longer get a 404 on every `count_tokens` call.

**Why:** Claude Code calls `/v1/messages/count_tokens` to pre-flight prompt size. The gateway
had no such route — it fell through to `unknown_route` and returned 404. A community proxy
(`claude-code-proxy`) implements the same endpoint, confirming the call path is real.

**Fix:**
- `gateway_anthropic.rs`: added `IMAGE_TOKENS = 1024` constant, `count_text_chars(&Value) -> usize`
  helper (walks `system` + `messages` + `tools`, same content `to_chat_body` forwards), and
  `count_tokens_h` handler — authenticates first, parses body, returns `{"input_tokens": N}`
  at 4-chars-per-token (flat image budget per block, tools counted as JSON). No upstream call,
  no ledger entry.
- `gateway.rs`: `.route("/v1/messages/count_tokens", post(count_tokens_h))` next to `/v1/messages`;
  `use anthropic::{count_tokens_h, messages_h}` import added.

**Tests:** 8 unit tests for `count_text_chars` (plain string, system string, system array, tool_use,
image, tools, empty body, tool_result) + 3 HTTP integration tests (happy path 200, unauth 401,
invalid body 400). Falsified by temporarily removing the route — all 3 integration tests failed
with 404, then passed after restoring. Full suite: **466 passed, 0 failed**.

**Plan doc:** `COUNT_TOKENS_PLAN.md` written to the project root, covering approach, text coverage,
image handling, endpoint contract, code locations, test plan, and build/verify steps.

## SETTLED — previous_response_id: not needed, not supported, Codex dropped it

**Finding:** `previous_response_id` is an **OpenAI Responses API** concept (stateless
conversation chaining: each request references the prior response to inherit context). It has
**nothing to do with Anthropic Messages or Claude Code.**

- No mention in our Rust codebase (`gateway_responses.rs`, `gateway.rs`, or anywhere else).
- Not in the gateway log or ledger.
- Codex CLI no longer uses it: GitHub issue openai/codex#4047 (Sep 2025) reports that Codex
  stopped using `previous_response_id` for multi-turn after PR #1641. The OpenAI reasoning best
  practices doc recommends it, but the client itself moved on.
- Our `/v1/responses` handler (`gateway_responses.rs:157-183`) translates `instructions` +
  `input` + `tools` → chat body. No `store` param, no response-id chain. This is fine for the
  current client mix.

**Verdict: item 4 is closed. No work needed.** The recommendation to build a response store
before verifying Codex actually sends `previous_response_id` was premature — Codex doesn't.
The original plan item drops from the board.

## Updated plan status

| # | Item | Status |
|---|---|---|
| 1 | Honour upstream `Retry-After` | Stage 1 done; stage 2 (client-facing) next |
| 2 | `/v1/messages/count_tokens` | ✅ DONE — implemented + tested (11 tests, 466 total pass) |
| 3 | `cache_control` | Cost measure; demoted — not the 429 fix |
| 4 | `previous_response_id` | ✅ CLOSED — Codex doesn't send it, no implementation needed |
| 5 | `thinking` blocks | Quality only, latent — last |

Next actionable item: Retry-After stage 2 (plumb longest key cooldown through to the client).

## DONE — Retry-After stage 2 (the cooldown now reaches the client)

**Picked up mid-edit and non-compiling.** `STAGE2_RETRY_AFTER_PLAN.md` said "planned, not
implemented", but the working tree already carried most of stage 2 (enum + command + bridge +
`AllAttemptsFailedError.maxRetryAfterMs()`) alongside **10 compile errors**: 8 × `E0027`
(handlers still destructuring the old two-field `BridgeMsg::Error`) and 2 × `E0308` (`err_ra`
was `&'static str`, which cannot carry a computed cooldown). Lesson: a plan doc's status field
is not evidence of the tree's state — check `git status` and compile first.

**Design deviation, and it is an improvement.** The plan wanted
`router.healthTracker.maxRemainingCooldownMs()`; the implementation uses
`AllAttemptsFailedError.maxRetryAfterMs()` — the provider's own cooldown across *this* request's
attempts. A global max would include a key cooled by an earlier request and over-tell the
client. `maxRemainingCooldownMs` had no caller (dead) → removed.

**Rust.** `gateway.rs` gained `cooldown_secs()` (ms → whole seconds, ceil, never 0) and
`err_with_cooldown()` (429 + cooldown → `Retry-After`, else `err`); `err_ra` now takes
`impl Into<String>`. All six non-streaming paths call the helper — chat, models, image,
messages, responses, gemini — one place, so no dialect can drift. Streaming paths destructure
with `..`: the 200 is already committed, so a header is impossible there.

**Falsified, both directions.** 4 new Rust tests (71 s / 30 s / 45 s / 400 ms → 1). Reverting
`err_with_cooldown` to plain `err` fails the three cooldown tests with `left: "1"` — the
middleware floor — while the two floor tests keep passing, so the new tests carry the signal.
2 new TS tests for `maxRetryAfterMs`; a "keep the last value" probe fails
`expected 12000 to be 45000`, because the longest is deliberately first in the chain.

**Verified.** `cargo test --lib` **470 passed** (466 before). router-core vitest **239 passed**
(237 before). `pnpm ci:local` **ALL GREEN** (98 browser).

**Not done, deliberately.** `web-test/shim.ts` untouched — its `gateway_error` case is a blind
`return null` that records nothing, so a field name there would be a no-op; verifying the field
at the web layer needs an invoke-recording facility the shim does not have. **Open question:**
max over-waits — the earliest a retry can succeed is the *shortest* cooldown. Conservative and
matches the plan; product call, not a bug.

**Committed.** The whole of today's work — Anthropic/Responses ingress, count_tokens, Retry-After
stages 1 and 2 — landed as three commits on `main`: `30cdcba` (the TS retry-after half),
`1b45ed7` (the Rust gateway half + the plan docs), and a memory commit. `30cdcba` was verified
green **in isolation** (typecheck across all three workspaces + router-core 239), so the split is
bisectable where it can be; `1b45ed7` is exactly the tree `pnpm ci:local` ran green on. The
ingress fixes, count_tokens and Retry-After stage 2 share `gateway.rs`, `gateway_anthropic.rs`
and `gateway_tests.rs`, so they could not be split without hunk surgery no commit could be built
from. **Pushed** — `3f4a6d8..f0ed2a2` to `origin/main`, after re-running `pnpm key-leak-grep` on
the exact tree (OK); that is the one step worth repeating before publishing to a public repo.
`main` is in sync with `origin/main`.

## CI has been running all along — the "Actions billing" note was a private-repo artefact

Tushu corrected a standing claim: the billing block applied to **private** usage. The repo went
public on 2026-09-21 ~21:33 UTC, and Actions minutes are free for public repos, so CI resumed
then. Checked against the API rather than the comment:

- `gh repo view` → `isPrivate: false`, `visibility: PUBLIC`.
- Runs **before** the flip failed in **9-10s** — the "the job was not started" signature
  (`35581471591`, `35582272079`). Runs **after** take **3-7 minutes** and report real results.
- `scripts/ci-local.sh` still claimed CI "has not started a job since ~2026-09-16". Corrected: it
  keeps its place as the fast local mirror, not as the only gate.
- A ~9-second CI failure is that old signature, not a test result. Read the duration first.

### The flake that was turning CI red: `history.spec.ts:122`

**Root cause, proven.** History loads in two steps: `loadHistorySessions` fills the rail, an
effect then sets `selected`, and `loadHistoryTimeline` fills the timeline. The test waited with
`page.getByText("what is in this folder").first()` — but the **rail renders the same session
preview** (`s.preview`, `History.tsx:171`) and comes first in document order, so `.first()` always
matched the rail. The wait was therefore satisfied *before the timeline fetch had even started*.
The test then does a one-shot `page.evaluate` (no retry) and sampled an empty `[data-kind]` list.

That is the recorded rule firing exactly: *"`getByText` matching two elements = a duplicated fact
on screen, not a bad selector. Scope to the container."*

**Fixed** by waiting on the timeline itself —
`expect(page.locator("[data-kind]")).toHaveCount(4, { timeout: 15_000 })`. `[data-kind]` appears
only in the timeline, `toHaveCount` retries, and the entries render in one commit, so once four
exist the order is final. The house pattern is already there at `history.spec.ts:199`.

**Falsified with a controlled race, not by hoping.** A temporary probe in `shim.ts` `handle()`
delayed the `history_timeline` reply by 3s to make the two-step load deterministic. Under the
probe the **old** wait failed with `Received: Array []` — byte-identical to the CI failure
(`35721284990` and the three 2026-09-21 runs) — and the **new** wait passed. Probe reverted.

**Not fixed, deliberately:** `history.spec.ts:110` carries the same misleading wait. It is
harmless because everything after it retries (`toBeVisible` / `toContainText`), and a test that
never fails cannot falsify a change to it. Flagged rather than churned.

**Trap for next time:** `~/Library/Caches/ms-playwright` does not exist in the sandbox HOME, so a
bare `pnpm exec playwright test` dies with "Executable doesn't exist … chrome-headless-shell".
`pnpm exec playwright install chromium` (~94 MB) fixes it. The gate's own install step ran against
a different HOME, which is why `pnpm ci:local` was green while a direct playwright run was not.

### Pushed and confirmed green in CI

`git push origin main` → `a2880b2..2670cdc` (three commits: the History flake fix, the `ci-local.sh`
comment correction, the memory record). Run **`35724733085`** finished **success** in 3m03s — all 16
steps, including the "Live-UI tests (Playwright harness)" step that had failed the four previous
runs. The only annotation is the unrelated Node-20-on-Actions deprecation notice.

Pre-push guardrails, in order: tree clean; `pnpm key-leak-grep` → OK on the exact tree; diff reviewed
(`origin/main..HEAD` = 3 files, `shim.ts` untouched, confirming the race probe was fully reverted);
the targeted `history.spec.ts` re-run → 7 passed; all six proxy vars unset via `env -u`; no force-push.

**Note on the prior run:** `a2880b2` (the commit *before* the fix) had itself passed CI. That is the
nature of the flake — it is a race, so green does not mean fixed. It failed on `f0ed2a2` and on three
2026-09-21 runs. The fix is justified by the deterministic reproduction, not by any single red run.

### MEMORY.md drift corrected — two counts were stale

Measured rather than assumed, and both were wrong:

- router-core `231` → **239** (`packages/router-core`, 20 files)
- Rust `--lib` `433` → **470** (the Retry-After work added 4; the rest predates it)

`vitest 193` and `browser 98` were already correct. The count line is now byte-neutral
(`231`→`239`, `433`→`470` are the same width), so the fix cost nothing.

Also added one rule, promoted from this investigation because a future spec author will not read
`history.spec.ts`: a readiness wait must match something **only** the awaited view renders, else the
wrong copy satisfies it. It extends the existing duplicate-fact rule rather than starting a new one.

**MEMORY.md is now 7,957 / ~8,000 bytes — 43 bytes of headroom.** The next rule cannot be added
without a distillation pass. `REFERENCE.md` (131 KB) is the intended overflow target, but nothing has
been moved there yet. This needs a decision, not another quiet append.

### The min-vs-max question, resolved — and the fold was wrong

Stage 2 shipped `maxRetryAfterMs()` (the *longest* wait any provider named) and left the inverse as an
open product question. Asked Tushu; he chose **min**. The evidence made it a one-way door rather than a
taste call:

**`route-planner.ts:156` filters on health** — `keys.filter((k) => health.isKeyUsable(k, now))`. A cooled
key is not deprioritised, it is **excluded from the plan**, and the plan is rebuilt per request. So the
router serves the retry when the *first* cooled key frees up. Reporting the longest made the client wait
for a key the planner would never have chosen. The stage-2 example inverted: keys cooling in 58 s / 42 s /
71 s returned `71`, forcing a 29-second idle that bought nothing.

`AllAttemptsFailedError.minRetryAfterMs()` now takes the minimum, floored at `COOLDOWN_FLOOR_MS`, over
**every** named wait — not only `RATE_LIMITED` ones. That last part is the one judgement call: a 503
carrying `Retry-After: 30` does not cool its key (`recordResult` only cools on `RATE_LIMITED`), so
filtering it out would tell the client to retry in a second, straight back into an overloaded provider.
I wrote the class filter first, then removed it — see "the first probe lied" below for why the
distinction matters. A test now pins the choice so a later tidy-up fails loudly.

`COOLDOWN_FLOOR_MS` is exported from `health-tracker.ts` and used by both the tracker and the fold. Two
hardcoded `1000`s were free to drift, and the whole point is that the wait the client is *told* matches
the wait the tracker *enforces*.

**Falsified twice, one probe at a time:**
- A true "longest" fold (seed `-Infinity`, fold `Math.max`) → **exactly one** failure,
  `expected 60000 to be 30000`. The shortest value sits in the *middle* of the chain, so a fold keeping
  the first or the last value also fails.
- Removing the floor → **exactly one** failure, `expected 400 to be 1000`.

**The first probe lied, and that is the lesson.** Seeding `+Infinity` and folding with `Math.max` yields
`Infinity`, so `Number.isFinite` sent the result to `0` and *four* tests failed — for a reason that had
nothing to do with max-vs-min. A probe must be a *working* implementation of the wrong thing. Had I
stopped at "tests went red", I would have recorded a falsification that never happened.

Rust tests were **not** touched: the choice lives entirely in the TS fold, and the Rust bridge receives a
single number.

**Gate: ALL GREEN** — typecheck clean, core 241, vitest 193, Rust `--lib` 470, browser 98.

### MEMORY.md distilled into REFERENCE.md (Tushu's call)

Chosen over leaving it and over patching the app bundle's 8,000-byte ceiling (which reverts on update).
The two largest rules — signing identity (351 B) and keychain approval (329 B) — were compressed to
one-liners with pointers, because REFERENCE.md already carried their depth. Net: **223 bytes freed**
while *adding* the new Retry-After rule. 7,957 → 7,842, headroom 43 → 158.

Moved to REFERENCE.md because it was **not** there yet: the current signing *policy* (never pin;
`APPLE_SIGNING_IDENTITY` overrides; **CI cannot catch it** because `ci.yml` runs no `tauri build`) and
the `tauri build` DMG/`hdiutil` failure. The code-signing section documented the *pinned* fix as correct;
it now records that the pin was superseded on 2026-09-22.

**Two contradictions found and fixed while reading:**
1. MEMORY.md said `pgrep SecurityAgent` "is the tell"; REFERENCE.md §Keychain measured that a resident
   `SecurityAgent` **proves nothing** — it is a persistent agent. The compressed claim was the wrong one;
   it was dropped.
2. REFERENCE.md's **"CI is dead for billing reasons"** section still carried the stale claim Tushu
   corrected earlier today. Rewritten: the block was private-repo-only, the repo went public 2026-09-21
   ~21:33 UTC, and a ~9-second failure is the old signature while a multi-minute failure did real work.
   Also fixed the line that told the reader to "use this, not CI".

**Stale counts corrected in three places** (all measured, not assumed): MEMORY.md, REFERENCE.md
§Test counts, and `COUNT_TOKENS_PLAN.md`. router-core 231 → 239 → **241**; Rust `--lib` 433 → **470**.
`vitest 193` and `browser 98` were already right.

**`COUNT_TOKENS_PLAN.md`'s verification curl was wrong.** It hardcoded `127.0.0.1:8787`, but the
compiled `DEFAULT_PORT` is only a fallback — `settings.gateway` overrides it, and the live row reads
`{"port":8800}`. So the documented command targeted whatever else holds 8787. Replaced with a
`sqlite3` lookup of the live port (verified: returns `8800`). The same 8787-for-our-gateway error appears
in ARCHITECTURE.md, MASTER_PROMPT.md, DECISIONS.md, SECURITY_AUDIT and TOOL_CALL_DIAGNOSIS — **left
alone deliberately**, since the README (newest) already says 8800 and a five-file sweep is a bigger
change than the finding warrants.

**Structural note:** MEMORY.md is at its information-density limit. Further distillation yields ~50 bytes
per rule at the cost of the index's usefulness. The real fix is a higher ceiling or a second injected
file, not more shaving.

### The 8787 sweep — and why the fix was *not* `8787 → 8800`

Earlier I offered to sweep the other docs that name 8787 as the router's endpoint. Doing it properly
turned on a distinction that changes the answer:

**`8787` is the compiled `DEFAULT_PORT` (`gateway.rs:33`); `8800` is a persisted setting.** The literal
`8800` appears nowhere in source. So for a *fresh clone* the gateway really does listen on 8787 —
hardcoding 8800 would be a new bug, not a fix. The truthful statement is that the port is
**configurable**, default 8787, currently 8800 because AI Hub v2 also claims 8787 (REFERENCE.md
§The 8787 collision — the move was done 2026-09-21).

**Split by forward-looking vs. dated, which is the part worth remembering:**

| doc | verdict |
|---|---|
| `ARCHITECTURE.md` (prose + diagram), `MASTER_PROMPT.md` (3 spots) | forward-looking → fixed to `<port>` + a note |
| `DECISIONS.md` | a *reusable check* ("curl … to test background mode") → annotated, since a reader would run it today |
| `SECURITY_AUDIT_2026-09-20.md`, `TOOL_CALL_DIAGNOSIS_2026-09-20.md` | **dated measurements — left alone.** 8787 was correct on 2026-09-20; rewriting them would falsify the record |
| `GATEWAY_MEMORY_LAYER.md:1050` | **correct as written** — that 8787 is AI Hub's MCP server, not ours |

So the sweep was 3 files, not 5. The two 2026-09-20 files and the AI Hub line are the *right* thing to
leave, and a blanket find-and-replace would have corrupted an audit trail.

The `ARCHITECTURE.md` ASCII diagram at §3.3 was safe to edit only after checking it is a loose
left-aligned flow with no closing border — a boxed diagram would have needed re-padding.

### Backlog accounting — two items were already done, one is closed

Tushu's five-item list had **three stale premises**. Verified each against the tree rather than the note:

1. **Honour upstream `Retry-After` — DONE.** Stage 2 landed earlier today, and the fold was then
   corrected from longest to shortest (see above).
2. **`/v1/messages/count_tokens` — DONE, not a 404.** The list said "not implemented, Claude Code calls
   it, we 404". The route is registered (`gateway.rs:1757`), the handler and `count_text_chars` exist
   (`gateway_anthropic.rs:63,135`), and **three route-level tests prove a 200**:
   `count_tokens_returns_estimate`, `count_tokens_unauthenticated_is_401`, `count_tokens_invalid_json_is_400`
   (`gateway_tests.rs:373,395,413`). The first is explicitly "before any bridge is consulted — no upstream".
3. **`previous_response_id` — CLOSED, and for a different reason than the list gave.** The list said
   `wire_api="chat"` avoids it; Codex's actual config says `wire_api = "responses"`. It is moot anyway:
   `disable_response_storage = true` kills server-side continuity, and `base_url` points at
   **OmniRoute on 20128**, not this gateway. Do not build a response store.
4. **`cache_control` — real cost, blocked on measurement.** Confirmed the premise from the ledger:
   `agnes-2.5-flash`, 648 requests, **36.0M input tokens vs 213K output** (~55.5K in/request). But
   `prompt_tokens_details.cached_tokens` appears **nowhere** in the codebase and the ledger has no column
   for it, so whether the upstream already caches is unknowable. Neither active manifest mentions caching.
   Order is measure → decide → rework; the measurement needs a real migration.
5. **`thinking` blocks — unchanged, quality-only, last.**

**The finding worth keeping: nothing in this system records a request path or body.** `ledger` is
metadata only (no path, no body, no error message); `gateway.log` is worker heartbeats only. So
"does client X call endpoint Y?" cannot be answered from telemetry — a verification step of that shape
must be checked for feasibility *before* it is promised. Detail: REFERENCE.md §What the gateway does NOT record.

Also noted while reading `~/.codex/config.toml`: a live `ANTHROPIC_AUTH_TOKEN` sits in
`[shell_environment_policy.set]`, which injects it into every shell Codex spawns. File perms are `0600`,
so it is not world-readable and this is **not** urgent — but anything running in those shells inherits it,
so it can reach logs or screenshots. Worth moving to a keychain reference if that block is ever copied.
